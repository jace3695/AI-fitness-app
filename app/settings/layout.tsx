import type { Metadata } from "next";
export const metadata: Metadata = { title: "통합 설정 | AI 연이", description: "공통 계정·PIN·생체인증 설정" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
