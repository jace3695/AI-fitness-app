import type { Metadata } from "next";
export const metadata: Metadata = { title: "통합 달력 | Jace AI Hub", description: "할 일·운동·식단·언어 통합 달력" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
