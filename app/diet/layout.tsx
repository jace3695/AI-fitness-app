import type { Metadata } from "next";
export const metadata: Metadata = { title: "식단 관리 | Jace AI Hub", description: "운동 기록과 연동되는 독립 식단 관리" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
