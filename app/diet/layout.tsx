import type { Metadata } from "next";
export const metadata: Metadata = { title: "식단 | AI 연이", description: "운동 기록과 연결되는 AI 연이 식단 관리" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
