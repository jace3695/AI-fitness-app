# 끼니별 식단 명령 검증 — 2026-09-16

## 지원 범위

- `오늘 점심 단백질 30g 기록해줘`, `오늘 저녁 단백질 27g 기록해줘`
- `오늘 점심 밥 130g 기록해줘`, `오늘 저녁 밥 80g 기록해줘`
- 오늘(한국 시간) 점심 또는 저녁의 식품 단백질 g·조리된 밥 무게 g를 **해당 끼니의 총량으로 교체**한다. 한 명령에는 수치 하나를 받는다. 단백질은 0~100g, 밥량은 0~1,000g 정수이며 0g도 명시적으로 기록할 수 있다.
- 음식의 무게에서 단백질을 추정하거나 공기·인분을 g으로 추정하지 않는다. 다른 날짜·추가량·여러 수치·불확실한 문장은 저장하지 않는다.
- 15분 확인 화면, 같은 탭에서 이동·새로고침 후 복구, 동일 요청 재확인, 기존 식단 실행 이력과 되돌리기를 사용한다. 기존 물·메모 명령과 과거 이력은 기존 형식을 유지한다.

## 원본 보호

- 단백질 변경은 끼니 상세, 하루 합계 저장소, 일별 요약 합계를 한 트랜잭션으로 갱신한다. 다른 끼니·아침/오후/저녁 보충제·점심 보충제의 기록된 값을 유지한다. 기존 합계와 상세가 불일치하면 수정 전에 식단 화면 확인을 요청한다.
- 밥량 변경은 해당 끼니의 밥 상세와 선택값을 갱신한다. 밥 종류가 기존에 있으면 보존하고, 없으면 `기타 / 종류 미기록`으로 둔다. `estimatedCarbs`는 기존 화면의 참고 계산(조리된 밥 g × 0.3의 반올림)을 유지하며 실제 탄수화물 측정값으로 취급하지 않는다.
- 기존 계산 정의에 따라 단백질 목표·점심 단백질 포함·저녁 밥량 요약 플래그를 해당 변경 범위에만 맞춘다. 식사 완료 여부, 마지막 식사 시각, 공복, 물, 메모, 일정, 다른 날짜와 알 수 없는 추가 필드는 보존한다.
- 끼니 값만 처음 저장해도 식단 화면에서 표시되도록 빈 필드를 중립적인 화면 기본값과 합친다. 저장하지 않은 `18:30`을 새 기록에 만들지 않는다. 외식 모드에서도 기록한 점심 식품 단백질 g를 볼 수 있다.
- 확인 당시의 관련 일별 저장소·초기화 표식을 DB에서 다시 비교한다. 이후 수정이 있으면 저장·되돌리기를 거절한다. 없는 키/날짜, 기존 JSON 문자열 저장 형식, 다른 날짜를 보호한다.
- 기존 `assistant_diet_command_history`와 소유자 RLS, 초기화 잠금, Auth 삭제 외래 키를 재사용한다. 새 함수는 `security invoker`, 빈 `search_path`, 인증 사용자 실행 권한으로 제한한다.

## 최종 검증 결과

- 단위/DB: **473/473**, VM 동기화 **27/27**. 점심·저녁 각각의 수치, 0, 경계·오류 입력, 합계 불일치, 저장·이력 원자성, 재시도, 이력 격리, 초기화, 원본 복구 포함.
- 타입·린트·독립 Webpack 빌드 통과. 로컬 기본 Turbopack 빌드는 공유 `node_modules` 심볼릭 링크가 프로젝트 루트 밖을 가리키는 환경 제한으로 중단됐으며, Webpack 빌드는 통과했다. CI의 실제 설치 환경에서도 기본 Turbopack 빌드를 통과했다.
- **126/126 통과**, 새 끼니별 명령 **8/8**(Chromium 4개·작은 WebKit 4개). [최종 CI 35101755073](https://github.com/jace3695/AI-fitness-app/actions/runs/35101755073), 소스 `0fdebdeb13eec80982b59314fd68e7d5136f497e`. 이번 최종 소스의 첫 CI에서 모두 통과했다.
- 호스팅 Preview 실제 저장·재조회·충돌 보호·되돌리기 확인. 원본 상태 복구를 확인했고 요청 만료 후 임시 이력·대화를 정리하고 20개 테이블 원본 해시 일치를 확인했다.
- 정식 앱 반영·PR 병합·실제 iPhone 검증은 마지막 단계로 유지한다. 6단계 전체는 부분 완료다.


## 호스팅 Preview 관찰

- 소스 `0fdebdeb13eec80982b59314fd68e7d5136f497e`, Preview `dpl_3aneNWTPvM9vx9AHGsnxhGXWbALM` READY, `target: null` 및 Preview 별칭 연결 확인.
- 호스팅 마이그레이션 `assistant_diet_meal_commands` 적용 전후 허가된 테스트 계정의 20개 테이블 행 수·내용 해시 일치. 보안 점검은 기존 내부 테이블 정책 없음 6건과 비밀번호 유출 보호 설정 1건이며 새 함수 관련 경고는 없었다. 기존 안내: [내부 테이블 RLS 정책](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy), [유출 비밀번호 보호](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection).
- 13:26 UTC 단백질 확인 요청을 새로고침으로 복구하고 저장(점심 30g). 13:27 UTC 저녁 조리된 밥 80g 저장. 실행 이력 2건, 각각 해당 끼니 값만 기록했고 식사 시각·공복·식사 완료를 만들지 않았다. 음성/AI 호출은 실행하지 않았다.
- 식단 화면으로 링크 이동 및 새로고침 후 점심 단백질 30g, 저녁 밥 80g, 기록된 단백질 합계 30g 재조회.
- 먼저 점심 명령을 되돌리면 이후 기록 보호 오류가 표시되고 상태를 유지한다. 저녁 밥량부터 되돌린 뒤 점심 명령을 되돌려 원본 상태 8개 키를 정확히 복구했다. 같은 화면 재조회·새로고침에서 식품 단백질과 합계가 다시 미기록인 것을 확인했다.
- 13:42 UTC 마지막 요청의 15분 유효시간과 여유 5초가 지난 뒤, 원본 상태와 갱신 시각이 복구 확인 때 그대로인지 잠금 안에서 대조했다. 확인한 ID·사용자·본문·생성 시각으로 검증 명령 이력 2건·대화 4건만 삭제하고, 테스트로 변한 상태 행의 갱신 시각을 작업 전 값으로 복구했다. 20개 테이블의 행 수·전체 행 내용 해시가 작업 전과 모두 일치한다. 기존 성장 루틴 3건, 언어 기록, AI·Zephyr 사용량 이력도 그대로다.


## 격리 검사와 남은 범위

- CI의 단위/DB 473개·VM 27개·lint·TypeScript·기본 Turbopack 빌드·브라우저 126개가 모두 통과했다.
- 126개 정리 보고에서 합성 Auth 계정 155개 제거, 관련 행 잔여 0, 외부 origin 요청 0을 확인했다. 검증 DB·볼륨·키 제거 단계와 합성 검증 결과 업로드도 성공했다.
- 브라우저 흐름은 확인 전 원본 유지, 취소, 같은 탭 복구, 저장 응답 유실 후 재시도, 저장 화면과 재조회, 320px 화면, 끼니별 수치·합계 일치, 외식 모드 표시, 두 세션 충돌, 이후 직접 입력 보호, 반대 순서 되돌리기 거절과 정상 역순 복구, 소유자 격리·만료를 포함한다.
- 실제 iPhone·Siri에서의 호출, 정식 앱 반영과 PR 병합은 이번 단계에 포함하지 않았다. 음식 무게에서 영양값 추정, 여러 수치 동시 명령, 과거 날짜, 식사 시간·공복·상세 수행 명령은 후속 범위다. 무료 모드·기존 Zephyr 승인 설정과 일일 분석 예약을 유지했다.
