# AI 연이 앱 전체 개선 — 구현 및 검증 기록

작성: 2026-09-09 KST

작업 브랜치: `fix/app-wide-reliability`

기준 운영 커밋: `902cd27579892a56ca68caceb6419f43b709b24b`

## 현재 상태

> 최신 후속: `b22818a`에서 PATCH·확인 GET 본문 대조는 완료했고, 로그인 직후 원본 17개 키 삭제는 실제 실패로 남긴다. 이번 후속 수정은 로그아웃 시 기록·기준값 정리와 이전 세션 요청 취소를 보완했다. 격리 회귀 **25개**, 자동 검사 **216개**, 린트·타입·빌드 통과. **수정 후 실제 로그인·재로그인 보존 검증은 미완료**이며 Draft를 유지한다. 마지막 ‘로그인·로그아웃 동기화 수명 수정’ 절을 우선 참고한다. 아래 이전 요약과 통과·실패 이력은 당시 결과로 보존한다.

> 후속 수정 `f90adf4`에서 오래된 달력 스냅샷 저장과 화면 이탈 후 대기 저장을 수정했다. 격리 12개 및 기존 자동 검사 200개가 통과했고 실제 신규 저장·연속 수정·삭제 후 화면 이탈에서 원본 보존을 확인했다. 독립 세션·실제 작은 화면·네트워크 오류·본문·백그라운드·업데이트 검증은 남아 Draft를 유지한다. 아래 ‘저장 경로 수정과 회귀 검증’을 우선 참고한다.

- [Draft PR #189](https://github.com/jace3695/AI-fitness-app/pull/189), 작업 브랜치 `fix/app-wide-reliability`에 구현했다. 운영 앱 병합·배포는 하지 않았다.
- 자동 검사 **200개**, 린트, TypeScript 검사, 프로덕션 빌드가 통과했다.
- 새 SQL을 격리된 PGlite에서 먼저 검증한 뒤, 미리보기의 실제 저장 검증을 위해 연결된 Supabase 프로젝트에 **20260908233141** 마이그레이션을 적용했다. 운영 DB에도 반영된 추가 스키마 변경이며, 기존 기록을 일괄 수정하거나 삭제하는 마이그레이션은 아니다.
- `7368a7096b0993a5989db9f0e250b786118c64fc` 미리보기에서 사용자가 선택한 계정으로 로그인하여 운동·식단·가계부·반복 할 일·성장 코칭의 실제 UI와 서버 값을 대조했다.
- 검증 중 찾은 운동 동기화의 더보기 탭 의존, 가계부 오전 날짜 해석, 지출 상세 삭제 확인 누락, 비서 대화의 반복 완료 경로를 추가 보완했다. 추가 수정 `7253300`에서 자동 동기화·지출 삭제 확인·비서 대화 반복 완료를 재검증했다. 달력 미응답 표시/수정과 금액 분석을 보완한 최종 코드 `061c3d1`도 미리보기에 배포했다. 마지막 두 화면 검증은 브라우저 연결 시간초과로 남았다.
- 합성 운동·식단·거래·반복 일정·성장 루틴·코칭을 정리했다. 기존 운동 기록·운동 설정·성장 루틴의 값이 시작 전과 정확히 일치함을 서버에서 확인했다.
- 320px/390px, 독립 세션 간 동기화 경합, 요청 전체 네트워크 대조와 백그라운드·업데이트 흐름은 이번 변경 기준으로 아직 실검증하지 않았다. 이전 서킷 PR의 통과 결과와 구분한다.

미리보기: https://ai-fitness-app-git-fix-app-wide-reliability-jace3695s-projects.vercel.app

첫 실검증 배포: https://ai-fitness-8h048tufb-jace3695s-projects.vercel.app (`7368a70`)

추가 실검증 코드: `72533008a36a390d0e75e32578c1872b18083217`, 배포 `dpl_HcnawHy2uDrq31ipf2c1z79ojPxa`.

최종 코드: `061c3d1b7e892fd2fac619fbff023218c5184aeb`, 배포 `dpl_HApzb385U5qg7rJhr97Zj4bubZNz` READY.

최종 코드 고정 미리보기: https://ai-fitness-kgwu52fqx-jace3695s-projects.vercel.app

## 구현 내용

| 영역 | 바뀐 동작 | 자동 검증 또는 코드 검토 |
| --- | --- | --- |
| 가계부 묶음 저장 | 지출·수입·저축과 저장 요청 식별자를 한 DB 트랜잭션으로 처리한다. 실패하면 전체 취소한다. 응답 유실 시 같은 요청을 재사용하여 중복 저장을 막는다. | SQL 중간 실패·혼합 입력·같은 요청 재시도·다른 계정 접근 테스트 |
| 가계부 날짜 | 오전 0~9시에도 화면의 현지 날짜로 오늘·어제·그제를 해석한다. AI 요청과 로컬 분석, 월별 조회의 UTC 날짜 변환을 수정한다. | 한국 오전·월초 경계 테스트 2개 |
| 가계부 금액 | 이름·날짜의 숫자보다 통화 단위가 명시된 금액을 우선한다. 천 단위 쉼표를 거래 구분 기호로 잘못 나누지 않는다. | 사용처 숫자·날짜·천 단위·여러 건·소수 배수 테스트 2개 |
| 가계부 입력 | 결과가 불명확한 요청은 기기에 보존하고 재접속 시 복구한다. 확인 전에는 요청 내용을 바꾸지 못하도록 하며, 서버가 전체 취소를 확인한 경우에는 수정할 수 있다. | 클라이언트 흐름 검토, 타입·빌드 검사 |
| 운동·식단 동기화 | 요청 시점과 응답 시점의 기기 기록을 다시 비교해 요청 중 생긴 변경을 보존한다. 서버에 아직 없는 변경은 반영 대기로 표시하고 후속 동기화를 예약한다. 운동 화면 진입부터 동기화를 유지하여 더보기 탭을 열지 않아도 작동한다. | 최신 메모·반복수 보존, 독립 원격 수정 합치기, 대기 중 삭제 보존 테스트 |
| 반복 할 일 | 목록 버튼과 비서 대화에서 현재 회차 완료와 다음 회차 생성을 함께 저장한다. 재시도·완료 취소 후 재완료에도 다음 회차는 하나만 만든다. 다른 곳에서 수정된 항목은 다시 확인하도록 한다. | SQL 실패 시 완료 취소, 재시도, 월말·한국 날짜, 다른 계정 접근 테스트 |
| 성장 코칭 | 선택한 루틴 변경과 결정 이력을 한 번에 저장한다. 이전 조회 이후 루틴이 달라졌으면 전체 적용을 거부한다. 같은 결정 재시도는 중복 적용하지 않는다. | 두 번째 루틴 충돌 시 첫 변경도 취소, 적용·유지·재시도·소유권 테스트 |
| 운동 응답 | 새 기록과 달력의 새 기록·수정에 허리 상태·난이도·피로의 양호한 초기값을 넣지 않는다. 달력 상세도 미응답으로 표시한다. 피로는 바로 선택할 수 있게 표시한다. 미응답을 제안 근거로 사용하지 않고 부족한 날짜를 알려준다. | 미응답 시 증가·교체 보류 테스트, 기존 안전 판단 회귀 테스트 |
| 운동 작성 중 | 외부 기록 갱신으로 작성 중인 피드백·운동 체크 입력을 덮어쓰지 않도록 한다. 실제 수정이 있을 때 업데이트 새로고침을 보류한다. | 코드·타입 검사; 아래 실제 저장·재조회 결과 참고 |
| 식단 | 현재 제공하는 식사·수분·시간·일정 입력에 맞게 안내를 수정한다. 여러 저장 항목의 이전 값을 보관하고 저장 실패 시 복원하며 작성한 내용은 유지한다. 여행 일정 저장·삭제 실패도 표시한다. | 중간 저장 실패·공간 부족·중단된 저장 복구 테스트 |
| 홈·비서 | 조회 실패 시 이전 값을 유지하고 실패한 영역·마지막 정상 확인 시각·재시도 버튼을 표시한다. 앱 복귀 시 갱신한다. 조회 중 저장 요청이 완료되면 후속 조회를 빠뜨리지 않는다. | 겹치는 조회와 저장 후 재조회, 실패 후 재시도 테스트 |
| 통합 캘린더 | 로딩·조회 실패·실제 빈 날짜를 구분한다. 실패한 자료의 마지막 표시를 유지하고 재시도할 수 있다. 복귀·연결 회복·로컬 기록 변경 때 갱신한다. | 코드·타입 검사; 실제 오류 화면 대기 |
| 삭제·설정 | 가계부 삭제 확인과 취소를 공통 대화상자로 제공한다. 설정 진입에서 공통 설정을 먼저 안내하고 기록 관리는 아래에 둔다. | 메뉴 키보드 순환·Escape·초점 복귀, 수입·저축 삭제 확인/취소 실검증; 후속 `7253300`에서 지출 확인·취소·확정 통과 |
| 공통 안내 | 운동·언어 동기화 및 PWA 알림을 하단 메뉴와 안전 영역 위로 배치한다. 서버 반영 상태와 기기 저장 상태를 구분한다. | CSS·상태 처리 검토; 320px/390px 실화면 대기 |
| 타이머·업데이트 | 타이머를 실제 경과 시간으로 계산하고 화면 복귀 때 갱신한다. 작성 중에는 서비스워커·동기화가 요청한 자동 새로고침을 보류한다. | 백그라운드 시간 경과 계산 테스트; 브라우저 복귀·업데이트 대기 |

미응답 처리는 앞으로 입력하는 기록에 적용한다. 이미 저장된 운동 기록의 응답을 추정하여 바꾸지 않는다. 기존 운동 라이브러리·루틴 종류·과거 계획·날짜 예외·캐릭터와 앱별 기능은 유지한다.

## 검사 결과

실행한 명령:

```sh
npm test
npm run lint
npm run build
git diff --check
```

- 테스트: 200 passed / 0 failed / 0 skipped.
- 린트: 오류 없음.
- 빌드: 컴파일·TypeScript 검사·46개 정적 페이지 생성 완료.
- diff 공백 검사: 통과.
- PGlite 기반 SQL 검사 9개는 실제 PostgreSQL 엔진에서 마이그레이션의 함수를 호출한다. 임의 실패 제약과 두 합성 계정의 RLS 조건으로 전체 취소·재시도·소유권을 검증한다. 운영 DB·운영 계정 검증으로 대체하지 않는다.
- 로컬 저장 테스트는 단일 실행 환경의 실패·중단 복구를 확인한다. 독립 탭의 동시 쓰기까지 검증했다고 해석하지 않는다.
- 테스트 실행기를 정리하여 기존 `lib`의 AI JSON 테스트와 새 DB 테스트도 `npm test`에 포함했다. PGlite는 개발 의존성이다.

## DB 적용 및 확인

마이그레이션: `supabase/migrations/20260908233141_app_wide_reliability.sql`

추가 항목:

1. 가계부 요청 결과를 식별하는 `budget_save_batches` 및 소유자 RLS.
2. `save_budget_batch(uuid, jsonb)`.
3. 반복 항목의 부모 식별자, 중복 방지 인덱스, 동일 소유자 외래 키.
4. `set_assistant_item_completion(uuid, boolean, timestamptz)`.
5. `decide_growth_review(uuid, jsonb, jsonb)`.

함수는 `SECURITY INVOKER`, 빈 `search_path`, 로그인·소유자 검사를 사용한다. 공개·익명 실행 권한을 제거하고 로그인한 계정에만 실행 권한을 부여한다. 기존 기록의 수정·삭제·중복 정리는 포함하지 않는다.

연결된 Supabase 프로젝트에 적용한 버전은 `20260908233141`이다. 적용 후 3개 함수 모두 `SECURITY INVOKER`, 익명 실행 불가, 로그인 계정 실행 가능이며 요청 테이블의 RLS 활성화를 재확인했다. 보안 Advisor의 새 경고는 없었고 기존 유출 비밀번호 보호 비활성화 경고 1건만 남았다. 인증 설정은 변경하지 않았다.

## 기존 실검증 보존

[기존 서킷 검증 문서](https://github.com/jace3695/AI-fitness-app/blob/c2f6d63da2277e67f96c1bd30e7293e254d8bc40/docs/adaptive-circuit-review.md)의 결과는 그대로 유지한다.

- 증상 저장·수정·재조회·보류, 증가·교체 적용과 복원, 준비 조건, 삭제 확인·취소.
- 복원·동기화·새로고침 3회 후 45세트 유지, 날짜별 5종목·15세트, 과거 계획·날짜 예외 유지.
- 독립 세션 간 양방향 저장·수정·삭제 전파, 요청 56건의 200 응답과 요청 식별자 대조, 삭제 기록 재등장 없음.
- 당시 합성 기록 정리와 원본 기록·설정 일치 확인.

위 내용은 이전 커밋에서 완료한 검증이다. 이번 변경의 인증 후 브라우저 검증을 완료했다는 의미로 다시 기록하지 않는다.

## 이번 변경의 실제 브라우저 검증

검증일: 2026-09-09 KST. 최초 미리보기 `7368a70`, 실제 로그인 계정의 허가된 합성 기록만 사용했다. 인증 정보는 읽거나 복사하지 않았으며 결과 문서에도 포함하지 않는다. 아래 서버 대조는 읽기 전용 SQL로 확인했다.

| 영역 | 실제 조작과 결과 |
| --- | --- |
| 운동 미응답 | 허리·난이도·피로를 고르지 않고 일부 수행과 합성 메모만 저장했다. 새로고침 후 미응답이 유지되며 임의의 정상 응답이 생기지 않았다. |
| 운동 수정·재조회 | 허리 불편 없음·보통 난이도·피로 3 및 수정 메모를 저장했다. 서버 값과 기록 달력의 상세가 일치했다. 삭제 취소 후 기록이 유지되고 확정 후 합성 기록만 제거됐다. |
| 식단 | 500mL와 합성 메모를 입력하고 동기화 버튼을 눌러도 입력이 남았다. 저장·새로고침 후 서버의 메모와 수분 값이 같았다. 오늘 기록 초기화 후 합성 식단·수분 항목이 제거됐다. |
| 가계부 전체 취소 | 합성 지출·수입·저축 3개 중 두 번째 수입에 DB 정수 범위를 넘는 금액을 넣어 실제 저장 실패를 만들었다. 서버의 거래 3종과 요청 결과가 모두 0건이었다. 입력은 남고 수정할 수 있었다. |
| 가계부 재시도 | 수입을 2,000원으로 고치고 저장을 연속 클릭했다. 지출 1,000원·수입 2,000원·저축 500원이 각각 1건, 저장 요청 결과도 1건이었다. 상세 화면과 서버 금액이 일치했다. |
| 삭제 확인 | 수입 삭제 대화상자의 최초 초점이 취소에 위치했고, 취소 후 기록이 남았다. 수입·저축 확정 삭제를 확인했다. 지출 상세의 확인 누락을 발견해 후속 수정했다. 검증용 지출만 삭제됐으며 원본을 삭제하지 않았다. |
| 반복 할 일 | 매일 반복 합성 할 일을 만들고 완료를 연속 클릭했다. 완료 취소·재완료 후에도 부모 1개와 다음 회차 1개뿐이었다. 다음 회차는 한국 시간 다음 날 23:59였다. 새 항목이 목록에 나타났으며 두 합성 항목을 삭제했다. |
| 성장 코칭 충돌 | SQL로 만든 합성 루틴 A/B와 합성 제안을 UI에서 선택했다. 적용 직전에 B만 변경하자 실제 RPC가 전체 적용을 취소했다. A 10분/B 21분을 유지했고 결정은 미저장 상태였다. UI가 최신 값을 표시하고 재확인을 안내했다. |
| 성장 코칭 재시도 | 최신 값으로 연속 클릭 적용 후 A 15분/B 25분과 두 선택의 결정 이력이 함께 저장됐다. 새로고침 후 결정이 유지되고 재적용 버튼이 사라졌다. AI 생성 품질 검증으로 해석하지 않는다. |
| 공통 메뉴 | 메뉴 대화상자의 최초 초점, Shift+Tab으로 마지막 항목 순환, Escape 닫기, 전체 메뉴 버튼으로 초점 복귀를 DOM으로 확인했다. 공통 설정과 앱별 화면 이동도 확인했다. |

### 합성 데이터 정리와 원본 보존

- 운동 기록 전체와 운동 설정의 전후 해시가 각각 동일했다. 테스트 날짜 운동 값은 제거됐고 기존 4일의 기록은 그대로였다.
- 테스트 날짜의 식단·수분 값이 제거됐다. 전체 클라우드 JSON이 바이트 단위로 같다는 주장은 하지 않는다. 앱의 빈 저장소 초기화 항목은 생길 수 있다.
- 가계부 합성 지출·수입·저축 각 0건. 두 차례 검증에서 만든 요청 결과 각 1건도 정확한 식별자로 제거해 0건이다.
- 비서 할 일과 대화 메시지는 시작 전과 동일한 0건이다. 마지막 대화 정리 확인창에서 브라우저 연결이 끊겨 해당 합성 메시지 2건은 정확한 식별자와 본문 조건으로 서버에서 제거했다.
- 합성 성장 루틴 2개와 코칭 1개를 정확한 식별자로 제거했다. 원본 루틴 2개의 전체 값 해시가 시작 전과 같고 코칭은 0건이다.
- 테스트 오류를 만들기 위한 새 실패 제약이나 인증 설정 변경은 운영 DB에 추가하지 않았다.

### 후속 실검증 (`7253300`)

- 운동의 오늘 화면에 진입했을 때 서버 반영 상태와 동기화 버튼이 보였다. 더보기·수동 동기화를 누르지 않고 운동 화면에서 합성 기록을 저장하자 서버에 동일 메모가 반영됐다.
- 이 기록의 서버 난이도·피로는 미응답이었으나 달력 상세가 적당함·2/5로 표시하는 문제를 발견했다. 달력에서 메모만 수정할 때 임의 응답이 저장될 수 있는 경로까지 `061c3d1`에서 수정했다.
- 가계부 날짜 필드가 한국 현재 날짜 2026-09-09로 표시됐다. 오전 9시 이전의 날짜 경계 자체는 단위 테스트로 확인했으며 이 후속 UI 조작은 오전 9시 이후였다.
- 합성 지출을 저장한 뒤 상세 내역에서 삭제 확인창 표시·취소 시 보존·확정 삭제를 확인했다. 서버에서도 해당 지출은 0건이었다.
- 합성 식별자의 숫자를 금액으로 읽는 로컬 분석 문제를 발견했다. `061c3d1`에서 통화 단위 우선 및 천 단위 쉼표 보존을 수정했다. 저장 전 입력 수정 기능으로 올바른 금액만 저장했으며 잘못 읽힌 금액은 저장하지 않았다.
- 비서 대화에서 합성 반복 할 일을 완료했다. 완료 부모 1건과 한국 시간 다음 날 23:59의 자식 1건이 함께 저장됐고 화면에서도 동일했다. 합성 부모와 자식은 삭제했다.
- 운동 기록은 UI에서 삭제한 직후 앱을 이동하여 서버 반영이 대기 중이었다. 브라우저 연결 종료 후 정확한 날짜와 합성 메모 조건으로 이 테스트 날짜만 서버에서도 제거했다. 원본 운동/설정 해시는 시작 전과 일치했다. 앱 이동 직전 대기 동기화의 이어서 반영은 후속 검증 항목이다.

### 남은 실검증과 연결 상태

- 대화 삭제 버튼의 기본 확인창 동작 중 `Input.dispatchMouseEvent timed out`이 발생했다. 이후 탭 조회·DOM 조회 및 취소 키 전송도 `CDP operation refresh tabs timed out after 20000ms`로 실패했다. 브라우저 연결 문제이며 이것만으로 앱 저장 장애를 단정하지 않는다. 인증 우회나 다른 브라우저 구현으로 대체하지 않았다.
- 최종 코드 `061c3d1`의 **달력 미응답 표시 → 메모만 수정 → 재조회**, **숫자가 있는 사용처·천 단위 금액 입력 결과**는 연결 복구 후 실제 화면에서 확인해야 한다. 테스트 200개와 최종 린트·빌드는 통과했고 미리보기는 READY이다.
- 이번 브라우저가 제공하는 실제 화면은 1359×936이다. 홈에서 가로 넘침이 없음을 확인했으나 320px/390px을 실제 시험한 결과가 아니다.
- 독립 세션 동기화 경합·요청 중 새 입력 보존·전체 요청별 네트워크 대조, 저장 결과 불명확 상태에서 새로고침, 조회 오류·복귀 갱신, 타이머 백그라운드 복귀 및 PWA 업데이트 보류의 전체 실제 조작은 남아 있다. 관련 단위·SQL 테스트 통과와 구분한다.
- `AGENTS.md`의 작은 화면·핵심 흐름 검증 요구에 따라 PR은 draft로 유지하고 운영 앱 병합·배포는 하지 않는다.

## 추가 기능으로 남긴 범위

운동 중량·밴드 강도·라운드 증가 제안의 확장, 식사 사진 분석, 삭제 후 복구 보관함, 앱 전체의 추가적인 시각 재설계는 이번 안정성 개선에 포함하지 않았다. 현재 지원하는 흐름을 먼저 검증한 뒤 별도 변경으로 평가할 수 있다.

## 최종 코드 후속 검증 — 2026-09-09 18:23~18:40 KST

**원본 운동 기록 누락을 발견하여 전체 실검증은 통과로 처리하지 않는다. PR은 Draft 유지, 운영 병합·배포 없음.** 아래 결과는 기존 통과 이력을 지우거나 대체하지 않고 추가한다.

### 환경과 판정

- A: 브랜치 미리보기, 배포 `dpl_AfggUKt7Q7Ce1Nw1YvggH6ZgmLT9`, 문서 커밋 `45d18a8`.
- B: 별도 origin의 고정 미리보기 `kgwu52fqx`, 배포 `dpl_HApzb385U5qg7rJhr97Zj4bubZNz`, 코드 `061c3d1`.
- 두 탭 모두 동일한 허가된 검증 계정으로 별도 로그인했다. 같은 origin의 새 탭으로 독립 세션을 대신하지 않았다.
- 새로 확인한 범위만 아래에 기록한다. 기존 저장 트랜잭션·반복 일정·성장 코칭·삭제 확인 통과는 반복하지 않았다.

| 항목 | 실제 결과 |
| --- | --- |
| 달력 미응답 → 메모만 수정 → 새로고침 | 9/2 합성 버드독 1세트 8회와 일부 완료만 입력. 난이도·피로·허리 미선택. 달력은 미응답/미기록으로 표시했고 메모 수정·새로고침 후에도 유지됐다. SQL에서 난이도·피로·허리 응답 필드가 생기지 않았음을 확인. 이 제한된 흐름은 통과. |
| 숫자 사용처·천 단위 금액 | `오늘 GS25 QA-PR189 카페 1,000원 체크카드, 점심 9,000원, 월급 2,500,000원, 적금 5만원`을 화면에서 분석. 4건의 금액 1000/9000/2500000/50000, 합계 2560000으로 표시. 금액 분석 통과. 첫 사용처는 ‘미분류’로 나와 이름 보존은 별도 관찰사항. 저장하지 않고 입력을 비웠다. |
| A → B → A 전달 | A의 9/4 합성 기록을 B에서 새로고침하여 확인. B에서 메모를 바꿔 저장한 뒤 서버 및 A 새로고침에서 같은 메모 확인. 전달 자체는 확인했으나 아래 원본 누락 때문에 독립 세션 전체를 통과로 판정하지 않는다. |
| 운동 작성 중 입력 | A의 달력 편집 메모를 미저장 상태로 두고 B에서 동일 합성 기록 수정. A에서 동기화 직후 메모를 추가 입력했으며, 서버 반영 완료 후에도 추가 입력이 유지됐다. 취소 후 새로고침하면 B의 저장 메모 표시. 실제 요청 지연을 제어하거나 입력 시점과 응답 시점을 계측하지 않았으므로 엄밀한 ‘요청 중 새 저장’ 경합 전체 통과로 확대하지 않는다. 운동 세션의 체크·피드백 전체 검증도 아니다. |
| 식단 작성 중 원격 갱신 | A에서 500mL와 미저장 합성 메모 작성, B에서 합성 운동 삭제. A 동기화 후 ‘다른 기록이 갱신됐어요. 작성 중인 내용은 그대로 보존하고 있습니다.’와 원래 500mL·메모를 확인. 저장하지 않고 입력을 원상 복귀한 후 새로고침. |
| 삭제 직후 다른 앱 이동 | A에서 9/2 합성 운동 삭제 직후 가계부로 이동. 후속 SQL에서도 합성 기록이 남았고 운동 화면 복귀 후 제거됨. 앱을 떠나 있는 동안의 대기 삭제 반영은 미완료/문제 관찰로 유지한다. |
| 타이머 | 3분 동작 타이머 시작·일시정지에서 약 10초 경과와 02:50 표시 확인. ‘저장 없이 종료’로 임시 진행 정리. 실제 백그라운드 복귀 검증은 하지 못했으므로 기존 미완료 유지. |
| 320px / 390px | 제공된 viewport API로 각각 크기를 요청했지만 DOM 실제 폭은 두 번 모두 1294px, 문서 폭 1290px였다. 작은 화면 테스트 성립 안 함. 임시 override는 reset했다. |

### 발견한 차단 문제: 초기 동기화/첫 저장 구간의 원본 누락

1. 시작 전 서버 운동 기록은 기존 4일이며 해시가 `68a05c5cb7a5f40cc3a9506ac2d6aef3`이었다. 운동 설정 해시는 `b5344dc2e68cc141e9709d701f690df3`.
2. 새 A 미리보기에서 초기 ‘기록 동기화 중’ 표시를 거쳐 9/2 합성 기록을 생성·수정했다. B의 수정 이전 서버 운동 전체 해시는 `3825b07ef64e922649e16f5bde7fd7c6`였다.
3. 사후 읽기 전용 계산에서 이 해시는 **9/2 합성 기록만 있는 JSON**과 정확히 일치했다. 원본 4일 + 합성 기록의 해시는 `f6a3b93f57a745a8beaa401ce135aabf`로 달랐다. 따라서 누락 시점은 B의 후속 수정 전이며, B의 수정·삭제 때문에 원본이 사라졌다고 단정하지 않는다.
4. 합성 날짜를 모두 삭제한 후 서버 운동 JSON은 빈 객체였다. 합성 날짜만 조작했음에도 기존 날짜가 누락된 것이므로 원본 보존 요구 실패다. 정확한 원인은 초기 클라우드 병합·첫 로컬 저장·운동 방향 초기 적용 구간의 추가 조사가 필요하다.
5. HTTP 200은 올바른 데이터 보존을 보장하지 않았다. 아래 네트워크 정상 응답으로 이 문제를 통과 처리하지 않는다.

### 요청별 네트워크 대조 범위

Supabase Logs Explorer의 읽기 전용 쿼리로 09:23:00 ≤ UTC < 09:35:00, 두 preview referer, `/rest/v1/user_app_state`만 범위를 고정했다. `request_id` 배열과 메서드·상태를 읽어 **58건**을 대조했다.

| Origin | GET | OPTIONS | PATCH | 상태 |
| --- | ---: | ---: | ---: | --- |
| A 브랜치 | 22 | 8 | 7 | 모두 200 |
| B 고정 배포 | 13 | 5 | 3 | 모두 200 |

주요 PATCH 요청 식별자:
- A: `01a0857c-da18-7bc3-9431-8eaf92eb30bd`, `01a0857d-1437-7cee-b522-ba19b1dbee7a`, `01a0857d-de65-7154-ad24-01bed2ef0a3e`, `01a08580-1bb8-7761-91b5-b7a2a37f0f66`, `01a08580-90f1-7a82-a9fc-e132514c19f7`, `01a08582-3f96-7180-8ead-564660c2c8e1`, `01a08582-5366-77ec-af67-a377e844cb91`.
- B: `01a08581-d64f-7029-9c5b-64ccd87e8a88`, `01a08583-c6f0-7879-9d82-ceb7f69915f2`, `01a08584-fedd-7bae-a114-2a8e18877494`.

이는 브라우저 HAR나 요청·응답 본문 전체 캡처가 아니다. 최종 저장 내용은 UI와 별도의 SQL 조회로 대조했다. Vercel 실행 로그도 확인했으나 요청 식별자·본문을 모두 제공하지 않으므로 앱 전체 모든 요청 대조 완료로 확대하지 않는다.

### 정리·원본 복원과 잔여 차이

- 합성 9/2·9/4 운동은 UI로 삭제, 식단·가계부 합성 입력은 저장하지 않고 비웠다. 운동 타이머는 저장 없이 종료했다. 이번 검증에서는 거래·비서·성장 데이터를 생성하지 않았다.
- 누락 발견 후 두 탭을 운동 화면에서 이동해 동기화를 중지했다. 보관된 원본 백업의 운동 4일 해시가 시작 전과 일치하는지 먼저 확인했다.
- 서버 운동 값이 빈 객체인 경우에만 적용하는 조건부 SQL로 **운동 기록 키 하나만** 복원했다. 현재 설정이나 다른 기록을 일괄 덮어쓰지 않았다.
- 복원 후 두 탭을 다시 동기화·새로고침했다. 양쪽 8월 달력에서 원래 기록 표시를 확인했고, 서버에서 기존 4일 및 운동 전체 해시 `68a05c5cb7a5f40cc3a9506ac2d6aef3`, 운동 설정 해시 `b5344dc2e68cc141e9709d701f690df3`의 일치를 확인했다.
- 다만 전체 클라우드 상태가 완전히 같은 것은 아니다. 내부 `ai-fitness-workout-direction-backup`의 해시가 바뀌었고 `savedAt`은 이번 B 첫 진입 시각인 `2026-09-09T09:30:43.575Z`였다. 이 내부 백업 객체의 시작 전 원문은 보관되어 있지 않아 임의 복원하지 않았다. 실제 적용 운동 설정은 시작 전과 같다.
- 기존 통과 결과는 역사적 결과로 보존한다. 새 미리보기의 원본 보존 실패를 해결하기 전 추가 개인 계정 쓰기 검증은 중단한다.

### 아직 남은 검증

원본 누락 원인 수정·회귀 확인, 앱 이탈 중 대기 삭제 완료, 실제 320px/390px, 계측 가능한 요청 중 쓰기 경합, 전체 앱 요청·본문 대조, 응답 유실 후 재접속 복구, 조회 오류·재시도, 실제 백그라운드 타이머 복귀, 실제 서비스워커 업데이트 보류는 미완료다. 제공된 브라우저에는 네트워크 지연·오프라인·응답 유실 제어가 없어 임의 저장소 수정이나 인증 우회로 결과를 만들어내지 않았다. 운영 배포로 업데이트 테스트를 유발하지 않았다.

## 후속 격리 검증 — 2026-09-09 19:04 KST

검증 기준은 PR head `ecb925a5aebd28bb4494df076844b9aff7aadaef`이며 애플리케이션 코드는 `061c3d1` 이후 바뀌지 않았다. 이 대화에서 브라우저 탭 조회를 재시도했지만 `CDP operation refresh tabs timed out after 20000ms`로 실패했다. 아래 결과는 **실제 브라우저 통과 결과가 아닌 격리 코드 검증**이다. 이전 통과 결과와 18:23~18:40의 실제 계정 검증·복원 기록을 그대로 보존한다.

### 방법과 실행

- `scripts/qa-pr189-sync.mjs`가 현재 소스의 달력 저장 함수, `CloudSyncPanel`의 동기화 effect, 저장소·병합·쿼리 모듈을 실행한다. 함수 본문을 다시 구현하지 않고 TypeScript 구문에서 가져와 컴파일한다.
- 브라우저·React 렌더러·인증·실제 HTTP·DB는 사용하지 않는다. 서로 독립된 메모리 저장소, 모의 서버, 이벤트, 가상 시간을 사용한다. 서버는 요청과 조건부 갱신을 기록하며 응답 대기·경합·반영 후 응답 유실·조회 오류를 지정한 시점에 발생시킨다.
- 모든 데이터는 `2030-01-01`~`2030-01-04`의 합성 원본 4일, `2030-02-02`의 합성 추가 기록 및 `fixture-*` 설정이다. 개인 계정, 개인 백업, 실제 설정을 읽거나 변경하지 않았다. 합성 값은 프로세스 메모리에만 존재하고 실행 종료 시 정리된다.
- 실행: `node scripts/qa-pr189-sync.mjs`. 결과 **10개 중 7개 통과, 3개 실패, 종료 코드 1**. 실패를 예상 성공으로 뒤집거나 무시하지 않는다. 새 검증 스크립트의 ESLint 및 변경 공백 검사를 통과했다. 애플리케이션 코드를 변경하지 않아 기존 200개 검사·빌드 결과는 역사적 결과로 유지하며 이번에 다시 실행했다고 보고하지 않는다.

### 격리 시나리오 결과

| 시나리오 | 결과 | 확인 범위 |
| --- | --- | --- |
| 초기 GET 대기 중 첫 로컬 기록 추가 | 통과 | 병합·서버·기기에 원본 4일과 추가 1일 유지 |
| PATCH 응답 대기 중 추가 수정 | 통과 | 늦은 응답 이후 추가 수정 유지, 대기 표시와 후속 저장 완료 |
| PATCH 응답 대기 중 삭제 | 통과 | 후속 저장 및 새 저장소에서 삭제 날짜 재등장 없음 |
| 독립 저장소 2개의 동시 수정 | 통과 | 조건부 갱신 충돌 표시 후 포커스 재시도로 양쪽 변경 보존 |
| 서버 반영 후 응답 유실 | 통과 | 오류 표시 후 재접속·재조회, 반영된 기록을 중복 PATCH하지 않음 |
| 조회 실패와 재시도 | 통과 | 오류 안내·기록 유지, 실패 중 쓰기 없음, 포커스 재시도 정상화 |
| 달력의 동기화 전 스냅샷으로 첫 저장 | **실패** | 서버 원본 4일이 사라지고 새 날짜 1일만 남음 |
| 최신 스냅샷으로 같은 달력 저장 실행 | 통과 | 대조군에서 원본 4일과 추가 1일·설정 보존 |
| 삭제 직후 동기화 컴포넌트 이탈 | **실패** | 가상 60초 후에도 삭제가 기기에만 있고 서버에는 기존 날짜가 남음 |
| PATCH 대기 중 추가 수정 후 이탈 | **실패** | 첫 값만 서버에 저장되고 마지막 수정은 기기에만 남음 |

### 실패 원인으로 확인한 코드 경로

1. **달력의 오래된 전체 스냅샷 저장:** `RecordCalendarView`는 마운트 시 한 번 읽은 `stores`를 보관하며 동기화의 기록 변경 이벤트를 구독하지 않는다. `saveNewWorkoutRecord`는 이 `stores.workouts`를 펼쳐 `writeWorkoutStore` → `writeJson`으로 전체 키를 교체한다. 달력이 초기 동기화 전에 빈 기록을 읽었다고 가정하고 실제 저장 함수와 실제 동기화 effect를 연결하면, 클라우드에서 받은 원본 4일이 제거된 상태가 서버로 전파된다. 최신 스냅샷을 사용한 대조군은 5일을 보존했다. 이 경로는 실검증의 누락 양상과 일치하지만, 실제 사고 당시 React 실행 시점·요청 본문까지 확보해 유일한 원인으로 확정한 것은 아니다.
2. **화면 이탈 후 미전송 변경:** `CloudSyncPanel`의 cleanup은 `active = false`로 만들고 후속 타이머를 취소한다. 이미 저장된 로컬 변경을 서버에 반영하기 전에 컴포넌트가 사라지면 후속 동기화가 실행되지 않는다. 진행 중 PATCH 뒤에 추가한 변경 역시 이탈 후 서버에 전달되지 않았다. 운동 화면과 달리 가계부에는 해당 동기화 컴포넌트가 없으며 공통 루트에도 배치되어 있지 않다.

저장 직전 최신 원본을 기준으로 선택한 날짜·필드만 수정하고, 편집 중 초안 보존과 원본 저장소 갱신을 함께 처리하는 수정이 필요하다. 화면 이탈 후에도 저장 대기를 처리할 동기화 수명 관리도 필요하다. 이번 커밋은 검증 스크립트와 결과 문서만 추가하며 기능 수정은 포함하지 않는다.

### 실검증 상태와 보존

엄밀한 실제 브라우저 경합·응답 유실·조회 오류 검증을 위 모의 시나리오로 통과 처리하지 않는다. 실제 320px/390px, 전체 앱 요청·응답 본문 대조, 백그라운드 타이머, 실제 서비스워커 업데이트 보류는 여전히 미완료다. 실제 계정에 대한 추가 쓰기 검증은 기록 누락 수정 후 복구된 브라우저에서 진행해야 한다.

이번 실행은 개인 기록·설정을 변경하지 않았으며 기존 운동 4일 복원과 해시 일치, 내부 운동 방향 백업 객체의 잔여 차이는 앞 절의 확인 범위 그대로 유지한다. PR은 Draft를 유지하고 운영 병합·배포를 진행하지 않는다.

## 저장 경로 수정과 회귀 검증 — 2026-09-09 19:30~19:50 KST

기준 `8af46d4`에서 시작해 기능 수정 `f90adf4e6296e28d16740233f8d716f8f286ee62`를 같은 Draft PR에 추가했다. 기존 통과·실패·원본 복원 이력은 위에 그대로 보존한다. 운영 병합·배포와 DB 변경은 하지 않았다.

### 수정 내용

- 달력의 운동 저장·수정·삭제는 저장 직전에 기기 저장소를 다시 읽고, 선택한 날짜의 해당 운동 필드만 바꾼다. 오래된 전체 `stores.workouts`를 덮어쓰지 않는다. 일반 운동을 수정해도 같은 날짜의 최신 유산소·철봉·폼롤러 필드는 보존한다. 날짜별 메모 저장·삭제도 최신 저장소에서 선택 날짜만 수정한다.
- 달력은 기록 변경·저장소 이벤트를 구독한다. 운동·보조 운동 편집이 열려 있으면 원격 갱신으로 초안을 초기화하지 않고, 선택 날짜가 바뀌거나 편집을 마치면 최신 기록을 반영한다. 작성 중 달력도 공통 새로고침 보류에 참여한다.
- `CloudSyncPanel`을 운동·식단 페이지에서 제거하고 공통 루트 레이아웃에 한 번 배치했다. Next.js 클라이언트 화면 이동 중 같은 동기화 인스턴스가 유지되어 500ms 대기 타이머와 진행 중 요청 뒤의 후속 저장이 가계부에서도 계속된다. 로그인 전에는 중복 로그인 폼을 표시하지 않는다.
- 내부 운동 방향 백업 키를 복원하거나 덮어쓰는 코드는 추가하지 않았다.

### 자동·격리 회귀

수정 전에 원래 스크립트의 **7 통과 / 3 실패**를 그대로 재현했다. 수정 후 원래 10개 시나리오와 추가 2개 시나리오가 **12 통과 / 0 실패**다.

추가 시나리오는 오래된 달력 스냅샷으로 저장할 때 같은 날짜의 원격 유산소 필드 보존, 루트에 동기화 인스턴스가 하나만 있고 개별 페이지에는 없는지 확인한다. 이탈 검증 어댑터는 실제 소스의 루트 소유 여부에 따라 페이지 이탈과 루트 전체 종료를 구분하도록 변경했다. 화면을 떠나도 루트 effect를 유지하며 최종 서버 값 단언은 그대로 유지했다. 이는 React 라우터 실행 자체를 시뮬레이션한 결과가 아니라 코드 기반 수명 모형이다.

- `node scripts/qa-pr189-sync.mjs`: 12 passed, 0 failed.
- `npm test`: 200 passed, 0 failed.
- `npm run lint`: 통과.
- `npm run build`: 컴파일·TypeScript·46개 페이지 생성 통과.
- `git diff --check`: 통과.
- Windows 체크아웃에서 기존 SQL 범위 테스트 1개가 CRLF 때문에 실패해, 테스트 입력의 줄바꿈만 정규화했다. SQL 및 기존 단언을 약화하지 않았고 이후 200개가 모두 통과했다.
- 이 격리 검증의 응답 유실·조회 오류·경합 통과를 실제 브라우저/HTTP 통과로 바꾸어 표기하지 않는다.

### 수정본 실제 브라우저

데스크톱 브라우저 연결을 확인했다. 수정본 배포 `dpl_6ocJpqCrz3YE5ryK6mp1sFgJt7cG`가 `f90adf4`와 일치하고 READY임을 확인했다. 브랜치 미리보기를 새로고침해 공통 동기화 상태가 홈·가계부에서도 표시되는 것을 확인했다.

| 검증 | 관찰과 판정 |
| --- | --- |
| 달력 신규 저장 후 원본 보존 | 빈 9/2에 `QA-PR189-f90` 운동 1세트 8회 저장. 서버에 합성 날짜가 추가되고 기존 4일의 전체 값 및 나머지 16개 키가 시작 전과 같았다. 실제 신규 저장 흐름 통과. 초기 GET 지연과 오래된 React 스냅샷의 정확한 시점 재현은 위 격리 검증으로 구분한다. |
| 연속 수정 후 화면 이탈 | 메모를 수정1 → 마지막 수정2로 연속 저장한 직후 가계부로 이동. 가계부에 머문 상태에서 서버 메모가 마지막 수정2임을 확인. 실제 이탈 후 최종 값 반영 통과. 첫 PATCH를 인위적으로 지연하지 못했으므로 정확한 요청 중 추가 쓰기 타이밍은 격리 검증 범위다. |
| 삭제 직후 화면 이탈 | 합성 9/2 운동 삭제 직후 가계부로 이동. 운동 화면에 복귀하지 않고 서버에서 합성 날짜 제거 및 전체 상태 원상 복귀를 확인. 이전 실패 흐름의 실제 회귀 통과. |
| 작은 화면 | viewport API로 320×800 및 390×844를 요청했지만 실제 `innerWidth`는 둘 다 1294, 문서 폭 1290. 실제 작은 화면 검증 성립 안 함. override reset 완료. |
| 백그라운드 타이머 | 타이머를 시작하고 브라우저 숨김을 요청했지만 `document.visibilityState`가 계속 `visible`. 실제 백그라운드 복귀로 인정하지 않는다. 표시 상태를 복귀시키고 타이머는 ‘저장 없이 종료’하여 임시 진행을 정리했다. |
| 독립 세션 | 수정본의 별도 origin `https://ai-fitness-420w3g808-jace3695s-projects.vercel.app/` 탭을 준비했지만 아직 로그인 화면이다. 이전 고정 배포 `kgwu52fqx`로 수정본의 독립 세션 검증을 대신하지 않았다. |


### 수정본 네트워크 관찰

Supabase Logs Explorer에서 UTC 10:43~10:47, 브랜치 preview referer, `/rest/v1/user_app_state`를 범위로 잡았다. PATCH 5건의 식별자와 HTTP 200을 확인했다.

| UTC | request_id | 상태 |
| --- | --- | --- |
| 10:44:19.421 | `01a085c4-c15f-7aea-8fcc-23d9d041e173` | 200 |
| 10:44:50.113 | `01a085c5-3941-7276-98f7-1954843ea41d` | 200 |
| 10:44:50.970 | `01a085c5-3c99-7820-a350-96a872396b17` | 200 |
| 10:45:29.984 | `01a085c5-d500-7b11-93d7-e34cc6cb5d3c` | 200 |
| 10:46:36.099 | `01a085c6-d743-7071-847d-0a0f0dd13e7e` | 200 |

합성 신규 저장·연속 두 수정·삭제의 시각과 요청을 대조하고 별도 서버 조회로 최종 값을 확인했다. 마지막 요청은 타이머 종료 구간이다. 이 로그는 요청·응답 본문 전체가 아니며, HTTP 200만으로 보존을 판정하지 않았다. 타이머 종료 후에도 전체 상태 해시가 시작 전과 같음을 다시 확인했다.

### 이번 원본 보존과 정리

브라우저 쓰기 전에 실제 클라우드 17개 키 전체를 비공개 작업 백업으로 보관했다. 합성 운동을 UI로 삭제하고 타이머 임시 진행을 정리했다. 거래·식단·비서·성장 합성 데이터는 이번에 생성하지 않았다.

삭제 후 서버 전체 JSON의 PostgreSQL MD5가 시작 전과 같은 `38219aa258571ec18705211a8a191e57`임을 확인했다. 기존 운동 4일과 설정뿐 아니라 **내부 운동 방향 백업 객체를 포함한 17개 키 전체가 동일**했다. 앞선 검증에서 남은 내부 백업 차이를 임의 복원하지 않았으며, 이번 시작 시점의 값을 그대로 보존했다. 서버 쓰기 SQL로 정리하거나 원본을 다시 복원할 필요가 없었다.

### 남은 항목과 필요한 환경

PR은 Draft로 유지한다. 수정본 독립 세션 로그인 후 경합 검증이 필요하다. 실제 320px/390px을 적용할 수 있는 브라우저, 요청 지연·반영 후 응답 차단·조회 실패 및 요청/응답 본문을 확인할 수 있는 검증 환경이 필요하다. 현재 제공된 브라우저 API에는 해당 네트워크 제어·본문 캡처 기능이 없다. 실제 백그라운드 전환과 서비스워커 업데이트 발생·보류도 미완료다. 임의의 저장소 조작이나 인증 우회, 운영 배포로 검증 조건을 만들지 않았다.

## 합성 전용 브라우저·HTTP 환경과 추가 검증 — 2026-09-09

기능 수정 `f90adf4`, 직전 문서 `923a8a9`를 유지하고, 데스크톱 내장 브라우저 연결을 확인한 뒤 `scripts/browser-qa/`에 재실행 가능한 로컬 검증 환경을 추가했다. 이번에는 개인 계정 탭을 조작하거나 실제 Supabase 데이터를 조회·수정하지 않았다. 기존 통과 결과와 내부 운동 방향 백업의 잔여 차이 이력은 그대로 유지한다.

### 환경과 검증 범위

실제 `RecordCalendarView`, `CloudSyncPanel`, `PwaManager`, 동기화 병합 코드와 앱 CSS를 브라우저에서 실행한다. `127.0.0.1:8871`과 `:8872`는 별도 origin 저장소를 사용하며, 같은 합성 서버 행을 공유한다. 인증과 Supabase 클라이언트는 전용 어댑터이고, 서버는 메모리의 합성 데이터만 사용한다. 환경 파일·개인 백업을 읽지 않으며 외부 연결은 CSP로 제한한다.

논리 GET/PATCH는 실제 HTTP **POST JSON envelope**로 전달한다. 따라서 아래 본문 대조는 실제 브라우저와 로컬 HTTP 서버 사이의 요청·응답이며, 운영 Supabase SDK의 GET/PATCH wire 또는 실제 인증/RLS 검증은 아니다. 화면 이동은 실제 React 달력 unmount와 공통 동기화 유지이며 Next 라우터 자체 검증과 구분한다. 실행·오류 제어·정리 절차는 [환경 안내](../scripts/browser-qa/README.md)에 있다.

### 추가 관찰

| 검증 | 관찰과 판정 |
| --- | --- |
| 실제 320px·390px 문서 | 두 iframe의 `innerWidth`는 320/390, 문서 scrollWidth는 316/386으로 측정됐다. 앱 CSS가 적용된 달력·기록 상세를 스크린샷으로 확인하고 두 폭에서 입력·저장·수정·삭제를 실행했다. 확인한 컴포넌트에 수평 넘침이 없었다. 전체 앱 메뉴·모든 페이지·실제 휴대폰 검증을 대신하지 않는다. |
| PATCH 대기 중 추가 수정 후 이탈 | A의 요청 #20을 서버 반영 후 응답 대기 상태로 유지했다. 메모를 `QA189-delayed-LAST`로 추가 저장하고 달력을 해제한 뒤 응답을 풀었다. 후속 #22가 마지막 값을 저장했다. 최초 합성 4일과 설정·백업 보존 통과. |
| 독립 저장소 동시 쓰기 | A의 GET #37이 오래된 응답을 보유한 동안 B가 다른 날짜를 #40으로 저장했다. A의 조건부 PATCH #41은 `data:null`로 거절됐고, #43 재조회 후 #44에서 두 날짜를 함께 저장했다. A에서 B의 운동, B에서 A의 최종 메모와 양쪽 ‘서버 반영 완료’를 확인했다. 합성 인증 환경의 경합·양방향 수렴 통과. |
| 반영 후 응답 유실 | B의 #27은 저장 후 실제 소켓을 끊었다. 동일 요청의 재전송 #28은 오래된 조건으로 거절됐다. #29가 반영된 메모를 읽고 #30이 다시 저장했다. 기록과 상세는 중복되지 않았다. 추가 PATCH가 없었다고 주장하지 않는다. 유실 직후 오류 문구는 포착하지 못했으며 최종 복구와 본문을 확인했다. |
| 조회 오류와 재시도 | B의 GET #32는 HTTP 503을 반환했다. 화면에 ‘기록 동기화 실패’, `fixture query unavailable`, ‘다시 시도’가 나타났다. 재시도 #33은 기존 기록을 반환했고 이후 정상 상태로 복구됐다. |
| 실제 서비스워커 업데이트 보류 | 실제 `public/sw.js`의 말미 버전 주석만 변경해 브라우저 업데이트를 발생시켰다. A의 미저장 메모 `QA189-unsaved-update-draft`가 유지되고 ‘지금 갱신’이 disabled였다. 편집 취소 후 버튼이 활성화되어 업데이트를 적용했고, 재로딩 뒤 저장된 메모 `QA189-A-concurrent`가 유지됐다. 로컬 실제 worker lifecycle 통과. 설치형 모바일 PWA 검증은 아니다. |
| 삭제 직후 이탈 | A에서 합성 9/7 삭제 후 달력을 해제했고 #53에서 삭제가 반영됐다. B에서도 9/6 삭제 후 이탈했으며 #61의 저장 상태가 시작 fixture 전체와 정확히 같았다. |

### 요청 본문 증거·정리·검사

[합성 요청·응답 72건](../scripts/browser-qa/evidence/2026-09-09.json)을 보관했다. `request`는 전달한 envelope, `response`는 서버가 계산한 응답, `delivery`는 실제 전달 또는 소켓 종료다. 유실된 응답이 브라우저에 도착했다고 해석하면 안 된다. 개인 식별자·인증 토큰·개인 기록은 포함하지 않는다.

`node scripts/browser-qa/audit.mjs scripts/browser-qa/evidence/2026-09-09.json` 통과. PATCH 10건(#20, #22, #27, #28, #30, #40, #41, #44, #53, #61)의 본문 모두에서 기존 합성 4일과 설정·내부 백업 객체의 정확한 값 보존, 운동 상세 중복 없음, 경합 재시도·오류 복구·마지막 수정과 삭제 결과를 단언했다. HTTP 200만으로 저장 성공을 판정하지 않는다. #28/#41은 200이어도 `data:null`인 조건부 쓰기 거절이다.

합성 운동 두 날짜를 UI로 삭제한 뒤 두 세션의 로컬 저장소·서비스워커·캐시를 정리했다. 마지막으로 합성 서버를 비웠고, 대기 응답 0개·서버 상태 `{}`를 확인했다. 보관한 증거는 실행 중인 운동 데이터와 구분한다. 개인 기록·설정은 이번 실행에서 접근하지 않았으므로 앞 절의 실제 계정 해시 일치를 새로 측정한 것으로 재표기하지 않는다.

환경 추가 후 전체 lint와 TypeScript 검사 통과. 기존 200개 검사·12개 격리 회귀·앱 빌드 통과 이력은 이전 수정 검증 결과로 유지하며 이번에 다시 실행했다고 표기하지 않는다.

### 여전히 필요한 실검증

- 수정본 미리보기의 실제 인증 두 세션 경합과 Supabase 요청·응답 본문 대조. 이번 합성 인증 결과로 이 항목을 닫지 않는다.
- 전체 앱의 320px·390px 화면 및 실제 휴대폰/설치형 PWA 동작.
- 실제 `visibilityState=hidden` 전환·기기 백그라운드 정지 후 타이머 복귀. iframe은 계속 visible이므로 통과 처리하지 않는다.
- 실제 미리보기의 업데이트 발생과 입력 보류, 최초 조회가 늦는 실제 인증 달력의 저장 시점 검증. 컴포넌트/격리 통과와 구분한다.

PR은 Draft로 유지하며 운영 병합·배포는 하지 않는다.

## 실제 인증 세션·작은 화면·아이폰 후속 확인 — 2026-09-09 20:43 KST 이후

앞의 합성 환경 통과 결과는 그대로 유지한다. 이번에는 실제 로그인 상태인 고정 미리보기 A (`420w3g808`, `f90adf4`)와 브랜치 미리보기 B (`r3hw9jf46`, `2c903ff`)를 사용했다. Vercel에서 두 배포가 READY인 것을 확인했고, 두 커밋 사이 앱 코드와 `public/sw.js`의 차이는 없다. 별도 origin의 두 로그인 세션임을 UI에서 확인했다.

### 실제 저장: 새 실패 관찰

개인 클라우드 상태를 비공개 백업한 뒤 빈 9/1에 A의 `QA189-live-A`, 빈 9/2에 B의 `QA189-live-B`를 각각 1세트 7회로 준비하고 저장 버튼을 연속 실행했다. 이후 서버에는 A만 남았고 B의 9/2는 없었다. B는 ‘서버 반영 완료’를 표시하며 달력에서도 9/2 기록이 사라졌다. 최초 두 요청의 정확한 중첩 시각·본문은 확보하지 못했으므로 조건부 PATCH 충돌의 발생이나 삭제 주체를 단정하지 않는다.

B에서 단독 대조로 `QA189-live-B-retry`를 다시 저장했다. 직후 UI에 운동 이름, `2 운`, ‘기기 기록 · 서버 반영 대기’가 나타나는 것을 확인했다. 이후 A/B UI와 서버에서 다시 9/2가 없고 ‘서버 반영 완료’가 표시됐다. **실제 기록 보존은 실패**다. 합성 환경의 경합 통과로 이 실패를 상쇄하지 않는다. 요청·응답 본문과 다른 활성 기기의 영향까지 확인해야 원인을 특정할 수 있다. 아이폰 검증도 같은 계정으로 진행되어 두 데스크톱 외 활성 세션이 있었다는 조건을 남긴다.

추가 합성 저장은 중단했다. 남아 있던 A의 9/1은 UI 삭제했다. 기존 운동 4일과 설정·내부 운동 방향 백업을 포함한 원래 17개 키의 값은 시작 시점과 같았다. 내부 백업 객체의 과거 잔여 차이는 임의로 수정하지 않았다.

### 실제 문서 폭과 화면 확인

이번 연결에서는 viewport 변경이 실제 문서 폭에 반영됐다. A는 320px, B는 390px이며 각각 문서 `scrollWidth` 316/386이었다. 도중 390px 변경은 선택된 B 탭에 적용되어 A는 여전히 320px인 것을 측정으로 구분했다. 요청한 크기만으로 판정하지 않았다.

홈(`/`), 운동, 가계부, 식단, 언어, 자기계발, 통합 달력, 통합 설정의 주요 화면을 두 폭에서 열고 스크린샷·DOM으로 확인했다. 공통 전체 메뉴, 운동 기록 입력/저장/삭제와 390px 가계부 입력 화면도 확인했다. 관찰한 화면의 가로 넘침은 없었다. 모든 학습 단계·설정 대화상자·모바일 키보드·실제 아이폰의 전체 앱 화면을 망라한 통과는 아니다. 거래·학습·식단·설정 값은 변경하지 않았다.

### 아이폰: 사용자 관찰과 아직 필요한 정보

사용자가 실제 아이폰 Safari에서 확인했다. 앱 전환 전 남은 시간 2분 45초, 복귀 후 2분 41초였다고 보고했고, 다른 앱 체류 시간은 약 2~4초라고 추가 확인했다. 짧은 앱 전환 구간의 진행 시간은 대체로 일치한 사용자 관찰로 기록한다. 장시간 백그라운드 정지나 OS 프로세스 종료 복구를 통과한 것은 아니다. 화면 잠금 전후 시간은 사용자가 미확인으로 답했고, 기종·iOS 버전도 확보하지 못했다.

사용자는 **‘저장 없이 종료’ 후 재접속하여 운동을 시작했을 때 이전 2분 23초에서 멈춘 진행이 복구됐다**고 보고했다. 임시 진행 정리 실패로 기록한다. 에이전트가 아이폰을 직접 조작하거나 화면을 관찰한 결과와 구분한다. 코드에서는 임시 진행을 동기화 대상 접두사 아래 저장하고, 종료 시 `localStorage.removeItem`을 직접 호출하는 경로를 확인했지만 이것만으로 원인을 확정하지 않았다.

20:56 KST 서버 재조회에서는 원래 17개 키가 모두 동일했고, 새 임시 진행 키 `ai-fitness-workout-session-draft:2026-09-09:1619637713`만 추가되어 있었다. 사용자가 확인 중인 운동을 임의로 서버 삭제하지 않았다. 이후 사용자가 ‘저장 없이 종료 후 홈 이동 완료’를 확인했지만 서버의 임시 키는 남았다. 데스크톱에서도 해당 검증 운동을 열었을 때 ‘이전 진행상태에서 이어서 시작했습니다’, 일시정지 타이머 **02:23**을 직접 확인했다.

데스크톱에서 검증 운동의 ‘나가기 → 저장 없이 종료 → 지금 동기화’를 실행해 정리했다. 21:00 KST 서버 재조회에서 임시 키가 제거되고 **17개 키 전체의 PostgreSQL MD5가 시작 전과 동일한 `38219aa258571ec18705211a8a191e57`**로 복귀했다. 기록을 완료 저장하거나 DB 쓰기 SQL로 복원하지 않았다. 자동 종료 정리가 실패한 이력을 명시적 동기화 후 정리 성공으로 대체하지 않는다. 검사 후 화면 크기 override를 해제했으며 현재 선택한 화면이 1294px로 돌아온 것도 확인했다.

### 네트워크·업데이트·최초 조회 지연

현재 연결된 브라우저 기능에는 화면 크기/표시 제어, DOM, 콘솔 로그가 있으나 요청 지연·응답 차단·요청/응답 본문 캡처 기능은 없다. 이번 SQL 조회는 서버 최종 상태 대조이며 Supabase 요청 본문 캡처가 아니다. 따라서 실제 요청별 본문 대조와 최초 GET을 지연한 달력 저장은 미완료다.

두 미리보기의 서비스워커 파일은 동일하다. 새 waiting worker를 발생·관찰하지 못했으므로 실제 미리보기 업데이트 보류를 통과 처리하지 않는다. 앞의 로컬 실제 worker lifecycle 결과는 해당 환경의 결과로 유지한다.

새 실제 보존 실패와 아이폰 임시 진행 복구 문제를 해결·회귀 확인하기 전까지 PR은 Draft다. 이 후속 실행에서는 앱 코드 수정, DB 쓰기 SQL, 운영 병합·배포를 하지 않았다.

## 실제 실패 후 코드 보완과 격리 회귀 — 2026-09-09

`4691665`의 실패 이력과 앞선 통과 범위를 보존한다. 이번 작업은 개인 계정·서버 데이터를 조회하거나 쓰지 않고 별도 작업 폴더의 합성 메모리 서버에서 수행했다.

### 확인한 경로와 수정

- `WorkoutSession`의 실제 ‘저장 없이 종료’ 클릭 핸들러는 임시 진행을 `localStorage`에서 삭제하지만 동일 문서의 공통 동기화에 변경을 알리지 않았다. 이 때문에 즉시 동기화 예약이 없고 서버의 임시 진행이 다음 주기까지 남았다. 종료와 운동 완료 시 삭제 알림을 발행해 공통 동기화가 처리를 이어가도록 했다. 종료 버튼은 삭제가 성공한 다음에만 임시 진행 자동 저장을 중단한다. 현재 방식은 삭제 알림 후 500ms 동기화 예약이며, 브라우저 강제 종료·오프라인에서도 서버 삭제가 즉시 완료된다는 보장은 아니다.
- 저장 경로는 조건부 쓰기 성공 응답만으로 동기화 기준을 갱신했다. 합성 제3 세션이 PATCH 반영 후 응답 도착 전에 전체 행을 덮어쓰는 상황을 주입하면, 기존 코드는 서버에서 새 기록이 없어도 완료로 표시했다. INSERT와 성공한 조건부 UPDATE 뒤 서버 상태를 재조회하고 전체 JSON 내용이 일치할 때만 성공을 반환하도록 보완했다. 불일치·확인 조회 실패 시 기준을 갱신하거나 로컬을 원격 값으로 덮지 않고 오류를 표시하며, 재시도는 마지막 확인 기준으로 병합한다.
- 재조회는 성공한 쓰기당 GET 1건을 추가한다. 다른 기기의 정상 변경도 확인 사이에 발생하면 불일치 안내 후 재시도가 필요할 수 있다. 확인 시점 이후의 타 기기 덮어쓰기까지 방지하는 잠금은 아니다. **실제 9/2 기록 소실의 삭제 주체·요청 본문은 여전히 미확보이므로 원인 해결 완료로 판정하지 않는다.** 제3 세션 덮어쓰기는 재현 가능한 가설이며 실제 사건의 확정 원인이 아니다.

### 검사 결과

`node scripts/qa-pr189-sync.mjs`는 실제 동기화 effect·저장/병합 함수와 실제 종료 버튼 핸들러를 실행한다. 브라우저·React 렌더러·실제 인증·HTTP 통신은 사용하지 않으며 서버·시간·이벤트를 격리한다.

- 수정 전: 기존 12개 통과, 새 보존/종료 검사 2개 실패를 확인했다.
- 수정 후: **17개 통과·0개 실패**. 기존 12개와 다음 5개를 확인했다: 성공 응답 뒤 제3 세션 덮어쓰기 감지 및 로컬 입력 보존·재시도, 실제 종료 핸들러의 자동 서버 삭제·새 기기 재조회, 저장 확인 GET 오류 복구, 확인 GET 대기 중 마지막 입력 보존, 임시 진행 PATCH 대기 중 종료 후 늦은 응답에 의한 재복구 방지.
- `npm test`: **200개 통과·0개 실패**. 전체 lint와 TypeScript 검사, `npm run build`의 46개 페이지 생성 통과. 최초 빌드는 작업 폴더 외부를 가리키는 의존성 심볼릭 링크 때문에 실패했으며, 의존성을 별도 작업 폴더에 복사한 뒤 동일한 기본 Turbopack 빌드로 통과했다. 앱 설정이나 빌드 방식을 바꿔 통과시킨 것은 아니다.

### 실검증 차단과 후속 절차

이 대화의 브라우저 탭 조회를 다시 실행했으나 `CDP operation refresh tabs timed out after 20000ms`로 실패했다. 따라서 이번 수정의 인증 후 UI·아이폰 검증은 수행하지 못했다. AGENTS.md의 실사용 검증 요구에 따라 기존 실제 실패를 통과로 바꾸지 않고 Draft를 유지한다.

데스크톱에서 새 수정본을 검증할 때는 다른 활성 세션의 origin·앱 버전을 기록하고, 먼저 격리 환경에서 성공 응답 직후 덮어쓰기·확인 GET 실패를 재현한다. 실제 인증 재검증은 원본 17개 키 백업 후 합성 날짜만 사용하여 요청/응답 본문, 저장 확인 GET, 두 UI와 최종 서버 상태를 대조해야 한다. 아이폰은 ‘저장 없이 종료’ 후 수동 동기화 없이 삭제 전파를 기다린 다음 같은 기기·다른 기기에서 재진입하여 확인한다. 화면 잠금·전체 앱 모바일·실제 preview 업데이트·최초 조회 지연은 별도로 남는다.

이번 합성 상태는 프로세스 메모리에만 생성되어 검사 종료와 함께 폐기됐다. 개인 기록·설정·내부 백업을 변경하지 않았으며, 원본 17개 키 해시는 이번에 다시 측정한 것이 아니다. DB 스키마·권한·인증 설정 변경, 운영 병합·배포는 하지 않는다.

## `508b4ca` 인증 UI 재검증 — 2026-09-09 21:49 KST 이후

문서의 기존 통과·실패 이력을 유지한다. 기준 커밋은 `508b4cac6c364352e3dca2a7d874dff02e99ea8f`이며 앱 코드 변경 없이 실검증했다. 격리 회귀 17개도 다시 실행해 모두 통과했다. 기존 200개 검사·lint·타입·빌드는 앞선 결과이며 이번에 다시 실행한 것으로 표기하지 않는다.

### 세션과 원본

- A: 브랜치 주소 `ai-fitness-app-git-fix-app-wide-reliability-jace3695s-projects.vercel.app`.
- B: 고정 주소 `ai-fitness-lbryf6bhf-jace3695s-projects.vercel.app`.
- 두 주소 모두 Vercel 배포 `dpl_D28dVGQ2cBEsezp9P1ckcX4ugX8y`, 커밋 `508b4ca`, READY로 확인했다. 같은 검증 계정의 로그인 UI를 확인했으며 별도 origin 저장소를 사용했다.
- 기존 `420w3g808` 탭은 `f90adf4`다. 동기화 중 이동이 자동 승인 검토에서 차단되어 고정 수정본은 새 탭에 열었다. 이후 이전 탭의 ‘서버 반영 완료’와 비어 있는 입력을 확인하고 닫은 뒤 테스트 쓰기를 시작했다. 사용자는 다른 기기에 열린 AI 연이 주소가 없다고 답했다. 연결되지 않은 기기를 에이전트가 직접 조사한 것은 아니다.
- 쓰기 직전에 원본 17개 키를 비공개 파일로 백업했다. 전체 PostgreSQL MD5는 `38219aa258571ec18705211a8a191e57`로 이전 정리 상태와 같았다. 공개 문서에 개인 백업 내용이나 인증 토큰을 포함하지 않는다.

### 기록 저장·수정·삭제

| 동작 | 실제 UI·서버 관찰 |
| --- | --- |
| A 신규 저장 | 빈 9/1에 `QA189-508-A` 1세트 7회 저장. A 자동 반영 완료, B 화면 수신과 서버 날짜 추가를 확인했다. |
| B 신규 저장 | 빈 9/2에 `QA189-508-B` 1세트 8회 저장. 서버에 두 날짜가 함께 유지됐다. 이전 검증의 B 날짜 소실은 이번 순서에서 재현되지 않았다. |
| 교차 수정 | A가 B의 9/2 메모를 `QA189-508-A-edited-B`, B가 A의 9/1 메모를 `QA189-508-B-edited-A`로 저장했다. 두 UI·서버 메모가 일치했고 운동 상세는 각 1개, 세트는 각 1개 7/8회로 유지됐다. |
| 교차 삭제 | A에서 9/2, B에서 9/1을 삭제했다. 자동 반영 후 두 합성 날짜가 없어졌고 원본 전체 해시가 백업과 같았다. |

각 단계에서 기존 운동 4일과 나머지 원본 키 값을 별도 서버 조회로 대조했다. 이 실행에서는 요청 지연을 제어하지 않았으므로 조건부 쓰기 충돌을 반드시 발생시킨 경합 검증으로 주장하지 않는다. ‘지금 동기화’ 버튼은 이 실행 내내 누르지 않았다.

### 임시 운동의 자동 종료 정리

A에서 새 운동의 03:00 타이머를 시작했다. 서버에 임시 진행 키가 생성되고 `timerSeconds=166`인 것을 확인한 뒤 ‘나가기 → 저장 없이 종료’를 실행했다. UI는 ‘기기 기록 · 서버 반영 대기’에서 ‘서버 반영 완료’로 바뀌었고, 별도 서버 조회에서 임시 키 제거와 원본 17개 키 해시 일치를 확인했다. 수동 동기화나 DB 쓰기 SQL은 사용하지 않았다.

자동 반영 후 A에서 재진입했을 때 03:00·‘시작’이 표시되고 이전 진행 복구 안내가 없었다. 다시 저장 없이 종료한 뒤 B에서도 재진입했으며 동일하게 03:00·‘시작’이었다. B의 재진입으로 생긴 임시 상태도 저장 없이 종료해 자동 정리했다. **같은 데스크톱 세션과 독립 origin 세션의 자동 삭제·재진입은 통과**다. 독립 origin을 실제 아이폰 하드웨어 검증으로 대체하지 않는다. 실제 아이폰에는 같은 고정 수정본으로 재진입하고 수동 동기화 없이 종료하는 확인을 요청했다.

이후 사용자는 실제 아이폰의 새 고정 수정본에서도 **3:00으로 표시됐으며, 저장 없이 종료 후 홈에 머문 상태**라고 확인했다. 다른 기기에서 이전 진행이 복구되지 않은 사용자 관찰로 기록한다. 에이전트가 직접 아이폰 화면을 본 것은 아니다. 종료 후 최종 서버 재조회에서도 임시 키 없이 17개 키 전체의 MD5가 시작 전 `38219aa258571ec18705211a8a191e57`과 같았다. 내부 운동 방향 백업도 포함하며, 원본 복원 SQL이나 수동 동기화 없이 합성 기록·임시 진행 정리를 완료했다.

### 증거 범위와 다음 단계

현재 브라우저는 네트워크 요청/응답 본문 캡처·요청 지연을 제공하지 않는다. `508b4ca` 소스에서 성공한 쓰기 뒤 `verifyRemoteState`가 확인 GET으로 전체 상태를 대조하는 경로는 확인했지만, 이번의 개별 PATCH 응답과 확인 GET 본문을 직접 캡처한 것은 아니다. SQL 최종 상태 조회를 그 본문 증거로 바꿔 부르지 않는다. 따라서 **기록 UI·서버 최종 값 검증은 통과했으나 요청별 본문 대조를 포함한 1번 전체 요건은 미완료**다.

두 실패의 전체 재검증이 끝난 뒤 진행하라는 조건에 따라 화면 잠금·전체 모바일·미리보기 업데이트·최초 조회 지연을 이번 단계의 통과로 추가하지 않는다. PR은 Draft 유지, 운영 병합·배포 없음이다.

## 요청 본문 수집·제어 환경 보완 — 2026-09-09

`e56f91e`의 실제 로그인 두 세션 저장·교차 수정·삭제, 자동 종료 정리와 사용자 아이폰 재진입 통과, 원본 17개 키 보존 결과를 유지한다. 동일 UI 순서를 개인 계정에서 다시 실행하지 않았다. 이번 브라우저 연결 재확인도 `CDP operation refresh tabs timed out after 20000ms`로 실패했다.

### 추가한 검증 수단

- [실행·정리 안내](../scripts/sync-qa/README.md)에 따라 전체 Next 앱을 loopback 개발 서버로 실행하고 `?qa-sync=1`로 켠다. Supabase SDK의 공식 custom fetch 경로에 선택적으로 수집기를 연결한다. 실제 앱 로그인·SDK·서버·조건부 쓰기를 그대로 사용하며, 인증 우회나 별도 합성 SDK를 실제 인증 검증으로 간주하지 않는다.
- 개발 모드·loopback hostname·탭의 명시적 활성화를 모두 요구한다. `next build`/운영/preview에서는 패널·수집이 켜지지 않는다. 전역 브라우저 fetch를 바꾸지 않고 이 SDK 인스턴스의 지정 테이블 요청만 관찰한다.
- `user_app_state`의 GET/PATCH/POST JSON 본문, 조건부 갱신 시각, 전송·수신·SDK 전달 시각을 기록한다. 인증 요청·다른 테이블·다른 origin·헤더는 수집하지 않으며 사용자 식별자와 인증 관련 JSON 필드는 마스킹한다. 운동·설정 본문은 여전히 개인 데이터이므로 메모리에서만 보유하고, 사용자가 내보내는 JSON은 비공개 보관한다. 공개 Git에는 실제 계정 본문을 넣지 않는다.
- 전송 전 보류, 실제 서버 응답 뒤 SDK 전달 보류, 응답 전달 실패 모사, GET 오류 모사를 제공한다. 응답을 수집했지만 SDK에는 전달하지 않은 경우와 합성 503을 실제 서버 응답에서 구분한다. SDK의 자동 GET 재시도를 끄지 않고 기록한다. GET 오류는 해제할 때까지 재시도에도 적용한다.
- [감사 도구](../scripts/sync-qa/audit.mjs)는 본문 없이 요청 ID와 조건부 거절·응답 미전달·후속 GET 내용 일치/불일치/미확보를 출력한다. HTTP 200이나 빈 결과를 곧바로 보존 통과로 판정하지 않는다. 수집 한도 초과·대기 요청을 별도로 표시한다. 전체 앱의 통과 판정이나 원본 보존 대조를 자동으로 대신하지 않는다.

### 이번 실행의 검증 범위

새 검사 8개는 실제 설치된 Supabase SDK가 loopback HTTP 서버에 보내는 실제 GET/PATCH를 사용한다. 요청 JSON과 서버에서 받은 본문 일치, 성공한 쓰기 뒤 확인 GET, 오래된 `updated_at` 조건의 빈 결과, 두 클라이언트의 보류된 GET 중 경합, 전송 전 보류, 서버 반영 후 SDK 전달 실패, GET 자동 재시도와 복구를 확인했다. 인증 정보·비대상 경로 제외, Request 본문 소비 방지, AbortSignal, 수집 한도 및 감사 오판 방지도 확인했다. **8개 통과**이며 브라우저·실제 계정·실제 Supabase DB/RLS 통과는 아니다.

`npm test`는 새 8개를 포함해 **208개 통과·0개 실패**다. 기존 격리 회귀도 **17개 통과·0개 실패**, 전체 lint·TypeScript와 기본 Turbopack 빌드의 46개 페이지 생성도 통과했다. 최초 빌드의 기존 Turbopack 캐시 손상(`.sst` 블록 읽기 오류)은 생성 캐시를 분리한 뒤 동일 명령으로 재실행해 해결했다.

개발 실행기로 앱을 시작해 루트 HTML HTTP 200과 QA 모듈 포함을 확인하고 서버를 종료했다. 운영 빌드로 로컬 `next start`를 실행한 경우 `?qa-sync=1`을 붙여도 HTTP 200 HTML에 QA 모듈·패널 문구가 없음을 확인하고 서버를 종료했다. 이는 HTTP 부팅·출력 검사이며, 연결이 차단된 브라우저에서 패널을 직접 클릭하거나 실제 로그인했다고 보고하지 않는다.

개인 기록·설정·백업·인증 토큰에는 접근하지 않았다. 새 검사에서 만든 합성 서버와 메모리 상태는 종료 시 폐기했으며 실제 원본 해시를 이번에 재측정한 것은 아니다. 원래 합성 HTTP 72건 증거와 기존 실제 통과는 과거 범위 그대로 유지한다.

### 남은 작업의 분리

데스크톱의 연결 가능한 브라우저에서 새 로컬 패널의 조작 확인·실제 인증 후 본문 대조·정밀 경합·최초 조회 지연을 진행해야 한다. 로컬 개발 모드의 React effect 재실행과 폴링이 제어 요청을 먼저 소비할 수 있으므로 실제 적용된 요청 순서를 확인하고, 관찰하지 못한 순서를 통과 처리하지 않는다.

아이폰 화면 잠금·전체 모바일 화면과 실제 preview 서비스워커 업데이트는 **본문 수집 완료를 기다리지 않고 독립적으로 진행 가능**하다. 앞 절의 일괄 보류는 당시 진행 이력으로 보존하며, 이후 작업을 묶어서 막는 조건으로 사용하지 않는다. 개발 모드는 서비스워커를 등록하지 않으므로 이 패널로 preview 업데이트 검증을 대체하지 않는다. 현재 대화는 브라우저 연결이 막혀 이 실검증들을 완료로 추가하지 않는다. PR은 Draft로 유지하고 운영 병합·배포는 하지 않는다.

## `2a4b947` 로컬 인증 패널 실검증과 원본 차이로 중단 — 2026-09-09 22:30~22:45 KST

기존 통과·실패 이력을 보존한다. 브라우저 연결과 PR head `2a4b947a67dfe5436e5c692ccb2e07e254fc6c1e`, Draft 상태를 확인하고 이 문서와 `scripts/sync-qa/README.md`를 읽었다. 기존 작업 폴더는 `e56f91e` 및 깨끗한 상태 그대로 유지하고 별도 detached worktree에 최신 코드를 준비했다. Node 24.19.0에서 문서의 `node --experimental-strip-types scripts/sync-qa/start.mjs 3000`으로 전체 개발 앱을 실행했다. 기존 의존성을 복사했으며 앱 코드는 변경하지 않았다.

### 실제로 확인한 범위

- A `http://127.0.0.1:3000/?qa-sync=1`, B `http://localhost:3000/?qa-sync=1`을 내장 브라우저에서 열었다. 양쪽 패널 펼치기와 제어 UI를 확인했다. A는 정상 로그인 후 실제 SDK GET 본문을 수집했다. B는 중단 시점까지 로그인 화면이므로 두 인증 세션 검증 완료가 아니다.
- 기존 브랜치 preview는 빈 입력과 동기화 완료 후 새로고침했다. 해당 주소의 Vercel 배포는 `dpl_SRWvhkdiSH2EUApMDDpxQw9UkKCC`, 고정 주소는 `ai-fitness-l9vdokf02-jace3695s-projects.vercel.app`, 커밋 `2a4b947`, READY다. 다른 기기의 활성 주소·실행 버전은 이번 사용자 답변을 확보하지 못했으므로 확인 완료로 표기하지 않는다.
- 실제 쓰기 전에 서버 전체 17개 키를 저장소 외부 비공개 파일로 백업했다. 시작 MD5는 `38219aa258571ec18705211a8a191e57`로 이전 정리 상태와 같았다. 추가 개인 키는 당시 없었다.
- B 로그인 대기 중 A에서 쓰기 없는 GET 오류·복구를 독립적으로 확인했다. `#7`~`#10`은 13:39:05.567~13:39:12.628 UTC에 SDK가 시도한 4건이며 모두 `synthetic-get-error`, `synthetic:true`, 본문 `{"message":"QA simulated GET failure"}`, 503이었다. 이 요청들은 서버로 전송되지 않았고 실제 Supabase 장애가 아니다. 앱의 ‘기록 동기화 실패’, ‘동기화에 실패했습니다.’, ‘다시 시도’를 확인했다.
- 제어 해제·재시도 후 `#11` GET이 13:39:35.565 UTC에 전송되고 13:39:35.631에 실제 200 본문을 받아 SDK에 전달됐다. 앱은 ‘서버 반영 완료’로 복구됐다. 모사 오류 안내·실제 재조회 복구의 제한된 흐름만 통과다. 작성 중 추가 입력, PATCH 응답 유실, 확인 GET 오류까지 통과한 것은 아니다.

### 중단 사유: 원본 내부 백업의 저장 시각 차이

아이폰 잠금 검증 절차를 안내한 뒤 읽기 전용 서버 대조에서 원본 `ai-fitness-workout-direction-backup` 값 변경과 임시 운동 키 추가를 발견했다. 추가 테스트 쓰기를 즉시 중단하고 변경 전후 전체 상태를 비공개로 보관했다. 에이전트는 합성 날짜 저장·수정·삭제 또는 운동 시작을 실행하지 않았다.

- 바뀐 원본은 내부 백업의 **`savedAt` 필드 하나**다. 백업 내부의 계획·설정 내용과 나머지 원본 16개 키는 모두 같았다. 운동 기록 소실은 관찰하지 않았다. 그러나 사용자 요구인 내부 백업 포함 전체 원본 동일 조건은 **실패**다.
- A 패널의 실제 GET `#1`~`#6`은 원본 17개 키와 기존 백업 시각을 포함했다. 오류 해제 후 `#11` 본문은 18개 키였고, 원본 백업 시각 변경과 임시 진행 추가를 포함했다. DOM에 렌더된 두 실제 응답 JSON의 키별 비교에서도 원본 16개 키가 같았다. SQL 200 상태나 최종 조회만으로 본문 관찰을 대신한 것이 아니다.
- 최종 서버 재조회에서는 임시 진행 키가 없어지고 다시 17개 키가 됐다. 전체 MD5는 `88d0e393a97fc2b6469cd6c81491c780`이며 내부 백업 시각 차이는 남았다. 이후 사용자가 최신 고정 주소에서 저장 없이 종료 후 홈으로 이동했고 현재 로그인 화면에서 대기한다고 확인했다. 사용자 기기 종료 보고와 서버의 임시 키 부재가 일치한다. 에이전트가 아이폰 UI를 직접 본 것은 아니다.
- 운동 페이지에는 초기 클라우드 조회 완료 전 기기 백업 키가 없으면 새 `savedAt`으로 내부 백업을 만드는 경로가 있다(`app/fitness/page.tsx`). 이번 차이와 일치할 수 있는 경로이나, 해당 변경을 보낸 기기의 PATCH 본문·주소를 수집하지 못해 확정 원인으로 단정하지 않는다. 내부 백업을 임의로 복원하거나 DB 쓰기 SQL로 정리하지 않았다.

### 증거 보관·정리와 미완료

서버 전후 전체 백업과 패널 요청 ID·시각·본문 비교 관찰 요약은 비공개 파일에 보관했다. 패널의 JSON 다운로드 버튼은 실행했지만 이 연결에서 저장 파일을 찾지 못했고, 브라우저 content export도 미지원이었다. 같은 시도를 반복하지 않았다. **전체 SDK trace 파일의 영구 보관·감사 CLI 실행은 미완료**이며 관찰 요약을 완전한 본문 export로 부르지 않는다. 공개 문서에는 개인 기록·인증 정보·응답 전체를 넣지 않았다.

A/B 모두 제어 없음·처리 중 0건에서 ‘수집 종료·내용 폐기’를 실행했다. A 로그아웃과 로그인 화면 복귀를 확인했고 B는 미로그인 상태였다. 로컬 서버도 종료했다. 개인 앱 저장소 전체 삭제는 하지 않았다. 새 합성 날짜 기록은 생성하지 않았으며 임시 진행 키의 최종 부재를 확인했다. **내부 백업 차이가 남아 원본 정리 전체 통과로 처리하지 않는다.**

최초 GET 지연 중 달력 저장, 두 실제 인증 세션의 조건부 충돌·병합, PATCH 요청/응답과 저장 확인 GET 대조, PATCH 보류 중 추가 수정·이탈, 응답 전달 실패 복구는 쓰기 중단으로 미완료다. 아이폰 Safari 사용자 관찰은 잠금 전 02:45 → 복귀 후 02:02였고 이후에도 계속 감소했다. 사용자는 처음 말한 30~40초가 대략적인 추정이며 실제로는 45초였을 수도 있다고 정정했다. 감소량 43초는 이 추정과 부합하며 타이머 오류로 판단하지 않는다. 잠금 후 타이머 진행 복귀는 사용자 관찰로 확인했고, 정밀한 초 단위 정확도 측정은 미실시다. 기종·iOS 버전과 전체 모바일 세부 화면은 미확인이다. 사용 주소는 최신 고정 `l9vdokf02`이며 사용자는 저장 없이 종료 후 홈 이동, 이후 로그인 화면 대기를 확인했다. 기존 320px/390px 주요 화면 통과는 반복하거나 확대하지 않았다. `508b4ca`와 `2a4b947`의 `public/sw.js`가 같고 새 waiting worker를 관찰하지 못했으므로 실제 preview 업데이트 보류도 미완료이며, 같은 preview origin에서 실제 서로 다른 worker 빌드가 필요하다.

원본 차이의 원인과 처리 방침을 확인한 뒤 새 백업 기준으로 쓰기 검증을 재개해야 한다. 기존 208개·격리 17개·lint·타입·빌드 결과는 유지하며 이번 실행에서 다시 돌렸다고 주장하지 않는다. 이번 변경은 검증 문서뿐이며 공백 검사를 수행한다. PR은 Draft 유지, 운영 병합·배포 없음이다.

## PATCH 본문 재개: 로그인 직후 전체 삭제를 실제 본문으로 확인 — 2026-09-09 22:50 KST 이후

사용자의 PATCH 본문·정밀 경합 완료 요청에 따라 PR 최신 `937e6e6`(검증 앱 기능 `2a4b947`)을 재확인하고 기존 별도 worktree에서 개발 서버를 재실행했다. 이전 내부 백업 시각 차이를 임의로 되돌리지 않고, 현재 17개 키 전체를 새 비공개 기준으로 백업했다. 시작 MD5는 `88d0e393a97fc2b6469cd6c81491c780`이다. 사용자는 A `127.0.0.1:3000`과 B `localhost:3000` 모두 같은 계정으로 로그인했다고 확인했으며 실제 두 홈 화면에서도 계정·서버 반영 완료를 확인했다.

### 실제 실패와 요청·응답 대조

에이전트가 합성 날짜를 저장하거나 운동 화면으로 이동하기 **전에**, A 로그인 초기 동기화가 빈 전체 상태를 서버에 저장했다. 이번에는 실제 SDK 본문 파일이 있어 아래 순서를 직접 대조했다. 모사 오류·지연 제어는 적용하지 않았다.

| A 요청 | UTC 시각·실제 내용 |
| --- | --- |
| #1 GET | 13:51:33.126 전송 → .838 수신. 서버 원본 17개 키가 들어 있었다. 전체 JSON이 검증 직전 비공개 백업과 정확히 같았다. |
| #3 PATCH | 13:51:33.842 전송 → .890 수신. 요청 본문은 `state: {}`와 새 `updated_at`이었다. 이전 서버 `updated_at`을 조건으로 보냈고 실제 200 응답 본문에 새 `updated_at` 한 행이 있었다. 빈 결과에 의한 CAS 거절이 아니다. |
| #4 확인 GET | 13:51:33.892 전송 → .917 수신. 실제 응답은 0개 키의 빈 상태였고 PATCH의 새 `updated_at`과 같았다. 이후 GET도 빈 상태였다. |

`audit.mjs`는 A 수집 8건에 `completeCapture:true`, PATCH #3 → GET #4 `confirmation-matches`를 보고했다. **이는 잘못 보낸 빈 상태와 서버가 일치한다는 뜻이며 데이터 보존 통과가 아니다.** UI는 서버 반영 완료였지만 읽기 전용 SQL에서도 0개 키, MD5 `99914b932bd37a50b983c5e7c90ae93b`였다. B의 수집 6건은 GET만 있었고 PATCH는 없었다. 이번 관찰 구간의 빈 상태 전송자는 A로 식별했다. 이전 사건의 원인까지 같은 것으로 단정하지 않는다.

소스에서 `AuthGate`는 `SIGNED_OUT` 때 `clearLocalCloudState()`를 실행한다. 이 함수는 `ai-fitness-` 키를 지우지만 다른 접두사인 `fitness-cloud-sync-base:`는 남긴다. 다음 초기 동기화는 남은 기준과 빈 로컬을 3방향 병합한다. 앞선 실행에서 A를 로그아웃한 이력과 이번 빈 PATCH에 부합하는 경로다. 이번 브라우저의 숨은 저장소를 직접 읽어 확인한 것은 아니므로 저장소 상태까지 관찰했다고 주장하지 않는다. 이 로그인/로그아웃 기준 정리 경로의 수정과 격리 회귀가 필요하다.

### 즉시 중단·원본 복구

전체 원본 누락 발견 즉시 추가 테스트 쓰기를 중단했다. A/B 실제 trace를 비공개 파일로 보관하고 제어 없음·처리 중 0건에서 수집을 종료한 뒤 두 탭을 닫아 재전송을 막았다. 기존 preview는 로그인 화면인 것을 확인했다. 개인 계정에서 합성 운동·설정 변경을 추가 실행하지 않았다.

검증 직전 백업을 이용해 **대상 계정의 서버 state가 아직 정확히 `{}`일 때만** 적용되는 조건부 복구 SQL을 한 번 실행했다. 이 SQL은 테스트 쓰기가 아닌 누락된 개인 원본의 복구이며, 현재 다른 값을 일괄 덮어쓰지 않도록 조건을 걸었다. 복구 직후와 후속 재조회에서 내부 백업을 포함한 **17개 키 전체가 새 시작 백업과 정확히 같고 MD5 `88d0e393a97fc2b6469cd6c81491c780`**임을 확인했다. 합성 날짜는 생성하지 않았고 임시 진행도 없다. 이전 내부 백업 시각으로 되돌리지 않았다. 로컬 서버는 종료했다.

### 비공개 본문 보관 보완과 판정

다운로드 파일 경로를 찾지 못했던 제약을 해결하려고 개발 전용 패널에 ‘비공개 증거 로컬 파일 보관’을 추가했다. 실제 두 UI 버튼으로 서버 작업 폴더의 Git 제외 `scripts/sync-qa/private/`에 A/B 원문 JSON을 저장했고 파일을 읽어 감사 도구를 실행했다. 서버가 파일명을 생성하고 기존 파일을 덮지 않으며 개발·loopback·동일 origin·전용 헤더·JSON 형식을 검사한다. 운영 모드는 404다. 최초 A 저장은 Next의 내부 URL과 원래 Host 차이로 403이었고, 실제 loopback Origin/Host 일치 검사로 보완한 뒤 양쪽 파일 저장을 확인했다. 동기화 알고리즘·SDK 요청 본문은 수정하지 않았다.

보관 경계 검사 4개(운영 거부·외부 origin 거부·전용 헤더 누락 거부·잘못된 형식 거부), 변경 파일 lint·TypeScript, 기본 Turbopack 프로덕션 빌드 47개 페이지 생성이 통과했다. 기존 208개·격리 17개 검사를 이번에 재실행했다고 주장하지 않는다. 개인 원문 파일은 공개 커밋에 포함하지 않는다.

**PATCH 요청·실제 응답·인과적으로 뒤따른 저장 확인 GET의 본문 확보와 대조는 완료했으며, 그 결과 전체 원본 보존은 실패다.** 엄밀한 두 세션 CAS 경합·최초 조회 지연 저장·보류 중 추가 수정/이탈·응답 전달 실패 복구는 새 실패 때문에 중단 상태다. 실패를 해결하기 전 같은 개인 계정에서 추가 쓰기 검증을 반복하지 않는다. 기존 통과 결과와 아이폰 잠금 복귀 관찰은 그대로 유지한다. PR Draft 유지, 운영 병합·배포 없음이다.

## 로그인·로그아웃 동기화 수명 수정 — `b22818a` 후속 격리 검증

### 원인 재현과 수정 범위

`b22818a`의 실제 요청 증거를 기준으로 별도 체크아웃에서 조사했다. 실제 계정·서버·비공개 본문 파일에는 접근하지 않았다. 원본 대신 메모리의 합성 키 17개로 기존 `clearLocalCloudState()` → 초기 병합을 실행하면 **로컬 0개·기준값 17개 → 전송할 상태 0개**가 재현됐다. 같은 잔여 기준값을 실제 `CloudSyncPanel` 인증·동기화 효과에 넣어도 빈 PATCH가 발생했다. 앞선 실제 A의 관찰과 부합하는 코드 결함을 확인한 것이며, 과거 모든 누락 사건의 원인이 같다는 뜻은 아니다.

- 로그아웃 정리는 공통 루트 동기화 소유자가 담당한다. 로컬 기록과 모든 동기화 기준값·계정 식별자를 한 저장 트랜잭션으로 제거한다. 중단된 저장 저널도 먼저 복구하므로 다음 조회가 로그아웃 전 기록을 되살리지 않는다. 저장 실패 시 기록과 기준값은 함께 복구하고 이전 요청은 무효 상태로 둔다.
- 로컬 계정 식별자와 세대값을 동기화 대상 접두사 밖에 둔다. 계정 전환은 이전 계정 캐시를 새 계정에 병합하지 않는다. 이전 버전의 **식별자 없는 빈 캐시+남은 기준값**은 삭제 의도로 해석하지 않고 서버에서 다시 채운다. 새 버전에서 계정이 확인된 캐시의 정상 삭제는 기존 병합 규칙을 유지한다.
- 로그아웃·계정 변경은 React 효과 정리 전에도 기존 요청을 취소한다. GET/PATCH/insert와 확인 GET에 SDK 취소 신호를 전달하고, 응답 후·CAS 재시도 전에도 현재 계정·세대를 확인한다. 이전 세션의 늦은 응답이 로컬 기록·기준값·완료 표시를 다시 만들지 않는다.
- 동기화 진행 여부는 각 효과가 소유한다. 이전 요청의 대기·종료가 새 계정 동기화를 막지 않는다. 같은 계정 로그아웃·재로그인이 한 번의 React 렌더에 합쳐지는 경우도 별도 인증 변경 횟수로 재시작한다. 동일 계정의 토큰 갱신·세션 재확인은 저장 기준을 초기화하지 않는다.
- 늦은 최초 `getUser()`와 PIN 조회 결과가 이후 인증 변경을 덮지 않도록 확인한다. 계정 변경 시 페이지 자식도 새로 마운트한다. 인증 콜백은 동기식으로 두고 네트워크 동기화는 별도 효과에서 실행한다([Supabase 인증 이벤트 문서](https://supabase.com/docs/reference/javascript/auth-onauthstatechange)).

요청 취소는 **이미 서버에 반영된 정상 쓰기의 롤백이 아니다**. 반영 후 로그아웃한 경우에도 새 세션에서 서버를 다시 조회한다. 기존 저장 후 확인 GET은 유지하지만, `confirmation-matches`만으로 사용자 의도·원본 보존을 판정하지 않는다. 로그인 직후 사용자가 편집하지 않았다면 원본 전체와 같아야 한다.

### 수정 전후 비교

동일한 `scripts/qa-pr189-sync.mjs`를 수정 전 `b22818a`와 수정 후 코드에 실행했다. 기존 17개는 양쪽 모두 통과했고 새 인증 회귀 8개는 **수정 전 실패 → 수정 후 통과**다.

| 추가 격리 사례 | 수정 전 | 수정 후 |
| --- | --- | --- |
| 로그아웃·재로그인 시 합성 17개 키 보존·불필요한 빈 PATCH 없음 | 실패 | 통과 |
| 이전 버전의 빈 캐시와 남은 기준값으로 로그인 | 빈 상태 전송 | 원본 조회·복구, PATCH 없음 |
| 로그아웃 직후 효과 정리 전 늦은 GET | 기록/기준값 잔존·재적용 | 재적용·후속 PATCH 없음 |
| 양쪽 변경의 CAS 거절 대기 중 로그아웃 | 재조회·재PATCH 계속 | 재시도 중단 |
| 이전 GET 대기 중 다른 계정 로그인 | 새 동기화 지연·이전 캐시 잔존 | 새 계정 즉시 조회·분리 |
| 늦은 최초 `getUser()` 뒤 로그아웃 유지 | 이전 사용자 재활성화 | 로그아웃 유지 |
| 같은 렌더에 합쳐진 동일 계정 로그아웃·재로그인 | 새 동기화 중단 | 재시작·원본 복구 |
| 동일 계정 토큰 갱신·재확인 중 저장 | 확인된 기준 초기화 | 기준·대기 입력 보존 |

이 어댑터는 실제 원본 함수와 루트 인증·동기화 효과를 실행하고, 인증 이벤트·`getUser()` 결과·서버·시계와 React의 효과 반영 시점을 모사한다. 의존성 변경 여부와 각 효과가 캡처한 사용자를 구분한다. **실제 React 렌더러·브라우저·인증 서버·HTTP 경합 검증은 아니다.**

- 격리 회귀: **25 passed / 0 failed**, 기존 17개 포함.
- `npm test`: **216 passed / 0 failed / 0 skipped**. 기존 208개에 저장 수명 6개와 실제 설치 SDK의 취소 신호 전달 2개를 추가했다. SDK 추가 검사는 메모리 fetch 경계이며 실계정 네트워크 검증이 아니다.
- 저장 수명 검사는 17개 키·무관한 설정 보존, 이전 버전 잔여 기준 처리, 정상적인 마지막 키 삭제, 계정 전환, 중단 저널, 정리 중 저장 실패 복원을 확인한다.
- SDK 검사는 GET/PATCH/insert·확인 GET의 취소 신호 전달과, PATCH 반영 후 취소 시 확인 GET을 새로 시작하지 않는 것을 확인한다.
- `npm run lint`, `tsc --noEmit`, 기본 Turbopack 프로덕션 빌드·47개 페이지 생성, `git diff --check` 통과.

### 실검증 재개 조건과 보존 상태

이 대화의 브라우저 탭 조회는 다시 **`CDP operation refresh tabs timed out after 20000ms`**로 실패했다. 따라서 위 수정의 실제 로그인·로그아웃 UI, 두 인증 세션과 실제 요청 본문 대조는 아직 미완료다. 실제 인증 후 핵심 흐름 검증이 필요한 `AGENTS.md`에 따라 자동 검사로 대체 완료 처리하지 않는다.

데스크톱에서는 수정 커밋의 별도 작업 폴더와 `scripts/sync-qa/README.md` 절차를 사용한다. 이전 버전의 열린 탭·미리보기에서 자동 쓰기가 섞이지 않도록 실행 버전을 먼저 확인한다. 검증 직전 원본 전체를 비공개로 백업하고, **합성 편집에 앞서 로그인 → 서버 전체 비교 → 로그아웃 → 같은 계정 재로그인 → 서버 전체 비교**를 먼저 수행한다. 로그인 전에 PATCH 전송 전 보류를 걸어 예상하지 않은 쓰기를 원본과 비교하고, 빈 상태·원본 차이가 있으면 전송하지 않고 중단한다. 제어를 건 관찰과 제어 해제 후 정상 완료는 구분해 기록한다. 이전 실패 상태처럼 빈 캐시와 잔여 기준값이 있던 세션의 업그레이드도 확인한다. 사이트 데이터 전체 삭제로 이 조건을 없애지 않는다. 각 구간의 PATCH·확인 GET 본문과 사용자 조작을 함께 대조한다.

이 보존 단계가 통과한 뒤 실제 두 세션 CAS 경합, 최초 GET 지연 중 저장, PATCH 보류 중 추가 수정·이탈, 응답 전달 실패 복구를 순서대로 재개한다. 개인 원본이 변하면 추가 테스트를 즉시 중단하고 해당 실행의 최신 백업 기준으로 처리한다. 기존 통과 항목은 반복하지 않는다. 아이폰 전체 화면·타이머 정밀 측정과 실제 preview 업데이트는 별도 미완료로 유지하며, 내부 백업 `savedAt` 변경의 실제 작성자는 아직 확정하지 않았다.

이번 작업은 합성 메모리/격리 테스트만 사용했으며 실제 개인 기록·설정·내부 백업을 읽거나 쓰지 않았다. 실계정에 만든 합성 데이터도 없다. 앞선 데스크톱이 복구한 17개 키·MD5 `88d0e393a97fc2b6469cd6c81491c780`는 **이전 확인 결과**로 유지하며 이번에 재조회했다고 주장하지 않는다. 비공개 본문·백업은 커밋에 포함하지 않았다. PR Draft 유지, 운영 병합·배포 없음.


## 데스크톱 없이 실행하는 CI 검증 구축 — 2026-09-10 KST

사용자의 장소 독립적인 검증 요청에 따라 GitHub-hosted runner에서 실제 production Next 앱·Playwright 브라우저·일회용 Supabase Auth/PostgREST/Postgres를 함께 실행하는 workflow를 추가했다. 설정과 사용법은 `tests/e2e/README.md`에 있다. 로그인 보존 프로젝트가 먼저 통과해야 후속 쓰기 검증 프로젝트가 실행된다. 현재 추가한 브라우저 사례는 16개이며, **구현·목록 확인을 실행 통과로 처리하지 않는다. 클라우드 실행 결과는 후속 기록으로 추가한다.**

브라우저 클라이언트가 Supabase 환경변수를 무시하고 운영 주소·키만 사용하던 설정을 서버와 같은 환경변수 쌍으로 통일했다. 설정하지 않은 기존 기본값은 유지하며 URL 또는 키만 지정하면 실패한다. CI 빌드는 임시 loopback 주소와 공개 키를 필수로 요구하고 외부 브라우저 요청도 차단한다. 운영 데이터나 비공개 백업은 읽지 않는다.

최소 fixture 스키마로 동기화/RLS 및 PIN 상태 조회 계약만 구성한다. 운영 스키마 전체·호스팅 서버 동일성 검증은 아니다. 각 테스트는 합성 17개 원본 키와 임시 인증 사용자를 사용하고, 테스트가 실패해도 브라우저 종료·사용자 삭제·DB 행 부재 확인을 수행한다. 마지막 단계는 해당 stack의 컨테이너·볼륨·키를 제거한다. 공개 artifact는 판정·시각·상태 코드·본문 해시·키 수·정리 결과만 포함한다.

이전 실계정 데이터 삭제 실패·복원·미완료 기록과 기존 수동 통과는 보존했다. 실제 iPhone 잠금·백그라운드 타이머·전체 화면, 운영/preview의 실제 배포 업데이트는 이 CI 결과로 대체하지 않는다. PR Draft 유지, 운영 병합·배포 없음.


### GitHub 실브라우저 실행과 발견된 기본값 덮어쓰기

- [첫 실행 `7736ad2`](https://github.com/jace3695/AI-fitness-app/actions/runs/34416317906): 설치·기존 검사·임시 DB·production 빌드 통과. 로그인 버튼의 실제 이름은 ‘AI 연이 시작’인데 테스트가 ‘로그인’을 찾은 문제로 3개가 실패했고 후속 12개가 차단됐다. 직접 인증 API/RLS 검사는 통과했다. 앱 결함으로 판정하지 않고 선택자를 수정했다. 임시 DB·볼륨 정리 단계는 통과했다.
- [두 번째 실행 `45fb3ad`](https://github.com/jace3695/AI-fitness-app/actions/runs/34417220711): **10개 통과, 6개 실패**. 실제 로그인·로그아웃·재로그인 후 합성 17개 키 완전 일치, 잔여 기준값 업그레이드, 늦은 GET 취소, 다른 계정 RLS, PATCH 보류 중 추가 저장/이탈, 반영 후 응답 유실·복구, 확인 GET 오류·복구, 삭제 후 이탈, WebKit 320/390 화면 검사가 통과했다. 오류 응답 503은 의도적으로 주입했으며 실제 서버 장애라고 주장하지 않는다.
- 최초 GET 지연 중 식단 저장에서는 **기존 식단 시작일 `2026-08-24`가 기본 `2026-06-22`로 바뀌는 실제 fixture 데이터 보존 실패**를 확인했다. `DietView` 초기화가 서버 응답 전에 기본값을 localStorage에 저장한 것이 병합에 포함된다. 기본값은 화면 표시에만 사용하고 사용자가 날짜를 바꿀 때만 저장하도록 수정한다. 합성 계정에서 발견했으며 개인 데이터에 같은 검사를 실행하지 않았다.
- Chromium 작은 화면 2개와 경합 2개는 이전 ‘서버 반영 완료’ 문구를 새 저장의 완료로 오인할 수 있던 테스트 대기 조건을 보완한다. 폼의 로컬 저장값 및 새 서버값 도착을 먼저 확인한다. 이 보완 전 결과를 저장 기능 통과로 바꾸지 않으며 재실행으로 판단한다.
- SW를 허용한 최초 로그인 대기도 실패했다. 첫 worker가 현재 화면을 제어하기 시작할 때도 재로드하던 코드를 확인해 첫 설치에서는 재로드하지 않도록 수정한다. 후속 worker 교체의 안전 갱신은 유지하며 최초 설치 중 자동 재로드가 없다는 실제 탐색 횟수 검사를 추가한다.
- 루트 사용량 알림의 `ai_usage_events` 누락은 최소 fixture 스키마의 빠진 읽기 의존성이어서 해당 테이블/소유자 읽기 정책을 보완한다. AI 서비스 호출·비용 검증은 아니다. 인증 토큰/키를 공개 로그에서 제거하고 각 세션의 요청·계정 정리 건수를 공개 가능한 요약으로 남긴다. 두 번째 실행도 테스트 정리와 임시 stack 제거 단계가 완료됐다.

위 수정의 최종 통과 여부는 다음 클라우드 실행 결과로 판단한다. 앞선 개인 원본 17개 키 복원 결과는 과거 확인으로 보존하며 이번에 재조회했다고 주장하지 않는다. Draft 유지, 운영 병합·배포 없음.


### `871fbe5` 재검증과 스키마 계약 확인

[세 번째 실행](https://github.com/jace3695/AI-fitness-app/actions/runs/34418034356)에서 **13개 통과, 3개 실패**였다. 식단 시작일의 실제 덮어쓰기는 수정 후 보존 검사에 통과했다. 두 독립 인증 세션에서 A→B/B→A 양쪽 순서로 조건부 PATCH가 각각 실제 0행을 수정한 뒤 오류·재시도·병합을 수행했고, 메모와 물 섭취 변경을 함께 보존했다. PATCH 본문과 후속 GET 본문 일치도 통과했다. 로그아웃 수명, 추가 저장/이탈, 응답 유실, 확인 GET 오류, 삭제 후 복구 방지, WebKit 작은 화면의 통과는 유지됐다.

남은 Chromium 화면 2개는 돌아가기 링크를 누른 직후 URL 이동 완료 전에 reload해 설정 화면을 다시 열 수 있는 테스트 경합이었다. 도착 URL을 확인한 뒤 새로고침하도록 수정한다. SW 사례는 로그인까지 성공했으나 Chromium의 같은 문서 `history.replaceState`도 세는 `framenavigated` 횟수 조건이 실패했다. 실제 문서 요청만 세도록 보완한다. 이 측정 실패 때문에 아직 SW 업데이트 전체 통과로 표시하지 않는다. 모사 503 응답에는 앱 origin에 맞는 CORS 헤더를 명시한다.

운영 DB의 개인 행·state 값을 읽지 않고 `user_app_state`의 시스템 카탈로그만 읽기 전용 조회했다. 세 컬럼의 타입/NULL/default, user_id 기본키, auth.users 삭제 연동, RLS 활성화, authenticated 소유자 SELECT/INSERT/UPDATE/DELETE 정책과 grants, 추가 트리거 부재가 fixture와 일치했다. 이는 해당 테이블 계약 대조이며 호스팅 Auth 설정·전체 스키마·실제 계정 E2E 통과와 구분한다.

세 번째 실행의 로그에 16개 사례 모두 계정 정리와 남은 동기화 행 0이 기록됐으며, 계정은 합계 17개였다. 외부 origin 요청은 0이었다. 임시 DB 컨테이너·볼륨·키 제거도 통과했다. 기존 개인 기록·설정·백업은 읽거나 변경하지 않았다.


### 데스크톱 없는 클라우드 검증 완료 — `4a4ff93`

[네 번째 GitHub 실행](https://github.com/jace3695/AI-fitness-app/actions/runs/34418886701)은 **16개 통과, 실패·건너뜀 0개**로 완료됐다. 고정 검증 커밋은 `4a4ff930dd86c751c5d40a0b79415585ca02a73c`이다. GitHub-hosted Ubuntu에서 실제 production Next 앱과 Chromium/WebKit, 일회용 Supabase Auth/PostgREST/Postgres를 함께 실행했다. 실제 브라우저 UI 사례 15개와 실제 Auth/RLS API 사례 1개이며, 로그인 화면만 열어 본 검증이 아니다.

| 검증 범위 | 사례 수 | 결과 |
| --- | ---: | --- |
| 로그인·로그아웃·재로그인 후 합성 17개 키 일치, 이전 빈 캐시/잔여 기준값, 로그아웃 뒤 늦은 GET, 다른 계정 RLS 차단 | 4 | 통과 |
| 최초 GET 전달 지연 중 식단 저장과 기존 시작일·원본 보존 | 1 | 통과 |
| 독립 인증 세션 A→B/B→A 양방향 조건부 충돌, 재조회·병합·양쪽 변경 보존 | 2 | 통과 |
| PATCH 응답 보류 중 추가 저장/이탈, 반영 후 응답 유실·복구, 확인 GET 오류/SDK 재시도·복구, 삭제 후 이탈/재진입 | 4 | 통과 |
| Chromium/WebKit 각각 320px·390px 식단 입력·스크롤·저장·설정·새로고침 | 4 | 통과 |
| 첫 worker 설치 시 로그인 재로드 없음, 작성 중 실제 worker 업데이트 보류, 저장 후 명시적 갱신·재조회 보존 | 1 | 통과 |

브라우저가 보낸 대상 동기화 요청 **109건**을 캡처했다. 실제 서버 응답 105건과 의도적으로 주입한 확인 GET 503 응답 4건을 구분한다. fixture 생성·검증용 SDK의 별도 조회와 다른 API는 이 건수에 포함하지 않는다. 경합 방향마다 실제 조건부 PATCH의 0행 결과를 1회씩 관찰했다. 최초 GET 지연·경합·추가 저장/이탈·확인 GET 오류·삭제·작은 화면 사례에서는 성공한 PATCH의 state 전체와 뒤따르는 실제 GET state를 메모리에서 대조했다. 응답 유실 사례는 서버 반영 후 전달 차단과 복구 GET을, worker 사례는 갱신 후 UI와 DB 보존을 별도로 확인했다. 원본 키·기존 날짜 값·내부 백업 시각은 요청/확인 일치와 별도로 비교했다.

이번 검증으로 발견한 식단 시작일 기본값 덮어쓰기와 첫 서비스워커 설치 재로드는 `871fbe5`에서 수정했고 최종 실행에서 재검증했다. 브라우저 Supabase 설정은 CI가 지정한 URL·공개 키 쌍을 사용하며 부분 지정은 거부한다. 운영 기본값은 기존대로 유지한다.

기존 검사도 **단위 220개, VM 회귀 25개, 린트·타입·production 빌드 47개 페이지**가 통과했다. 브라우저 재시도는 0회다. 각 사례의 로그에 합성 Auth 사용자 삭제와 동기화 행 0이 기록됐으며, 합계 **17개 임시 계정 제거, 남은 행 0, 외부 origin 요청 0**이다. 해당 임시 stack의 컨테이너·볼륨·키 제거 및 증거 업로드 단계가 성공했다. 실제 개인 기록·설정·내부 백업은 읽거나 변경하지 않았다. 이전 개인 원본 복원 해시는 과거 확인으로 보존한다.

[공개 합성 증거 artifact](https://github.com/jace3695/AI-fitness-app/actions/runs/34418886701/artifacts/10130257510)는 본문 해시·키 수·상태·시각·정리 결과만 담는다. GitHub가 반환한 ZIP SHA-256은 `9f546865c057fd2ae7b9aa0699dc6cf52fdd30bceb4baabffa620472d2a00adf`이며 보관 기한은 2026-09-16 23:58 UTC다. 로그와 artifact 메타데이터를 확인했으며 ZIP을 내려받아 별도 재감사했다고 주장하지 않는다. 개인 본문·백업·인증 비밀은 커밋이나 artifact에 넣지 않았다.

이제 PR의 **Checks → Isolated browser verification → Details → Re-run jobs**로 장소에 관계없이 재실행할 수 있다. 새 코드 커밋도 자동으로 검증한다. 개인 PC나 데스크톱 앱을 실행해 둘 필요가 없으며 [실행 안내](../tests/e2e/README.md)와 기존 수동 안내에 경로를 반영했다.

**남은 범위:** 호스팅 Auth 설정과 실제 호스팅 세션 E2E, 개인 원본의 별도 재확인, 실제 iPhone 전체 화면·정밀 화면 잠금/백그라운드 타이머, 실제 preview 배포 전환이다. 시스템 카탈로그의 동기화 테이블/RLS 일치와 CI의 실제 인증·HTTP 경합 통과를 이 범위의 완료로 확대하지 않는다. 서비스워커 검사는 같은 production 앱 origin의 실제 worker 교체이며 앱 배포 전환 검증이 아니다. 앞선 수동 통과·실패·복원 이력은 유지한다. PR Draft 유지, 운영 병합·배포 없음.
