# 마지막 식사 시각 명령 검증 — 2026-09-16

## 지원 범위

- `오늘 마지막 식사 18:30 기록해줘` 또는 `오늘 마지막 식사 시각 18:30 저장해주세요`.
- 오늘(한국 시간)의 명시적인 24시간제 `HH:mm`만 받는다. 없는 날짜, 다른 날짜, 아직 지나지 않은 시각, `지금`·오전/오후 등 추정이 필요한 표현, 여러 작업을 섞은 문장은 저장하지 않는다.
- 15분 확인, 같은 탭의 확인 요청 복구, 저장 응답 유실 재시도, 공통 식단 실행 이력과 되돌리기를 연결한다.
- 식단 요약·끼니 상세·저녁 시각·공복 시작을 한 트랜잭션으로 맞춘다. 기존 화면의 정의에 따라 `dinnerBefore1830`만 입력 시각으로 계산하며 공복 성공·시간·달성 상태, 식사 완료, 물·영양값·메모는 보존한다.
- 공복 시작의 과거 원시 시각·JSON 시각·객체/JSON 날짜 지도 형식을 모두 읽는다. 새 저장은 오늘 날짜가 있는 객체로 기록하므로 다른 날짜에 오늘 시각을 재사용하지 않는다. 날짜 지도의 이전 날짜와 추가 필드는 유지하고 되돌리면 이전 원시 값을 그대로 복구한다.
- 캘린더의 공복 타이머도 같은 형식 판독기를 사용한다. 기존 원시 시각은 날짜 정보가 없으므로 과거 클라이언트와 동일하게 읽는다.
- 관련 일별 필드와 전체 공복 시작 값을 확인 당시/저장 직후 값과 비교한다. 공복 값에 다른 날짜가 추가됐어도 되돌리기는 보수적으로 중단한다. 다른 저장소의 다른 날짜는 보존한다.
- 기존 물·메모·단백질·밥량 확인/이력 형식과 초기화 잠금·소유자 RLS를 유지한다. 새 함수는 invoker, 빈 search_path, 인증 사용자 권한만 사용한다.

## 로컬 검증

- 단위/DB **497/497**, 새 시각 명령 **21/21** 및 검사 종료 경합 **3/3** 통과. 네 저장소 원자성, 기존 네 형식, 미기록/빈 값 복구, 응답 유실, 만료, 잘못된 시각/미래 시각/날짜, 충돌, 초기화, 소유자와 익명 권한 포함.
- VM 동기화 **27/27**, lint·TypeScript와 독립 Webpack 빌드 통과. 공유 의존성 심볼릭 링크가 있는 로컬 환경이므로 최종 CI에서는 실제 기본 Turbopack 빌드도 통과했다.
- 브라우저 신규 4개 시나리오를 Chromium·작은 WebKit에 추가했다. 최종 CI에서 두 엔진 합계 **8/8** 통과했다.

## 진행 상태

- Preview 배포와 호스팅 저장·재조회·복구는 완료. 검증용 이력 정리와 21개 테이블 원본 보존, 수정 후 전체 CI **134/134**까지 완료.
- 정식 앱 반영·PR 병합·실제 iPhone/Siri 검증은 마지막 단계다. 무료 모드·Zephyr 설정·일일 분석 예약을 유지한다.

## 호스팅 Preview 관찰

- 앱 소스 `39b14021d0b6a777789542f3fe6fb3b10cc261c7`, Preview `dpl_6Fg6GBk85sNgd8z41kg1pFQ6ZiHS` READY, target null 및 Preview 별칭 연결 확인. 이후 소스 `12e132ddd9d9ed910fca1179b6cbca04abd34238`는 브라우저 검사 선택자만 수정했다. 해당 Preview `dpl_7pzBYRffH6Y3n4uKkE8fyVACAzbr`도 READY, target null 및 같은 별칭 연결을 확인했다.
- 호스팅 마이그레이션 `assistant_diet_time_commands` 적용 전후 허가된 테스트 계정의 **21개 테이블** 행 수·전체 행 내용 해시 일치. 기존 성장 루틴 3건, 언어·AI 사용·Zephyr 이력 포함.
- 보안 점검은 기존 내부 RLS 테이블의 의도된 정책 없음 6건과 비밀번호 유출 보호 설정 1건이며 새 함수 관련 경고는 없다. 기존 안내: [내부 테이블 RLS 정책](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy), [유출 비밀번호 보호](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection).
- 14:34 UTC 확인 화면에서 저장 전 미기록을 확인하고 취소. 새 요청을 만든 뒤 새로고침으로 확인 화면 복구.
- 14:35 UTC 마지막 식사 18:30 저장. 일별 요약·끼니·저녁 시각·날짜가 있는 공복 시작이 일치하고 다른 완료·영양·공복 달성 값은 생성하지 않았다. 식단 화면 이동 및 새로고침 후 18:30, 다음 날 06:30·08:30 표시 확인.
- 14:36 UTC 18:30을 19:10으로 변경. 미래 시각 23:59는 오류 안내와 함께 차단됐고 저장값은 19:10 유지. 식단 요약의 18:30 이전 여부는 false이며, 운동 캘린더에서 `미달성(19:10)`과 `시작 19:10 / 예상 종료 09:10` 표시를 확인했다.
- 이력에서 오래된 18:30 명령을 먼저 되돌리면 이후 기록 보호 오류가 표시된다. 최근 19:10부터 되돌리고 18:30을 되돌려 14:38 UTC 원본 상태 **8개 키를 정확히 복구**했다. 식단 화면의 시각이 빈 값, 계산 결과가 시간 미설정으로 돌아온 것을 확인했다.
- 음성·유료 AI 호출 없이 검증했다. 14:51 UTC 마지막 요청의 15분 유효시간과 여유 5초가 지난 뒤, 상태 원본 및 복구 직후 갱신 시각을 잠금 안에서 대조했다. 확인한 전체 행 값·ID·소유자가 일치하는 임시 이력 2건·대화 6건만 정리하고 테스트로 변한 상태 갱신 시각을 원본으로 복구했다. **21개 테이블의 행 수·전체 행 내용 해시가 모두 작업 전과 일치**한다.

## 브라우저 검사 수정 이력

- [첫 CI 35109215021](https://github.com/jace3695/AI-fitness-app/actions/runs/35109215021)은 14:00 공복 예상 시각이 요약과 공복 기록 두 곳에 표시돼 단일 요소를 요구하는 새 검사 하나가 실패했다. 실제 저장 오류는 아니며, 해당 검사 선택자를 `공복 기록` 제목이 있는 section 내부로 한정했다.
- 수정 소스의 새 검사를 시작하면서 첫 실행은 취소됐다. 종료된 로그에 성공 80개·실패 1개, 새 시각 명령 성공 3개가 확인됐고, 81개 정리 보고의 합성 Auth 계정 98개 제거·잔여 행 0·외부 origin 0 및 전체 임시 DB/키 제거·증거 업로드 성공을 확인했다. 이를 전체 검사 통과로 계산하지 않는다.
- [두 번째 CI 35110325353](https://github.com/jace3695/AI-fitness-app/actions/runs/35110325353), 소스 `12e132ddd9d9ed910fca1179b6cbca04abd34238`에서 브라우저 **133/134**, 새 시각 명령 **8/8** 통과. 기존 자기계발 WebKit 복구 검사 1개가 `route.fulfill: Route is already handled!`로 실패했다. 134개 정리 보고·합성 Auth 계정 165개 제거·잔여 행 0·외부 origin 0·전체 임시 DB/키 제거 및 증거 업로드 성공을 확인했다.

- 설치된 Playwright의 종료 처리를 확인했다. `unrouteAll({behavior: 'wait'})`는 먼저 라우팅 목록을 비우며, 먼저 끝나는 콜백이 서버의 가로채기를 해제해 아직 전달 중인 다른 응답과 경합할 수 있다. 공통 fixture에 처리 중인 콜백을 추적하는 `RouteDrain`을 추가하고 **가로채기가 설치된 상태에서 모든 콜백을 기다린 뒤** 해제한다. 오류는 계속 전파하며 검사 조건·시간 제한·자동 재시도는 바꾸지 않았다. [공식 종료 API](https://playwright.dev/docs/api/class-browsercontext#browser-context-unroute-all).
- 새 비동기 회귀 3개는 동시 응답 중 마지막 응답 대기, 기다리는 중 추가되는 응답, 오류 전파와 정리 완료를 검사한다. 로컬 전체 **497/497**, 해당 파일 lint·TypeScript 통과. 앱 실행 코드는 유지되며 소스 `61a215c9d5d334c41415cb0e99edbbd09bc0cec2`의 [최종 CI 35112668023](https://github.com/jace3695/AI-fitness-app/actions/runs/35112668023)가 모두 통과했다. 해당 Preview `dpl_ASYAr2NiibiBcSQGyBb2e1vgWaLG`도 READY·target null·Preview 별칭 연결을 확인했다.

## 최종 결과

- 최종 소스 `61a215c9d5d334c41415cb0e99edbbd09bc0cec2`: 단위/DB **497/497**, VM 동기화 **27/27**, lint·TypeScript·실제 기본 Turbopack 빌드·전체 브라우저 **134/134** 통과.
- 새 시각 명령 **8/8**, 기존 자기계발 명령 **8/8** 통과. 확인 전 보존·취소·같은 탭 복구·응답 유실 재확인·320px 화면·재조회·서로 다른 세션 충돌·이후 직접 수정 보호·역순 되돌리기·소유자 격리·만료를 포함한다.
- 최종 134개 정리 보고에서 합성 Auth 계정 **165개** 제거, 추적 행 잔여 **0**, 외부 origin **0**. 검증 DB·볼륨·키 제거와 정제된 결과 업로드 성공. [검증 결과](https://github.com/jace3695/AI-fitness-app/actions/runs/35112668023/artifacts/10455025148).
- 실제 호스팅 저장·재조회·공복 표시·오류 안내·되돌리기와 원본 상태 복구, 요청 만료 후 임시 이력 정리를 별도로 완료했다. 로컬/CI 결과와 실제 호스팅 결과는 위에 구분했다.
- 정식 앱 반영·PR 병합·실제 iPhone/Siri 검증은 남아 있다. 과거 날짜, 자동 공복 달성, 오전/오후 표현이나 현재 시각 추정, 여러 명령 동시 수행은 이번 범위에 포함하지 않는다. 6단계 전체는 부분 완료다.
