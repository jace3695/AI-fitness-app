# 운동·식사 기록 함께 보기 — 2026-09-17

## 범위와 한계

식단에 한국 날짜로 어제까지 28일의 운동 표시·운동 후 식사 응답·마지막 식사 시각을 함께 표시한다. 기존 운동 기록에는 실제 시작/종료 시각이 없고 저장 시각만 있어, 이번 단계에서는 시간 간격을 계산하지 않는다. 실제 운동 시각 입력과 시간 간격 비교는 후속 범위다.

- 운동 완료·일부·중단 및 유산소·철봉의 명시적 표시를 사용한다. 저장 시각·목표·메모·소요 시간만으로 운동을 추정하지 않는다.
- 운동 표시가 있는 날의 식사 응답만 예/아니요/미기록으로 집계한다. 운동 미확인은 휴식이 아니다.
- 날짜별 근거에 양쪽 기록을 함께 표시한다. 마지막 식사가 운동 후 식사라고 추정하지 않으며 미기록·잘못된 시각을 구분한다.
- 오늘 입력, 오늘/미래 날짜, 기간 밖 기록과 잘못된 날짜를 제외한다. 동기화된 기존 조회 결과만 사용하며 데이터 쓰기·DB 변경·AI 호출이 없다.
- 저장 형식을 해석할 수 없으면 오류 안내를 표시한다. 기존 공통 동기화 상태와 입력 보호를 유지한다.

## 기본 검증

단위/DB **572/572**(새 4개), 타입·린트 통과. 날짜 경계·윤년·미기록·잘못된 형식·저장 시각 추정 방지·입력 원본 보존 확인.

격리 브라우저에는 320px 근거 조회·입력 분리·새로고침·재진입·원본 보존과 잘못된 운동 기록 오류·복구의 2개 시나리오를 양쪽 브라우저에 추가했다. Preview와 최종 CI 결과는 확정 후 기록한다. 운영 반영과 실제 iPhone은 마지막 단계로 유지한다.

## 실제 Preview 확인

- 코드 `8751242f529c8422c4d29c8218e7e2892b8a4296`, tree `298f3aaae2ad04167205b0cb88cf13f69fe1c237`, Preview `dpl_32Ee6KP6DJKTCMBU1dqDZqGHrifo` READY.
- 허가된 테스트 계정은 최근 28일 식단·운동 기록이 없어, 실제 Preview에서 0일 집계·날짜별 근거의 기록 없음 안내를 확인했다. 기간 밖의 기존 운동 기록을 최근 기록에 포함하지 않았다.
- 오늘의 운동 후 식사 응답을 미기록→예→미기록으로 바꾸는 동안 과거 집계가 그대로였고 저장은 실행하지 않았다. 새로고침과 홈 이동·식단 재진입에서 같은 집계를 확인했다.
- 전후 27개 테이블의 행 수·전체 내용 해시 일치. 호스팅 합성 자료 생성·수정·삭제 없이 조회와 입력 원복만 수행했다. 날짜별 값·잘못된 형식 복구는 격리 CI 범위다.
- 브라우저 제어에서 label 평가와 ‘홈’ 링크 선택이 실패했으나, 새 DOM에서 실제 combobox와 ‘AI 연이’ 링크를 확인해 정상 조작했다. 앱 오류나 통과한 검사로 취급하지 않는다.

## 첫 실행의 테스트 기준 수정

Actions `35221268702`의 Chromium 새 근거 사례는 화면·재조회·전체 상태 비교까지 통과했으나, 그 뒤 공통 보조 검사에서 실패했다. 이 검사는 운동 날짜가 추가되지 않은 공통 fixture를 기준으로 삼아, 이 사례가 시작 전에 추가한 합성 날짜 2개를 차이로 판단했다. 전체 키와 중첩 값이 실제 시작 상태와 정확히 같은지 확인하는 `toEqual(state)`를 유지하고 잘못 적용한 공통 보조 검사를 제거했다. 앱 코드·시간 제한·재시도 정책은 바꾸지 않았다. 수정본 업로드로 오래된 실행을 종료하고 새 전체 검증을 진행한다. 정리 여부와 최종 결과는 별도로 기록한다.
