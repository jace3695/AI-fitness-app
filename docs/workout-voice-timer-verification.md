# 운동 중 연이 음성·타이머 첫 터치 수정 — 2026-09-19

## 사용자 iPhone 확인

- 화면/저장, 일반 음성, 잠금/복귀: 정상으로 보고받았다.
- 발열: **사용 7분 경과 시 정상**. 장시간 발열 검증으로 확대하지 않는다.
- 추가 요청: 운동 중 안내를 연이 목소리로 통일하고, 동작 타이머 첫 시작 시 Vercel Toolbar 메뉴가 열리는 현상을 없애기.

## 수정

- 운동 세트·휴식·다음 동작·완료 안내의 `speechSynthesis`를 제거하고 기존 인증/월 승인/문자수 제한을 적용하는 `/api/tts`의 **ko-KR-Chirp3-HD-Zephyr**로 연결했다. 기존 문구를 유지한다.
- 타이머 시작 터치로 같은 오디오 요소를 활성화하고 다음 전환 문구를 준비한다. 준비 여부와 관계없이 타이머는 바로 시작한다. 완료 이벤트에서 두 안내가 서로 끊던 호출도 하나로 정리했다.
- 짧은 운동 안내(200자 이하)는 계정/월/문구 해시로 탭의 sessionStorage에 보관한다. 재생·일시정지·새로고침은 받은 오디오를 재사용한다. 일반 비서 답변의 기존 메모리 전용 정책은 유지한다.
- 생성 전 요청 번호 보관, 응답 유실 시 재생성 금지, 저장 용량 초과 시 기존 시도 기록 유지, 기기 음성으로 대체하지 않는 정책을 유지한다. 새 탭/다른 기기까지 동일 음성을 공유하는 기능은 아니다.
- 음소거·이전 안내 대체·통증 화면·종료·계정 변경 시 늦은 응답이 재생되지 않게 한다. 자동 재생이 막히면 이미 받은 안내를 버튼으로 재생한다.
- 타이머 버튼 터치 영역은 최소 44px. 일시정지 시 음성 준비/화면 잠금 요청을 반복하지 않는다.

## Vercel Toolbar

- 사용자 사진은 Preview 개발 도구 메뉴다. 앱 타이머에는 이 메뉴를 여는 코드가 없다. 사진만으로 첫 터치의 정확한 좌표/겹침을 재현했다고 주장하지 않는다.
- Vercel 프로젝트 `ai-fitness-app` → General → Vercel Toolbar → **Pre-Production Deployments: Off**를 저장하고 새로고침 후에도 Off인 것을 확인했다. Production 설정은 Default 그대로다.
- 공식 설정은 **새 배포부터 적용**된다. 새 Preview에서 Toolbar 스크립트/프레임 유무와 타이머 동작을 확인한다. 인증·배포 보호 설정은 변경하지 않았다.
- 공식 설명: https://vercel.com/docs/vercel-toolbar/managing-toolbar

## 검증 범위

- 캐시 단위 검사: 운동 안내 재사용, 계정 격리, 유실/손상 응답 재생성 금지, 보관 실패 시 메모리 재생.
- 격리 Chromium/작은 WebKit: 첫 시작·일시정지·계속·자동 전환·다시 재생·새로고침, 한도/통신 오류에도 타이머 유지, 대기 중 음소거, 기기 음성 미사용, 기록 보존.
- 위 브라우저 검사는 합성 음원을 쓰며 Google 호출/사람의 음질 판정으로 계산하지 않는다.
- 새 코드의 CI와 호스팅 Preview 결과는 PR #189에 기록한다. 수정 후 실제 iPhone 음질·첫 터치 결과는 별도 재확인 대상이다.
