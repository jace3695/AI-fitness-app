# Zephyr 답변 읽기 — 2026-09-16

## 청구 확인과 현재 상태

사용자가 제공한 9월 16일 Google Cloud 보고서 영상에서 `Count of characters for Chirp3-HD voices`, SKU `F977-2280-6F1B`의 **244자·사용비용 0원·소계 0원**을 확인했다. 앞서 직접 생성한 61자와 앱에서 생성한 183자의 합계와 일치한다. Standard 음성 5자는 별도 항목이고, 보고서 전체 112원은 Gemini API 항목이다. 이 결과는 해당 보고서의 사용분에 대한 확인이며 앞으로 발생할 모든 사용료를 보장하지 않는다.

일반 답변 읽기 UI를 구현한 뒤 9월 월 승인과 Preview 전용 설정을 적용했다. **일반 `/api/tts` 경로의 실제 Google 생성 1회·38자와 5.616초 재생 완료를 확인했다.** 같은 음성의 다시 재생도 완료했고 원장은 1회·38자로 유지됐다. 기존 244자의 0원 보고서와 이번 추가 38자의 아직 확인하지 않은 청구 내역을 구분한다. 실제 iPhone 확인과 운영 반영은 마지막 단계에 남아 있다.

## 사용할 수 있도록 준비한 화면

- 가계부 AI 상담의 답변 읽기를 공통 Zephyr 재생에 연결했다. 기존 자동 읽기 토글과 자동 호출을 제거했다.
- 비서 대화의 답변, Siri 빠른 명령 결과, 네 분야 무료 조언, 저장된 ChatGPT 조언에 같은 버튼을 추가했다.
- 화면을 열거나 답변이 도착하는 것만으로 음성 API를 호출하지 않는다. `speak=1` 주소도 음성을 자동 생성하지 않는다.
- 버튼 옆에 읽을 문자수와 Google 전송 안내를 표시한다. URL과 마크다운 표시를 정리하고, 1,200자를 넘는 답변은 앞부분만 읽는다고 안내한다.
- 버튼 클릭 → 로그인 계정 확인 → 서버 한도 조회 → 요청 번호 보관 → 서버 문자수 예약 → 음성 생성 → 브라우저 재생 순서다.

## 중복 호출과 재생 처리

- 같은 계정·월·정리된 답변의 동시 요청은 한 번의 생성으로 합친다. 받은 음성은 메모리에 최대 8개 보관하고, 재생 중지·재생 완료·다시 재생을 구분한다.
- 재생이 브라우저 정책으로 막히면 받은 음성의 재생 버튼을 안내한다. 다시 재생할 때 추가 TTS 호출을 하지 않는다.
- 생성 전 보관하는 시도 기록은 계정/월/문장의 SHA-256과 요청 UUID만 포함한다. 문장과 음성은 브라우저 영구 저장소에 보관하지 않는다.
- 같은 탭에서 새로고침으로 오디오를 잃거나 응답이 유실되면 같은 답변을 다시 생성하지 않는다. 한도 확인에서 차단된 요청은 음성 생성 시도로 기록하지 않아 나중에 상태를 다시 확인할 수 있다.
- 시도 기록은 `sessionStorage` 범위다. 새 탭·다른 기기·탭 종료 이후의 동일 문장까지 전역으로 금지하는 기능은 아니다. 서버의 요청 번호 중복 금지와 앱 전체 월/일/속도 한도는 계속 적용된다.
- 문장이 바뀌거나 화면을 떠나거나 계정이 바뀌면 이전 음성의 재생을 중단한다. 늦게 도착한 이전 응답을 자동 재생하지 않는다. 다른 답변을 재생하면 앞서 재생 중인 음성을 멈춘다.
- 기기 음성이나 다른 유료 모델로 자동 전환하지 않는다. 실패한 Google 요청의 예약 문자수는 기존 정책대로 돌려주지 않는다.

## 검증

- 코드: `0d8187138cf9895481eb7277e0d3fb2cf7c7e63e`.
- Preview: `dpl_4prJrmdKU1RCK3K2DPtuYq2ZX1jq`, READY, 위 코드와 일치. 운영 대상이 아닌 브랜치 배포다.
- Vercel 인증을 거치는 정상 커넥터 조회에서 앱 사용자 인증 없는 `/api/tts`는 HTTP 401·`Cache-Control: no-store`를 유지했다. 인증 후 실제 음성 검증으로 계산하지 않는다.
- 로컬 단위/DB **382/382**, lint, TypeScript, diff 공백 검사 통과.
- 새 단위 검사 7개: Unicode 제한, 동시 생성/캐시 재생, 응답 유실/새로고침, 한도 도달, 저장소 실패, 잘못된 음성/응답·제공자 오류, 계정/월 격리와 캐시 제거 후 시도 기록 유지.
- [독립 브라우저 CI 35043561251](https://github.com/jace3695/AI-fitness-app/actions/runs/35043561251) **두 번째 실행 성공**: 단위/DB 382개·코드 검사·독립 빌드·Chromium/WebKit 90/90 통과. 새 음성 사례 6개도 모두 통과했다.
- 첫 실행은 89/90이었다. 기존 WebKit 가계부 항목 수정 사례에서 `선택한 결제수단 변경` 확인창의 버튼을 15초 내 찾지 못했다. 해당 확인창·수정 로직은 이번 음성 변경에 포함되지 않았으며 원인은 확정하지 못했다. 앱·검사 코드를 바꾸지 않고 실패 작업을 한 번 재실행했고, 두 번째 실행에서는 같은 사례를 포함해 전체 검사가 통과했다. 재시도 횟수나 제한시간을 늘리거나 검사를 제외하지 않았다.
- 두 실행 모두 각각 임시 계정 109개 제거·추적 행 잔여 0·외부 origin 0·검증 DB/키 정리를 확인했다.
- 추가 브라우저 사례는 실제 로그인·저장된 비서 답변·가계부 답변·빠른 명령을 사용한다. TTS 응답은 로컬에서 만든 0.7초 MP3로 대체하고 실제 오디오의 재생 완료를 확인한다. Google 호출을 하지 않는다.

## 9월 승인과 Preview 활성화

- 사용자가 제공한 사용자 인증 정보 화면에 프로젝트 `AI Yeoni Calendar`, 키 이름 `AI-Yeoni-TTS`, 생성일 2026-09-07, 제한 API `Cloud Text-to-Speech API`가 표시됐다. 앞선 보고서의 프로젝트 ID `ai-yeoni-calendar`, 전체 조회 범위의 244자·0원, 기존 Preview TTS 키 등록일 9월 7일과 대조했다. 키 원문을 열람하거나 새 키·결제 계정을 만들지 않았다.
- 성공한 이전 검증 슬롯 세 개는 모두 사용 완료이며 키 SHA-256 지문은 하나였다. 이 지문에 9월 승인 한 건을 연결했다. 승인 전 월 행·일반 예약은 모두 0건이었고, 기존 행의 예약량을 초기화하지 않았다.
- [Google 공식 요금표](https://cloud.google.com/text-to-speech/pricing)를 2026-09-16 다시 확인했다. Chirp3-HD SKU `F977-2280-6F1B`는 월 100만 자 무료, 초과분은 문자당 USD 0.00003이다.
- 승인 값: 기존 보고서 사용분 **244자**, 외부 사용 여유분 **799,756자**, 앱 전체 월 상한 **100,000자**. 합계 900,000자로 Google 무료 한도 대비 100,000자를 남긴다. 외부 여유분은 계획상 할당이며 다른 프로젝트의 실제 호출을 차단하거나 실시간 집계하는 기능은 아니다.
- 승인 만료: 2026-10-01 07:00 UTC, Google 보고서의 Pacific 월 경계. 기존 함수는 만료 2분 전인 **한국 시간 10월 1일 15:58부터** 새 생성을 차단한다. 다음 달 자동 승인 없음.
- Vercel Config `GOOGLE_TTS_FREE_ENABLED=true`는 **Preview / `fix/app-wide-reliability` 브랜치 하나**에만 저장했다. Production·다른 브랜치·기존 키·공통 `FREE_MODE=true`·인증 보호·DB 구조와 권한·일일 분석 예약을 변경하지 않았다.
- 같은 소스 `bcd648ca6fce3b6093630f2dcfa24f35ba5a7f8d`를 새 설정으로 재배포했다. `dpl_G23DQkuGhAPqMHgBEVxbB36fnrrq`, `ai-fitness-g4ebioxjm-jace3695s-projects.vercel.app`, READY, target Preview. 운영 배포를 선택하거나 승격하지 않았다.

## 실제 일반 답변 검증 — 2026-09-16

기존 검증 계정으로 보안 입력 로그인 후 [Preview 빠른 명령](https://ai-fitness-app-git-fix-app-wide-reliability-jace3695s-projects.vercel.app/assistant/quick)에서 실행했다. 개인 기록이 포함되지 않는 고정 안내 문장만 Google에 보냈다. TTS 응답을 모사하지 않았다.

| 확인 | 관찰 결과 |
|---|---|
| 명령 | `일본어 복습 시작해줘` |
| 읽을 답변 | `일본어 복습 화면을 준비했습니다. 아래 버튼을 눌러 바로 시작하세요.` — 공백·문장부호 포함 38자 |
| 읽기 전 | 답변 도착 후에도 일반 예약 0건·0자. 자동 음성 생성 없음 |
| 명시적 답변 읽기 | 일반 API와 월 승인 경로로 생성 1회 성공. 화면 남은 한도 99,962자 |
| 서버 예약 | 한국 시간 11:57:58.972, 요청 `403a8e14-4920-4086-90d4-1fcfe7851a9b`, 38자. 계정·문장 해시 일치 |
| 실제 오디오 | MP3 길이 5.616초, 재생 중 `paused=false`, 마지막 `currentTime=duration`, `ended=true`, 오류 없음 |
| 다시 재생 | 같은 화면에서 다시 재생 버튼 클릭 후 두 번째 재생 완료. 원장 1건·38자, 남은 99,962자 그대로 |
| 검증 대화 정리 | 이번 명령으로 만든 정확한 메시지 두 건만 삭제. 기존 계정의 메시지 수와 전체 행 해시가 시작 전과 일치. 음성 예약은 환불·삭제하지 않음 |

이번 후속은 설정과 승인 데이터 적용이며 앱 코드를 바꾸지 않았다. 위 단위/DB 382개와 브라우저 90개는 동일 코드의 앞선 검증 결과이고, 이번에는 호스팅된 일반 경로의 실제 생성·재생을 추가로 확인했다. 다른 답변 화면은 공통 컴포넌트 및 앞선 격리 검사 범위이며 각 화면에서 Google을 반복 호출하지 않았다.

기존 보고서 244자에 이번 38자를 더한 **282자는 확인된 생성량의 합계**다. Google 보고서에 282자·0원이 표시됐다는 뜻은 아니다. 이번 38자의 청구 반영, 실제 iPhone의 재생·백그라운드 동작과 운영 반영은 아직 확인하지 않았다. 사람이 음질을 평가한 결과로도 설명하지 않는다.
