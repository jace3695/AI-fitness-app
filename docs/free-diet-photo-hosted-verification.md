# 무료 식단 사진 실제 판독 검증 — 2026-09-13 KST

실제 Google 판독·추정값 수정·확인 후 입력칸 반영을 통과했다. PR #189는 Draft이며 운영 병합·Production 배포·DB 재마이그레이션은 하지 않았다.

## 전제와 배포

사용자가 `AI-Yeoni-Free`의 Free 등급·결제 계정 미연결, `GEMINI_FREE_API_KEY`와 `GEMINI_FREE_TIER_CONFIRMED=true`의 Preview 저장 완료를 확인했다. 이를 기준으로 재개했다. Google 로그인 화면에 다시 접근하거나 API 키 값을 읽지 않았다. 환경변수 관리 화면의 범위를 별도로 조회한 것은 아니며 실제 요청으로 런타임의 전용 키·무료 확인 조건 통과를 확인했다.

동일 소스 트리로 재배포한 커밋 `347ee6ceda4e8d733c9df68a45d4c02639afc194`, Preview `dpl_CUMMmUGB6Sy1imk53FuJNighagyf`는 READY·`target: null`이었다. 기존 Gmail 테스트 세션과 이전의 합성 음식 사진을 사용했다. 사진 SHA-256은 `5389df40d7c0e3d785c514db3f378d804f2a58764e4ad47b179fd76ae3f0a7a6`으로 기존 검증 기록과 동일하다.

## 실제 실패와 수정

23:27:53 KST의 요청은 설정 확인을 통과했지만 Google의 `gemini-2.5-flash-lite` 요청이 **404 `NOT_FOUND`**로 거절돼 앱이 503을 반환했다. 이 응답만으로 키 오류나 모델의 전면 종료를 단정하지 않는다. 자동 재시도는 없었고 테스트 계정 동기화 전체 행은 변경되지 않았다.

수정 커밋 **`0af64f6dc21f1bfcf8d642460b6c988e5ce71b27`**은 식단 사진 전용 모델을 `gemini-3.5-flash-lite`로 변경했다. [모델 문서](https://ai.google.dev/gemini-api/docs/models/gemini-3.5-flash-lite)의 이미지 입력·구조화 출력과 [요금표](https://ai.google.dev/gemini-api/docs/pricing#gemini-3.5-flash-lite)의 무료 입력/출력을 확인했다. Gemini 3의 `thinkingLevel: minimal`과 출력 상한 768을 사용한다. 전용 키·무료 확인 조건, 25초 제한, 단일 요청, 유료 라우터 미사용, 오류 시 직접 입력 안내, 불완전한 응답 거부는 유지했다. 자동 모델 탐색·대체·재시도는 추가하지 않았다.

## 실제 호스팅 결과

수정 Preview **`dpl_58HoDH4ZSQ1g9W7dBC51gwn2BHqZ`**는 위 수정 커밋과 일치하며 READY·`target: null`이다.

- [고정 Preview](https://ai-fitness-ihshmwbtl-jace3695s-projects.vercel.app)
- [브랜치 식단 화면](https://ai-fitness-app-git-fix-app-wide-reliability-jace3695s-projects.vercel.app/diet)

| 항목 | 이번 실제 결과 |
| --- | --- |
| 전송 전 확인 | 사진 선택 후 데이터 활용 안내 확인 전 분석 버튼 비활성, 확인 후 활성 |
| 실제 Google 판독 | **23:33:44 KST `POST /api/diet/photo-analysis` 200**, Vercel `cache: MISS`와 화면 결과 일치. 모의 제공자 응답을 사용하지 않음 |
| 음식명 | **쌀밥, 닭가슴살 구이, 브로콜리 데침**. 사진에 보이는 세 음식과 일치 |
| 최초 추정값 | 단백질 **35g**, 조리된 밥 **150g**, 모델 표시 신뢰도 높음 |
| 수정·확인 | 단백질 **32g**, 밥 **130g**으로 수정. 음식·양 확인 전 반영 버튼 비활성, 확인 후 활성 |
| 입력 반영 | 점심 식품 단백질 직접 입력 32g·밥량 130g·합계 32g 표시. 반영 후 확인 체크 해제와 재반영 버튼 비활성 |
| 자동 저장 차단 | 입력 반영 직후에도 서버 동기화 전체 행이 시작 전과 동일. 아직 저장되지 않았다는 화면 안내 |
| 정리·새로고침 | 검증용 입력값과 사진 선택 정리 후 새로고침. 미리보기·판독 결과 없음, 분석 버튼 비활성 |
| 원본 보존 | 최종 테스트 동기화 **전체 행**이 시작 전과 동일. 사진 data URL 없음. 기존 AI 사용 행 1개 유지, 이번 신규 사용 행 0개 |

실제 중량 정답이 없는 합성 사진이므로 위 수치를 영양량 정확도 검증으로 확대하지 않는다. 모델이 표시한 신뢰도 역시 독립 검증 수치가 아니다.

이번 호스팅 검증에서는 **오늘 식단 저장을 누르지 않았다**. 실제 판독·수정·확인 후 입력 반영·자동 저장 차단과 원본 보존을 검증했다. 저장·재조회는 아래 격리 브라우저 검사 범위로 구분한다. 개인 대표 계정의 기록은 조회하거나 변경하지 않았다.

무료 경로는 공통 유료 사용량·예약 함수를 호출하지 않는다. 신규 사용 행 0개를 Google 청구 명세 검증으로 해석하지 않는다. Free·결제 미연결은 사용자의 확인에 근거하며 이번 작업에서 결제 연결·충전·유료 키 사용·자동 모델 전환은 수행하지 않았다.

## 자동 검사

- 관련 로컬 검사 **11개**, 변경 파일 lint, TypeScript, `git diff --check` 통과. 모델 없음 오류의 단일 요청·민감한 오류 원문 미노출 회귀 포함.
- 수정 커밋의 [GitHub Actions `34762948494`](https://github.com/jace3695/AI-fitness-app/actions/runs/34762948494) **전체 성공**. 단위·DB 계약 검사 256개, lint·타입·48경로 빌드, Chromium/WebKit 브라우저 28개 통과. 브라우저의 성공 판독값·한도 오류는 모의 응답이며 위 실제 Google 응답과 구분한다.
- 격리 검사의 정리 이벤트 28건, 일회용 계정 31개 제거, 관련 행 잔여 0개, 차단된 외부 origin 요청 0건. 대상 요청 161건 중 실제 격리 서버 응답 157건이며 기존 복구 검사의 의도적 조회 오류 4건을 포함한다. 식단 저장·새로고침, 사진 미저장, 무료 오류 뒤 초안 유지 회귀가 양쪽 엔진에서 통과했다.
- 공개 증거 artifact는 19,992바이트, SHA-256 `d6838f67219252aa66c4347edfae311ecb10ba31f4ce930fa1fc41a29f869dcf`. 인증정보·개인 원본·합성 사진은 공개 증거에 넣지 않는다.

실제 무료 사진 판독 대기는 해소됐다. iPhone 전체 화면·정밀 잠금·장시간 백그라운드 검증은 사용자 요청대로 마지막 단계에 남겨 둔다. 결과 문서만 추가하는 후속 커밋은 위 코드 커밋의 검사와 구분한다.
