# 1~9단계 연속 작업 — 2026-09-19 검증 완료

사용자는 1~9단계를 이어서 진행하도록 요청했다. 기존 기능을 다시 만드는 대신 남은 자체 기능을 구현하고 외부 조건과 실제 검증을 분리한다. Preview·허가된 테스트 계정만 사용하며 실제 iPhone 및 운영 병합은 마지막에 유지한다.

## 이번 변경

- 운동 상세 명령: 오늘의 피로도(1~5), 전체 난이도, 마지막 세트 RPE(1~10), 통증 부위, 일부 완료·중단을 한 항목씩 확인 후 저장한다. 기존 세트·유산소·다른 날짜 보존, 원자적 실행 이력, 같은 요청 재확인, 조건부 되돌리기를 기존 공통 이력에 연결했다. 통증 부위는 통증 있음도 표시하고 일부·중단은 완료 표시를 해제한다. 확인 화면에 함께 표시한다.
- 운동 기록 근거: 어제까지 28일을 두 14일로 비교하며 수행 상태·세트·피로·RPE·통증과 바로 다음 날짜의 컨디션을 표시한다. 미기록은 실패·휴식·무증상이 아니다. 일부 완료를 브리핑의 전체 완료로 표시하던 오류도 수정했다.
- 식단·운동 시각 근거에 당일 저장 단백질과 운동 후 식사 응답을 추가했다. 마지막 식사 시각을 단백질 섭취 시각으로 간주하지 않는다.
- 아침·저녁 요청형 브리핑: 할 일·예산·운동·식단·일본어·자기계발과 다음 행동, 조회 실패 영역을 구분한다. 채팅 브리핑도 실패하지 않은 영역의 결과를 보여준다. Work 예약 작업의 진행 상태를 앱 브리핑으로 대체하지 않는다.
- 기억 관리: 소유자 조회·추가·수정·삭제, 변경 전후 확인, 이전 내용과 일치할 때만 수정·삭제, 응답 유실 뒤 조회만 재시도. 원본 일정·기록에는 쓰지 않는다.
- 일본어: 기초·업무·여행 선택과 복습일·힌트·누적 오답에 따른 우선 문제, 도면 치수·공차 및 측정·품질 수업 2개(전체 62개). 예문은 직접 작성했다. 용어 확인 자료: [KEYENCE 일본어 공차 설명](https://www.keyence.co.jp/ss/products/measure-sys/gd-and-t/basic/about.jsp). 실제 검사 기준을 지정하는 자료가 아니다.
- 타자: 최종 입력에서 다른 글자 수, 같은 문장의 최근/이전 14일 가중 정확도·타수. 삭제·수정한 오타나 키 입력 이력은 측정하지 않는다. 한 연습의 고정 ID로 응답 유실을 복구하며 새 ID로 중복 저장하지 않는다.
- 손글씨: 남은 획 수·접촉 시간·화면 사용 범위, 실제 변화를 감지한 펜 압력만 표시한다. 목표 시간을 실제 수행 시간으로 쓰던 오류를 수정했다. 그림과 지표의 되돌리기를 함께 처리하며 빈 화면 저장을 막는다. 비공개 이미지·자료·세션은 고정 식별자로 재확인한다. 글씨 품질 점수는 만들지 않는다.
- 자기계발의 기존 중단 이유·요일·시각·운동일 비교와 목표 검토를 유지하며, 기록이 없다는 이유로 실제 미수행을 단정하던 코칭 문구를 정정했다.

## 자동 검증

검증한 기능 코드: `3d1c466428e11a319bccb17860867e00cc8198d1`. [CI 35308799008](https://github.com/jace3695/AI-fitness-app/actions/runs/35308799008)는 **2026-09-18 14:21:44 KST에 성공 종료**했다. 9월 19일 재개 시 같은 코드와 성공 결과를 확인했다. 완료 안내가 중단된 Work 내부 원인은 확인하지 못했으며 앱 수정으로 Work 중단이 해결됐다고 주장하지 않는다.

- 단위/DB **599/599**, 동기화 **27/27**, TypeScript·lint·CI 기본 Turbopack 빌드 통과.
- 로컬 기본 Turbopack 빌드는 작업 환경의 외부 node_modules 심볼릭 링크를 거부했다. 지원되는 `--webpack` 로컬 빌드 통과. CI는 기존 기본 Turbopack 빌드를 사용한다.
- 새 SQL의 형식 오류를 로컬 PGlite에서 수정했고, 저장·재시도·되돌리기·충돌·원자성·계정 분리 검사 6개가 통과했다. 수업 수 기준도 60→62로 갱신했다. 초기 실패를 최종 통과로 숨기지 않는다.
- `assistant_workout_feedback_commands` 적용 전후 테스트 계정 29개 테이블의 행 수와 전체 행 해시가 동일했다. 함수는 SECURITY INVOKER, 빈 search_path, anon 실행 불가, authenticated 실행 허용이다. 데이터 변환은 없다.
- 전체 브라우저 **204/204**, 새 7개 시나리오의 Chromium·320px WebKit **14/14** 통과. 응답 유실·중복 방지·동시 수정 보호·브리핑 조회 실패·일부 운동 표시·학습 복습 우선순위·비공개 Storage 계정 격리를 포함한다.
- 격리 검증 정리 보고 **204건**, 합성 계정 **249개 제거**, 잔여 행 **0**, 합성 Storage 파일 **2개 제거**, 차단된 외부 origin 목록 **빈 배열**, 임시 DB·키 제거 성공을 로그에서 확인했다.
- 전체 작업 상한은 더 많은 사례와 Storage 초기화를 위해 30→40분으로 변경했다. 개별 90초 제한·재시도 0·1 worker·원본 비교는 유지했다.


## 인증 후 실제 Preview 검증 — 2026-09-19

[검증한 Preview](https://ai-fitness-app-git-fix-app-wide-reliability-jace3695s-projects.vercel.app)는 위 기능 코드와 일치하며 Vercel 배포 `dpl_AsdRCgv3T4LBLdLk55JH8qhAY4Fy`가 READY였다. 허가된 Gmail 테스트 계정으로 로그인한 호스팅 데스크톱 브라우저와 소유자 범위의 DB 결과를 함께 확인했다. 로그인 화면만 확인한 결과가 아니다.

| 흐름 | 실제 확인한 결과 |
|---|---|
| 기억 관리 | 새 기억의 검토 전 DB 0건 → 확인 후 1건 → 새로고침 재조회 → 내용 수정·재조회 → 삭제 취소 시 1건 유지 → 삭제 확인·새로고침 후 0건. 일정·원본 기록에는 쓰지 않음 |
| 운동 상세 명령 | `오늘 운동 마지막 세트 RPE 7 기록해줘` → 변경 전후 확인 → 확인 대기 상태에서 새로고침 → 한 번 저장 → 공통 실행 이력 재조회 → 확인 후 되돌리기. 운동 상태 전체 해시가 저장 전과 일치함. 다른 상세 항목의 경계값·충돌·응답 유실은 자동 검증 범위 |
| 결제 예정일 | 검토 전 계획·지출이 생성되지 않음 → 임시 1,200원·매월 31일 설정 저장 → 9월 30일 표시 → 새로고침 → 19일로 수정·당일 예정 표시 → 설정 삭제. 지출 0건 유지, 기존 구독 1건 보존 |
| 아침·저녁 브리핑 | 두 모드 전환 후 할 일·예산·운동·식단·일본어·자기계발의 실제 조회 결과 표시. 되돌린 운동을 완료로 표시하지 않음. 호스팅 장애를 강제로 만들지는 않았으며 실패 영역 분리는 격리 CI에서 확인 |
| 운동 수행 근거 | 최근/이전 14일과 날짜별 근거 펼치기·새로고침. 이 계정의 기간 내 저장 기록 없음 표시 확인. 일부 수행·RPE·다음 날 컨디션의 값이 있는 사례는 격리 CI에서 확인 |
| 타자 | 최종 입력 `xx`에서 ‘천 2회’ 표시 → 진행 저장 1건 → 실제 문자 2·정확 문자 0·부분 진행 저장 → 새로고침 후 최근 14일 1회 표시 → 자기계발 홈에서 검증 기록 삭제 |
| 손글씨 | 빈 화면 저장 차단 → 실제 포인터 입력 2획 → 1획 되돌리기와 측정값 갱신 → 남은 1획·가로 27%·세로 7%·압력 미측정 저장. 빠른 검증 입력의 접촉 시간은 0초·실제 0분이며 목표 15분을 실제 시간으로 쓰지 않음 |
| 손글씨 자료·재조회 | 완료 세션과 비공개 PNG 자료가 같은 식별자로 연결됨. 27,084바이트 파일·비공개 버킷 확인 → 새로고침 후 자료함 재조회 → ‘열기’로 실제 1획 이미지 확인 → 자료 삭제와 완료 기록 삭제 → 자료·세션·파일 잔여 0 |
| 일본어 | 집중 복습 분야 선택과 복습할 문제 없음 안내, 전체 62개 수업 표시 확인. 새 ‘도면 치수와 공차 확인’·‘측정 결과와 품질 확인’ 수업의 실제 표현 화면을 열었음. 퀴즈 완료·발음·녹음·Apple Pencil 품질을 확인한 결과는 아님 |

### 검증 자료 정리와 기존 기록 보존

이번 임시 기억·예정일·타자·손글씨 자료와 세션은 앱에서 삭제했다. 확인을 끝낸 운동 변경은 앱에서 되돌렸고, 이번 대화 2건·되돌린 운동 이력 1건·임시 예정일 요청 이력 3건은 소유자와 정확한 ID로 한정해 정리했다. 기존 9월 17일 대화 2건·되돌린 유산소 이력 1건 등 검증 전 자료는 보존했다.

새 수업을 열면서 자동 생성된 이어하기 초안도 정리했다. 해당 신규 필드를 제외하면 검증 전 학습 상태 해시와 정확히 일치하는 것을 먼저 확인하고, 같은 현재 해시일 때만 제거했다. 전체 학습 초기화는 사용하지 않았다. 이후 브라우저의 학습 재동기화까지 확인했다.

최종 비교는 테스트 계정 소유의 **40개 테이블**을 대상으로 했다.

- 행 수: **40/40 일치**.
- `updated_at`을 제외한 저장 내용 해시: **40/40 일치**.
- 모든 열을 포함한 전체 행 해시: **38/40 일치**. `user_app_state`·`language_user_state` 두 테이블에는 정상 저장·복구의 갱신 시각이 남았다. 시각을 원래 값으로 덮어쓰지 않았다.
- 이번 합성 Storage 파일: **0개 남음**. AI·TTS 사용 원장의 건수와 내용도 검증 전과 일치한다.

## 마지막 기기 확인과 운영 반영

이 문서 갱신은 기능 코드·DB 구조·권한을 변경하지 않는다. [PR #189](https://github.com/jace3695/AI-fitness-app/pull/189)는 실제 iPhone 확인 전까지 Draft·미병합 상태로 둔다. 저장소 `AGENTS.md`의 실사용 검증 규칙과 사용자가 정한 마지막 단계 순서를 따른다.

| 남은 확인 | 실제 기기에서 확인할 내용 |
|---|---|
| iPhone 화면·입력 | 위 Preview를 Safari와 설치한 앱에서 열어 로그인 유지, 작은 화면 메뉴·입력·확인 버튼, 저장 후 새로고침을 확인 |
| 음성·녹음 | 이미 승인된 이번 달 범위에서 Zephyr 답변 읽기·일본어 소리·녹음 권한과 중지를 확인. 청구 반영은 별도 근거로 확인 |
| 백그라운드·업데이트 | 입력 중 화면 잠금·앱 전환·복귀, 타이머, 앱 업데이트 후 기록 유지와 실제 발열을 확인 |
| Apple Pencil | iPad가 있을 때 손글씨 입력·되돌리기·저장과 실제 압력 변화 확인. 데스크톱 포인터 결과로 대체하지 않음 |
| 운영 | 기기 결과를 기록하고 그때의 PR 필수 검사를 확인한 뒤 병합·배포, 운영 상태와 기록 보존을 재점검 |

닫힌 앱의 정시 푸시·외부 앱 연결·새 달 음성 승인은 아래 외부 조건에 속한다. 현재 구현·검증한 무료 기능 묶음의 완료를 원본 로드맵 전체 완료로 표시하지 않는다.

## 외부 조건

- 기존 무료 Gemini·Zephyr 검증은 유지한다. 이번 작업은 유료 AI/TTS를 추가 호출하지 않는다. 추가 음성의 실제 청구 반영과 다음 달 허용은 미확정 상태다.
- 2026-09-18에 조회한 Work 예약은 활성 상태, 매일 07:00 한국 시간, 최근 실행 시작 기록 2026-09-18 07:02:55였다. 시작 시각만으로 분석 완료·앱 저장 성공을 주장하지 않는다. 개발 작업에서 별도 분석방의 예약을 수정하지 않는다.
- 닫힌 앱의 정시 푸시·외부 앱 연결은 무료 조건과 기기 권한 확인이 필요한 후속 범위다. 실제 iPhone·Apple Pencil·음성·백그라운드·업데이트·발열 검증은 데스크톱 브라우저로 대체하지 않는다.
