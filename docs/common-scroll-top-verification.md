# 공통 TOP 버튼 및 운동 화면 전환 검증 — 2026-09-07

## 변경과 첨부 자료 확인

일본어 전용 TOP 버튼을 인증된 앱의 공통 메뉴에 연결했다. 홈, 운동, 가계부, 식단, 일본어, 자기계발, 통합 달력, 통합 설정과 각 하위 화면에서 같은 컴포넌트를 사용한다. 로그인/PIN 잠금 화면에는 공통 메뉴가 표시되지 않는다.

- 문서를 300px 초과해 내리면 TOP 버튼 하나를 표시하고 맨 위로 이동하면 제거한다.
- 버튼 크기는 64×48px이며 접근성 이름은 ‘맨 위로 이동’이다. 포커스 표시와 키보드 실행을 지원한다.
- 하단 메뉴·앱 설치 버튼과 간격을 두고 안전 영역을 반영한다. 767px 이하에서는 하단 여백을 더 확보한다.
- ‘전체’ 메뉴가 열리면 TOP을 제거한다. 모달이 열렸을 때 숨기는 CSS도 추가했다.
- 스크롤 리스너는 passive와 requestAnimationFrame을 사용하고, 경로 변경·해제 시 리스너와 예약 프레임을 정리한다. OS 동작 줄이기 설정에서는 즉시 이동하고 등장 애니메이션을 생략한다.
- 일본어의 기존 TOP 표시를 제거해 중복되지 않게 했다.

사용자가 첨부한 약 116초 영상은 Edge의 iPhone 16 Pro Max 440×956px 에뮬레이션이다. 변경 전 일본어 TOP이 하단 ‘전체’ 메뉴와 겹치는 장면, 휴대폰에서 과정 카드가 좁은 3열로 표시되는 장면을 확인했다. 과정 카드는 767px 이하에서 한 열로 표시하도록 개선했다. 이 영상은 수정 후 320·390px 실사용 검증의 증거로 사용하지 않는다.

처음 첨부된 Vercel 환경변수 화면에는 GEMINI_API_KEY와 Google Calendar 설정, Supabase 공개 설정이 보이며 GOOGLE_TTS_API_KEY는 보이지 않았다. 비밀 값은 읽거나 문서에 기록하지 않았다. 직전 앱 프리뷰의 음성 요청은 2026-09-07 09:54:48 UTC에 HTTP 503 및 `[japanese-tts] configuration_missing`으로 확인됐다. TOP 검증 당시에는 음성 요청을 다시 실행하지 않았다. 이후 키 등록 및 성공 재생은 아래 추가 검증에 기록했다.

## 코드 검사와 배포

| 변경 | 로컬 커밋 | GitHub 커밋 | 동일 트리 |
| --- | --- | --- | --- |
| 공통 TOP·휴대폰 과정 카드 | f0781b1542f078b68b8671d44b7362d9873c7620 | 51b2e69cbbfd475dd90a144f002d97fa0a0d7c43 | c2e2a1ec8bdea11138faa9472550cf3ac75224a0 |
| 운동 화면 반복 상태 갱신 수정 | a2df613d2cb8309e3c5d3a14a5321abf7f1db0b1 | eea67513077c882209e3469bddd14f2cb653f802 | 260a3e13fd65186d81da7686b32b8e584e3cf7a2 |

- 공통 TOP 변경: 전체 ESLint, 145개 기존 테스트, TypeScript와 Next.js 운영 빌드 통과. 45개 정적 경로 생성.
- 운동 화면 수정: 변경 파일 ESLint, TypeScript와 운영 빌드 통과. 45개 정적 경로 생성. 기존 테스트 전체를 다시 실행하거나 변경 내용을 그대로 검사하는 테스트는 추가하지 않았다.
- React 검토: 한 번만 표시하는 소유권, 스크롤 이벤트 정리, 숨겨진 버튼의 키보드 접근 방지, 기본 배열의 참조 안정성, 선택 배열의 불변 갱신을 확인했다.
- 공통 TOP 프리뷰 `dpl_HKLvg73JvYUs9ThuVYQjUT8gJZEa`: READY, Vercel success.
- 최종 앱 코드 프리뷰 `dpl_AMr7SyGei71FLzn5zAM3GULrEJoo`: READY, Vercel success.

## 공통 TOP 실사용 결과

`51b2e69`의 로그인된 허가 계정으로 1363×936px 브라우저에서 아래 화면을 실제 이동·스크롤·클릭했다. 아래 수치는 문서의 scrollY이며 단위는 px다. 데이터 저장·삭제 버튼은 실행하지 않았다.

| 화면 | 경로 | 내린 위치 | TOP 표시 | TOP 실행 후 |
| --- | --- | ---: | --- | --- |
| 일본어 홈 | /language | 609 | 1개 | 0, 버튼 제거 |
| 통합 홈 | / | 630 | 1개 | 0, 버튼 제거 |
| 운동 홈 | /fitness | 238 (최대) | 없음 | 300px 이하이므로 실행 대상 없음 |
| 운동하기 | /fitness | 800 | 1개 | 0, 버튼 제거 |
| 가계부 홈 | /budget | 800 | 1개 | Enter 실행 후 0, 버튼 제거 |
| 식단 | /diet | 800 | 1개 | 0, 버튼 제거 |
| 자기계발 | /growth | 609 | 1개 | 0, 버튼 제거 |
| 통합 달력 | /calendar | 89 (최대) | 없음 | 300px 이하이므로 실행 대상 없음 |
| 통합 설정 | /settings | 800 | 1개 | 1 이하, 버튼 제거 |

- 표시된 TOP의 영역은 x=1279~1343, y=792~840이며 하단 메뉴·앱 설치 버튼과 겹치지 않았다. 일본어 화면은 스크린샷으로 배치도 확인했다.
- 일본어에서 ‘전체’ 메뉴 열기 → TOP 0개 → 메뉴 닫기 → TOP 1개 복원을 확인했다.
- 통합 설정을 스크롤한 상태로 새로고침했을 때 브라우저가 y=665로 복원했고 TOP은 표시됐다. 이후 실제 클릭으로 위쪽으로 이동했다.
- 달력 설정도 실제 진입했고 y=0에서 TOP이 없었다. 이 계정의 데스크톱 달력은 짧아 300px 초과 상태의 TOP 클릭은 검증하지 않았다.
- 확인한 화면의 문서 폭은 1359px로 가로 넘침이 없었다. 모바일 분기나 OS 동작 줄이기를 데스크톱 결과로 대신 보고하지 않는다.

## 실사용 중 발견한 운동 화면 전환 오류

폼롤러 부위 기록이 없는 운동하기 화면에서 공통 메뉴의 가계부·일본어를 눌러도 화면 이동이 대기하는 현상을 재현했다. 가계부에 직접 주소로 들어가면 정상 로드됐고, 운동 홈 탭으로 바꾸면 대기 중인 이동이 실행됐다.

DayView의 `foamRollerAreas = []`가 렌더링마다 새 배열을 만들고, 이를 의존성으로 사용하는 effect가 다시 상태를 갱신하는 반복을 확인했다. 모듈 범위의 고정 빈 배열을 기본값으로 사용하도록 수정했다.

최종 앱 코드 `eea6751`에서 다음을 실제 조작했다.

- 운동하기 진입 → 폼롤러 ‘종아리’ 선택 → ‘운동 후’ 선택 후 두 선택 표시가 유지됐다. 이후 종아리를 해제하고 시점을 운동 전으로 돌렸다. 완료 저장은 실행하지 않았다.
- 운동하기 y=742에서 TOP 1개 → 클릭 후 y=0, 버튼 0개. 메뉴·설치 버튼과 겹침 없음.
- 운동하기 → 하단 가계부 클릭으로 /budget 진입 성공.
- 가계부 → 운동하기 → 새로고침으로 운동하기 복원 → 하단 언어 클릭으로 /language 진입 성공.
- 수정 배포 생성 이후 앱 출처의 새 콘솔 오류 없음. 확장 기능의 메타데이터 전송 오류는 앱 오류와 구분했다.
- 해당 프리뷰의 2026-09-07 10:20:25~10:30:25 UTC 런타임 error/fatal 조회 결과 없음. 클라이언트 상태 반복 문제의 부재를 서버 로그만으로 판정하지 않고 위 이동 결과로 확인했다.

## 남은 확인과 운영 조건

- 현재 브라우저는 뷰포트/기기 에뮬레이션 및 OS 동작 줄이기 설정 변경 기능을 제공하지 않는다. 수정 후 320·390px에서 TOP·하단 메뉴·앱 설치 버튼 간격, 일본어 상단 설정 메뉴와 한 열 과정 카드, 초기화 확인 양식의 잘림 여부는 미검증이다. 사용자의 440px 영상은 변경 전 참고 자료다.
- ‘전체’ 메뉴 외 다른 모달의 TOP 숨김과 OS 동작 줄이기 분기는 코드 검토 범위다.
- GOOGLE_TTS_API_KEY는 사용자가 Preview와 Production에 등록했고, 아래 프리뷰 음성 검증을 통과했다. 운영에서의 최종 음성 확인은 PR 병합 후 진행한다.
- 저장소 AGENTS.md의 실사용 검증 요구와 기존 PR #186의 릴리스 조건에 따라 작은 화면 검증 완료 전 Draft를 유지한다. 이번 PR의 운영 병합/배포는 실행하지 않았다.

## 음성 키 등록 후 추가 실사용 검증 — 11:00 UTC

사용자의 후속 환경변수 캡처에서 GOOGLE_TTS_API_KEY가 Preview와 Production에 각각 등록된 것을 확인했다. 값은 열람하거나 복사하지 않았다. 사용자가 생성한 배포는 기존 main 커밋의 운영 재배포였으므로, 검증 중인 PR에도 새 키가 반영되도록 코드 변경 없는 재배포 커밋을 추가했다.

- 로컬 `75b5a4e1a5a1ca351be4b8ae519dd526da0cbe47` → 원격 `893bba3255a752a27607c0462507a783bc75c6ff`. 기존 문서 커밋과 동일 트리 `010051d5ae96d34242eda95885e17d7ab326b55f`.
- 프리뷰 `dpl_Fe8PRH1pjyredDLu2nZe173PptEb`: READY, Vercel success.
- 로그인한 `/language/start`의 첫 글자 あ에서 ‘소리 듣기’를 실제 클릭했다. 버튼 비활성화 및 ‘소리 멈추기’ 표시 후 재생이 끝나고 듣기 버튼이 다시 활성화됐다. 오류 안내 없음.
- 새로고침 후 다시 ‘소리 듣기’를 클릭했다. 10:59:56.971 UTC에 재생 중 버튼 비활성, 11:00:00.349 UTC에 버튼 재활성화를 관측했다. 이 간격에는 서버 응답 대기와 브라우저 재생이 모두 포함된다.
- Vercel 로그의 POST `/api/language/tts`는 10:58:51, 10:59:56 UTC 두 건 모두 HTTP 200, cache MISS였다. 실제 음성 생성 응답 성공과 UI 재생 완료를 함께 확인했다.
- 10:58:30~11:00:52 UTC의 해당 배포 런타임 error/fatal 없음. 같은 검증 구간의 앱 출처 새 콘솔 오류 없음.
- 홈으로 돌아와 완료 수업 0/60, 오늘 자유 학습 0/5를 확인했다. 수업 완료 저장이나 기록 초기화는 실행하지 않았다.
- 이 결과는 연결된 Chrome 브라우저의 재생 흐름 검증이다. 실제 iPhone 스피커 출력이나 음질을 청취 평가한 결과는 아니다. 수정 후 320·390px 확인은 계속 남아 있다.

고정 프리뷰: https://ai-fitness-app-git-feat-japanese-begi-e27f4e-jace3695s-projects.vercel.app/language

이전 학습·초기화·캐릭터 검증은 `app-record-reset-verification.md`와 `yeoni-mascot-verification.md`에서 별도로 확인한다.
