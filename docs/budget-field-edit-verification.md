# 가계부 금액 검색·항목 수정 검증

이 문서는 항목 수정 작업 당시의 검증 기록이다. 이후 추가한 결제 예정일·구독 점검과 최신 60/60 브라우저 결과는 [후속 검증 기록](budget-payment-plans-verification.md), 현재 단계는 [전체 진행 기록](free-first-progress.md)을 참고한다.

2026-09-14 통합 실행 순서 5번의 후속 범위다. 앞선 분류·월별 점검에 금액 조건 검색과 지출의 다섯 항목 수정을 추가한다. 원본 PDF 마지막 제안의 5단계와 통합 실행 순서의 5번은 서로 다른 번호다.

## 앱에서 확인하는 순서

1. [가계부 Preview](https://ai-fitness-app-git-fix-app-wide-reliability-jace3695s-projects.vercel.app/budget)에서 **상세 내역**을 연다.
2. 최소·최대 금액이나 지출 결제수단으로 내역을 찾는다. **현재 결과 CSV 저장**도 같은 검색 결과를 사용한다.
3. **지출 수정·분류 기억·변경 이력**을 펼치고 내역을 선택한다. **수정할 항목**과 새 값을 고른다.
4. 변경 버튼을 누르면 내역마다 변경 전후가 나온다. 취소하거나 확인 후 저장한다. **변경 이력**에서 전후 값을 확인하고 되돌릴 수 있다.

수정은 선택한 지출에 실제로 반영된다. 동작만 둘러볼 때는 마지막 확인 화면에서 취소하면 된다.

## 구현

- 상세 내역에서 최소·최대 금액(경계 포함, 같은 값으로 정확한 금액 검색)을 적용한다. 지출 결제수단 필터도 추가했다. 금액 조건은 수입·지출·저축 모두에 적용하고 결제수단은 지출에만 적용한다. 잘못된 금액·뒤집힌 날짜 범위는 오류를 표시하고 결과와 CSV 내보내기를 막는다.
- 검색 조건을 바꾸면 숨겨진 지출 선택을 해제한다. 현재 필터 결과가 CSV에도 적용된다. 기존 CSV의 문자 그대로인 `\\r\\n`·`\\uFEFF`를 실제 개행·BOM으로 고쳤고, 따옴표·개행을 보존하며 수식으로 해석될 수 있는 문자열을 이스케이프한다.
- 지출 1~100건을 선택해 분류 또는 금액·날짜·결제수단·장소·메모 중 **한 항목을 같은 값으로** 변경한다. 금액은 각 지출에 적용한다. 날짜 변경은 월별 집계에 반영된다. 결제수단 변경은 충전·사용 등 거래 유형을 유지하고, 장소 변경은 기존 분류 기억을 자동 이전하지 않는다.
- 확인 대화상자에서 선택한 내역 각각의 변경 전후 값을 볼 수 있다. 확인 전에는 저장하지 않는다. 메모를 비우는 것과 기존 `null` 값은 구별하며 되돌리기에서 원래 값을 복원한다.
- 서버의 원본 행 전체를 확인하고 한 건이라도 바뀌거나 삭제됐으면 전체 변경과 이력을 취소한다. 같은 요청의 응답 유실·새로고침·재전송은 한 번의 변경으로 확인한다. 이미 되돌린 요청은 다시 적용하지 않는다.
- 기존 분류 이력에 수정 항목과 전후 스칼라 값만 추가한다. 전체 거래 원문을 새 서버 이력에 복제하지 않는다. 거래 삭제 시 상세 이력도 제거하며, 삭제된 거래나 이후 변경을 되돌리기로 덮어쓰지 않는다. 기존 분류·기억의 되돌리기도 그대로 검증한다.
- 입력은 저장 후 거래와 이력을 모두 재조회할 때까지 잠근다. 서버 응답 형태가 예상과 다르면 완료로 표시하거나 확인 중인 요청을 지우지 않는다.
- 후속 검토에서 거래 조회 함수가 실패를 삼켜 저장 후 화면을 완료로 표시할 수 있음을 발견했다. 조회 성공 여부를 전달하고 실패 시 수정 입력을 잠그도록 보완했다. 실제 저장 후 거래 GET만 끊고 완료 문구 미표시·입력 잠금·수동 재조회 복구·이력 한 건 유지를 확인하는 브라우저 검사를 추가했다.

## DB 및 비용

마이그레이션 `20260914131417_budget_expense_fields`를 호스팅 DB에 적용했다. 기존 이력 테이블에 열을 추가하고 항목 수정 함수 한 개 및 기존 되돌리기 함수를 확장했다. 기존 거래를 변환하는 데이터 마이그레이션은 없다. 소유자 RLS 3개를 유지하며 두 함수의 SECURITY INVOKER·anon 실행 불가를 확인했다. [Supabase 함수 안내](https://supabase.com/docs/guides/database/functions).

적용 전후 지출 36건·앱 상태 2행·언어 상태 2행의 전체 행 해시가 모두 동일했다. 수입·저축 각각 0건 유지. 보안 진단의 새 항목은 없고 기존 [유출 비밀번호 보호 설정 경고](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection)만 유지됐다.

추가 유료 API·요금제·서버를 사용하지 않는다. 음식 사진과 네 분야 무료 Gemini 조언 정책을 유지하고 Zephyr 재연결은 무료 조건 확인까지 대기한다. 대표 개인 기록을 이용한 쓰기 테스트는 하지 않는다.

## 검증 상태

- 단위/실제 격리 DB 304개, 동기화 VM 25개, lint·TypeScript 통과.
- 새 브라우저 시나리오: 금액·결제수단 필터→실제 CSV 다운로드→선택 초기화→금액 변경 취소/저장→새로고침→되돌리기; 날짜·결제수단·장소·빈 메모의 저장/재조회/원본 복원; 응답 유실 복구와 독립된 두 로그인 세션의 충돌·되돌리기 거절. Chromium/WebKit 및 320px 화면에 추가했다.
- 최종 코드 `c0c409b29337050e2dd4ecb156b2740ba62c3c22`의 실제 격리 브라우저 54/54, 프로덕션 빌드와 같은 코드의 Preview 배포 확인을 완료했다. 앞선 실패와 수정 근거는 아래에 보존한다.
- 호스팅 테스트 계정의 전체 저장 흐름은 이전 닉네임 최초 설정 제한으로 미검증이다. 격리 Auth·PostgREST·Postgres의 브라우저 검증과 구별한다. 실제 iPhone 검사는 마지막에 한다.

## 원래 단계 판정

1번 부분 완료(Zephyr 무료 조건 남음), 2번 Preview 완료, 3번 대기, 4번 미구현, **5번 부분 완료**를 유지한다. 이번 일괄 수정은 지출의 위 여섯 항목이며 수입·저축의 일괄 수정은 포함하지 않는다. 결제 예정일·선제 알림·미사용 구독 후보 판단·증가 원인의 구체적 설명도 남아 있다. 6~9번 및 실제 iPhone·운영 반영을 이번 변경으로 완료 처리하지 않는다. PR #189는 Draft·미병합이다.

## 최초 브라우저 실행과 수정

코드 `009b4b38ad015a9b8b839e10a3950923ff60074e`의 [Actions 34848329379](https://github.com/jace3695/AI-fitness-app/actions/runs/34848329379), job `103989624462`는 52/54 통과였다. 두 브라우저 모두 금액 사례의 CSV 다운로드 이후 `유형` 선택 메뉴를 정확한 접근성 이름으로 찾지 못해 금액 변경 전에 멈췄다. 실제 날짜·결제수단·장소·빈 메모의 저장/복원과 응답 유실·두 세션 충돌은 양쪽 브라우저에서 통과했다.

선택 메뉴에 `내역 유형` 등 명시적인 접근성 이름을 추가했다. 대기 시간을 늘리거나 검사를 생략하지 않았다. 별도 코드 검토에서 발견한 저장 후 거래 재조회 실패 처리도 위와 같이 보완해 재검증한다. 최초 실행은 54개 정리 보고, 일회용 계정 63개 제거, 관련 행 잔여 0, 외부 origin 요청 0으로 종료했다. 같은 코드의 Preview `dpl_EJgPrNsUcofRkvA8UasrRzhNmyGE`는 READY이며, 이 배포 확인을 전체 브라우저 성공으로 대신하지 않는다.

두 번째 코드 `c96cded30c5c9ee37e0e4c7038b88c6c3f4f4f32`의 [Actions 34849657186](https://github.com/jace3695/AI-fitness-app/actions/runs/34849657186), job `103994085138`도 52/54였다. 접근성 이름 문제는 해소됐고, 이번에는 의도한 조회 실패 문구를 찾지 못했다. 설치된 Supabase SDK의 GET 자동 재시도 코드를 확인하니 한 번만 끊은 요청이 재시도로 복구되어, 지속 실패라는 검사 전제가 성립하지 않았다.

앱의 자동 복구는 유지하고, 테스트에서 수동 복구 단계 전까지 거래 GET의 SDK 재시도도 모두 끊도록 보정했다. 오류 안내·완료 미표시·입력 잠금·복구 후 이력 한 건 검사는 그대로 유지하고, 실제 재시도 요청도 발생했는지 확인한다. 두 번째 실행도 54개 정리 보고·계정 63개 제거·잔여 행 0·외부 origin 요청 0으로 종료했다. 두 번째 Preview `dpl_A6CsE9FeWnPX1hs9jXkwmSHgNnvy`도 같은 코드로 READY였다. 세 번째 검증은 기능 코드를 바꾸지 않고 이 오류 주입과 문서를 보완한다.

## 최종 결과 — 2026-09-14

검증 기준은 `c0c409b29337050e2dd4ecb156b2740ba62c3c22`다. 앱 기능 코드는 직전 `c96cded30c5c9ee37e0e4c7038b88c6c3f4f4f32`와 같고 오류 주입 검증을 보완했다. 이후 결과 정리 커밋은 문서만 바꾼다.

| 항목 | 확인 결과 |
|---|---|
| [Actions 34850985127](https://github.com/jace3695/AI-fitness-app/actions/runs/34850985127), job `103998572731` | 전체 성공. 단위/DB 304개, 동기화 VM 25개, lint·TypeScript·격리 프로덕션 빌드 통과 |
| 실제 인증 후 브라우저 | Chromium/WebKit 54/54 통과. 가계부 6개 시나리오를 두 브라우저에서 각각 검증했고, 기존 공통 기능 회귀 검사도 통과 |
| 금액 수정·복구 | 실제 CSV 결과 일치, 필터 변경 시 선택 해제, 변경 전후·취소·저장, 조회의 SDK 재시도까지 실패할 때 오류·입력 잠금, 수동 재조회 후 중복 이력 없이 복구, 새로고침·원본 되돌리기 통과 |
| 다른 항목·충돌 | 날짜·결제수단·장소·빈 메모 수정 및 원래 null 복원, 응답 유실 후 같은 요청 재조회, 독립된 두 세션의 덮어쓰기·되돌리기 거절, 320px 화면 통과 |
| 검증 정리 | 54개 정리 보고, 일회용 계정 63개 제거, 관련 행 잔여 0, 외부 origin 요청 0. 일회용 DB·볼륨·키 제거 단계 성공 |
| Vercel Preview | `dpl_JE6LQ85MpNmKoZLGG6hywXf2LQCd` READY, `target: null`, 검증 SHA 일치 |
| 원본 보존 | 호스팅 마이그레이션 전후 지출 36건·앱 상태 2행·언어 상태 2행의 전체 행 해시 동일, 수입·저축 각각 0건 유지. 실제 기록 쓰기 검사는 격리 환경에서만 수행 |

이번 검색·지출 항목 수정 범위는 구현과 격리 브라우저 검증·Preview 배포 확인을 완료했다. **통합 실행 순서 5번 전체는 부분 완료**다. 호스팅 계정의 전체 핵심 흐름, 실제 iPhone 확인과 운영 앱 반영은 완료로 표시하지 않는다. PR #189는 open·Draft·미병합으로 유지한다.
