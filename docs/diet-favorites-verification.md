# 식사 즐겨찾기 — 2026-09-17

## 범위

식단 화면에서 점심/저녁의 현재 입력 구성을 이름으로 저장하고, 내용 확인 후 다시 입력칸에 불러온다. 저장 대상은 식품 단백질(미기록과 0g 구분), 밥 이름·조리 후 무게, 점심 보충 단백질뿐이다. 날짜·완료·물·시각·상태·메모·다른 끼니는 복사하지 않는다. 적용할 때 현재 점심의 식사 평가도 유지한다. 수치는 불러온 뒤 직접 입력 형식으로 표시된다.

- 즐겨찾기 저장/삭제는 확인 후 별도 테이블에서만 실행한다. 오늘 식단 저장은 기존 버튼으로 별도 실행한다.
- 같은 계정·끼니·이름은 중복되지 않으며 기존 구성을 덮어쓰지 않는다. 수정이 필요하면 다른 이름으로 저장하거나 기존 항목을 삭제 후 새로 저장한다.
- 저장/삭제 응답 유실 시 GET으로 결과를 확인한다. 확인이 실패하면 불확실 상태를 유지하며 재확인 버튼은 GET만 실행한다. 임의 재삽입·반복 삭제가 없다.
- 저장 행은 인증 클라이언트가 수정할 수 없다. 삭제에는 소유자·ID와 조회한 생성 시각 조건을 적용한다. 기존 날짜의 식단 기록을 삭제하지 않는다.
- 최근 100개를 표시한다. 초과 시 안내하며 항목을 정리하면 이전 항목을 확인할 수 있다.
- 무료 자체 기능이며 AI 호출·운영 반영은 없다. 실제 iPhone은 마지막 단계다.

## DB와 기본 검증

- migration `20260917114328_diet_meal_favorites.sql`: 소유자 RLS, 최소 select/insert/delete 권한, 입력 제약과 이름 중복 제한. 기존 테이블·행을 변경하거나 새 식단을 채우지 않는다.
- 새 단위 6개·DB 5개 포함 전체 **568/568**, 동기화 **27/27**, lint·타입 통과.
- 호스팅 DB 적용 완료. 적용 전후 테스트 계정 기존 26개 테이블 행 수·전체 내용 해시 일치, 새 즐겨찾기 0건.
- 실제 Preview 저장·불러오기·삭제·재조회와 원본 보존, 최종 브라우저 검증 완료. 아래 결과 참조.

## 브라우저 범위

격리 실제 Auth/DB와 Chromium·작은 WebKit에서 320px 확인·취소·저장·새로고침·입력 적용·하루 저장·즐겨찾기 삭제 후 하루 기록 보존, 응답 유실 후 GET 복구·중복 이름, 다른 계정 접근·조회 오류 안내·저녁 적용을 확인한다.

## 실제 Preview

- 코드 `51f950bf56a5d58ac2aabdf0779d0a107a1c13f3`, tree `2ed2a2b7bfbbfa0d1bb31d386f39adda8081ed5b`; Preview `dpl_65H2ydJRz4oGC9fFWtpgiKiwvkns` READY.
- DB 적용 전후 Security Advisor의 기존 INFO/WARN 종류가 동일하며 새 경고가 없다.
- 허가된 테스트 계정에서 ‘검증용 점심 즐겨찾기’를 식품 단백질 30g·밥 0g·보충 단백질 0g으로 확인 후 저장했다. 확인 화면과 취소 시 DB는 0건이었다. 저장 후 1건과 정확한 수치를 확인하고 새로고침 후 재조회했다.
- 불러오기 확인·취소 후 다시 적용해 입력칸의 30g과 미저장 안내를 확인했다. 기존 26개 테이블의 행 수·내용 해시는 그대로였다. 실제 하루 식단은 저장하지 않았다.
- 입력을 원복하고 즐겨찾기 삭제 확인·취소 후 최종 확인 삭제했다. 새로고침과 홈 이동·식단 재진입에서 빈 목록을 확인했다.
- 검증용 즐겨찾기 1건 삭제 후 새 테이블을 포함한 **27개 테이블의 행 수와 전체 내용 해시가 시작 전과 일치**했다. 합성 일일 기록·실행 이력·파일은 만들지 않았다.
- 삭제 확인 버튼에서 브라우저 제어의 Runtime.evaluate timeout 1회가 발생해 현재 화면을 다시 읽고 같은 버튼을 눌렀다. DB 삭제는 최종 확인 버튼을 누른 뒤에만 진행됐다. 앱 코드를 이 현상 때문에 수정하지 않았다.

## 최종 자동 검증

- [Actions 35218209317](https://github.com/jace3695/AI-fitness-app/actions/runs/35218209317), 코드 `51f950bf56a5d58ac2aabdf0779d0a107a1c13f3`: 첫 실행에서 **176/176** 통과, 실패 0. 새 즐겨찾기 3개 시나리오가 Chromium·작은 WebKit에서 모두 통과(**6/6**).
- 단위/DB **568/568**, 동기화 **27/27**, lint·타입·격리 프로덕션 빌드 통과.
- 즐겨찾기 확인·취소·새로고침·불러오기·명시적 하루 저장·삭제 후 하루 기록 보존, 응답 유실의 GET 전용 복구, 이름 중복 보호, 다른 계정 격리, 조회 오류와 빈 목록 구분을 실제 격리 Auth/DB로 확인했다.
- 176개 cleanup 보고에서 임시 계정 **213개 제거**, 잔여 추적 행 **0**, 차단된 외부 origin **0**. 임시 Supabase 컨테이너·볼륨·키 정리 단계 성공.
- 기능 코드 이후에는 이 검증 문서와 진행 현황만 갱신한다. Draft PR #189 유지, 운영 병합·배포와 실제 iPhone 최종 검증은 진행하지 않았다.
