# 공통 연이 안내와 동작 비용 개선 — 2026-09-07

> 후속 검증: 첨부 영상 검토 후 공통 디자인과 320·390px 실제 앱 화면 검증을 완료했다. 아래 기록은 최초 검증 당시 상태이며, 작은 화면 미검증과 그에 따른 보류는 [공통 디자인 후속 검증](./yeoni-design-unification-verification.md)으로 보완했다. 실기기 온도·OS 동작 줄이기 전환 등의 한계는 그대로 남는다.

## 변경 목적과 범위

일본어에서 사용하던 고양이 연이를 홈·운동·식단·가계부·자기계발·달력과 설정 화면에 재사용한다. 안내는 기존 화면 데이터 또는 정해진 문구를 사용하므로 별도 AI 요청이나 데이터베이스 변경이 없다. 그림 연습은 추가하지 않는다.

- 공통 말풍선: 앱 첫 화면은 짧은 인사/현황, 입력·운동·연습·초기화 화면은 작은 정지 안내.
- 통합 설정과 일본어 설정에서 캐릭터 표시, 절약(기본), 홈 반복, 모든 움직임 끄기를 즉시 변경한다.
- 설정은 `yeoniAppearanceSettingsV1`로 이 기기에 저장한다. 같은 출처의 다른 탭에도 storage 이벤트로 반영한다. 개인 기록 초기화 대상에는 포함하지 않는다.
- 새 설정이 없으면 기존 일본어의 숨김·정지 선택을 초기값에 반영한다. 기존 학습 설정의 필드는 이전 버전/클라우드 호환을 위해 보존한다.

## 비용을 줄인 부분

- 기본 동작은 유한한 CSS 애니메이션이다. 홈 반복은 사용자가 선택할 때만 활성화한다. 기존 138,256-byte WebP를 재사용하고 새 이미지·라이브러리·3D 렌더러는 추가하지 않는다.
- 화면 밖, 숨겨진 문서, 입력 포커스, 열린 모달에서는 캐릭터 재생을 정지한다. OS 동작 줄이기도 존중한다.
- 장식용 JavaScript 반복 타이머를 추가하지 않았다. 페이지 상태 이벤트는 공통 구독으로 묶고, 정지 상태에서는 캐릭터의 화면 관찰자를 설치하지 않는다.
- 자기계발 경과 시간은 작은 자식 컴포넌트만 갱신한다. 루틴 목록과 주·월간 통계를 매초 다시 렌더링하지 않는다. 문서가 숨겨지면 표시용 타이머를 정리하고, 다시 보이면 실제 시각으로 따라잡는다. 저장 시간도 시작·종료 시각으로 계산한다.
- 767px 이하 고정 메뉴의 backdrop blur를 불투명 배경으로 바꿔 스크롤 중 필터 합성 비용을 줄인다.

물리 기기의 온도·전력·배터리 소모를 측정한 결과는 아니다. 발열이 없어진다고 보장하거나 개선율을 추정하지 않는다.

## 검증

- 단위 테스트 148/148 통과: 기존 145개와 설정 손상·이전 선택 보존·정지 우선순위 회귀 3개.
- ESLint, TypeScript 검사 통과.
- 로컬 Next.js 프로덕션 빌드 성공, 45개 정적 경로 생성.
- React 검토: 구독과 관찰자 정리, SSR 스냅샷, 안내 텍스트 유지, 기존 채점/클라우드 로직 보존, 시간 표시의 상태 분리, 입력 접근성.
- 프리뷰 인증 후 데스크톱 실제 조작을 아래와 같이 완료했다. 작은 화면은 미검증으로 남겨 둔다.

## 실사용 검증 기록

대상 앱 코드: 원격 `f93b15690d2486c8e01f949bd6baa2955f2781de`, 로컬 `c51a70a714ff37f074d01f44f3646a94714528ac`. 두 커밋은 동일한 트리 `042327a85314cf69aad68581c63bdf7c861d5897`다. 이후 검증 문서 커밋은 앱 코드를 바꾸지 않는다.

- [PR #187](https://github.com/jace3695/AI-fitness-app/pull/187): draft.
- [프리뷰](https://ai-fitness-app-git-feat-yeoni-hub-guide-jace3695s-projects.vercel.app): 배포 `dpl_3TAWQUKZ1YdV9kSPpAFpSRYSpDpd`, READY, GitHub Vercel 체크 success.
- 2026-09-07 12:34–12:44 UTC, 로그인된 검증 계정, 브라우저 1363×936px. 로그인 게이트 확인과 인증 후 조작을 구분했다.

| 흐름 | 실제 확인 |
| --- | --- |
| 홈 | 기존 고양이 이미지 로드, 말풍선 66.8px 높이/캐릭터 88px, 기본 CSS 반복 횟수 1 |
| 공통 설정 저장 | 홈 반복 선택 → 저장 안내 → 새로고침 후 `home` 유지 |
| 앱·탭 간 적용 | 별도 탭의 일본어 홈에서 숨김 즉시 반영, 캐릭터 0개와 안내 문장 유지 |
| 전체 정지 | 공통 설정에서 off → 일본어 `animation-name: none` → 새로고침 후 off 유지 |
| 입력 정지 | 홈 캐릭터가 화면 안(상단 216px, 하단 304px)에 있는 상태에서 입력 포커스 → paused, 입력 종료 → 재개 |
| 스크롤 | 홈 스크롤 650px에서 캐릭터가 화면 밖으로 나가 정지, TOP으로 상단 복귀 |
| 운동 | 홈 안내 표시, 운동하기 전환 시 52px 정지 캐릭터와 운동 안내 |
| 식단·가계부 | 홈 안내 표시, 가계부 기록 메뉴 전환 시 정지 안내 |
| 자기계발 | 로딩 후 현황 문구, 루틴 시작 시 정지 안내, 경과 시간 00:00 → 00:13 증가 |
| 연습·자료·코칭 | 타자·손글씨·내 자료·주간 코칭에 작은 정지 안내 |
| 달력 | 날짜 상세 열기·닫기, 모달이 열린 동안 캐릭터 CSS paused |
| 설정 | 통합 설정·일본어 설정의 공통 제어와 운동·달력 기록 관리 안내 확인 |
| 가로 넘침 | 확인한 앱·설정·연습 화면 모두 데스크톱에서 없음 |

검증 후 캐릭터 표시 켜짐/절약(`reactions`)으로 복원하고 일본어 설정의 새로고침으로 재조회했다. 채팅은 전송하지 않았고 루틴 타이머는 저장하지 않고 나왔다. 개인 학습·운동·루틴 기록의 신규 생성·초기화 검증을 실행한 것으로 보고하지 않는다.

현재 프리뷰 출처의 브라우저 오류·경고는 발견하지 않았다. 확장 프로그램 오류와 이전 배포/Vercel 로그인 페이지 로그는 구분했다. 프리뷰의 12:24 UTC 이후 런타임 error/fatal 및 5xx 검색에서 해당 로그가 없었다. 탐색 중 브라우저의 `ERR_ABORTED` 한 번은 같은 화면의 가계부 링크로 다시 이동해 성공했으며 앱 콘솔 오류가 동반되지 않았다.

## 남은 검증과 운영 배포

- 현재 브라우저가 뷰포트 변경·기기 에뮬레이션·OS 동작 줄이기 변경 기능을 제공하지 않는다. 새 공통 안내/설정의 320·390px 실제 화면, OS 설정 적용은 미검증이다. CSS 분기 검토를 실기기 검증으로 보고하지 않는다.
- 다른 탭을 조작해도 기존 문서가 `hidden: false`이므로 문서 숨김에 따른 캐릭터/타이머 정지는 직접 실검증하지 못했다. 입력·스크롤·모달 정지는 직접 확인했다.
- 저장 공간 부족 안내는 예외 경로 코드 검토 범위다. 브라우저 저장 실패를 임의로 주입하지 않았다.
- 기기 온도·전력·CPU/GPU 사용량은 측정하지 않았다. 추가 장식 타이머 없음, 기본 유한 애니메이션, 상태 분리 등의 코드 근거와 실제 UI 상태만 보고한다.

저장소 `AGENTS.md`가 작은 화면을 포함한 실사용 검증 없이 운영 병합·배포하는 것을 금지하므로 아직 병합·운영 배포하지 않았다. 휴대폰에서 프리뷰 홈·통합 설정·각 앱을 열고 말풍선/메뉴 잘림, 설정 변경, 스크롤·TOP 이동을 확인한 화면 또는 영상이 필요하다. 확인을 마치면 기존 승인 범위에서 병합·운영 배포 및 최종 검증을 이어간다.
