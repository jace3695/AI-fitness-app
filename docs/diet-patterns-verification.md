# 식단 28일 기록 비교 검증 — 2026-09-17

## 범위

기존 식단 7일 요약 아래에 최근 28일과 이전 28일의 소화 불편·야식 응답을 비교한다. 오늘은 제외하고 어제까지의 서로 겹치지 않는 기간을 사용한다. 날짜별 근거를 펼칠 수 있다.

- 소화 상태가 편안함·속쓰림·더부룩함·메스꺼움으로 저장된 날만 소화 분모에 포함한다. 편안함 외 세 응답을 소화 불편으로 집계한다.
- 야식의 예·아니요만 야식 분모에 포함한다. 미응답과 잘못된 값은 지표별로 제외한다.
- 각 기간의 해당 지표 응답이 7일 이상일 때만 비율 차이를 %p로 표시한다. 이는 화면의 최소 기록 기준이며 통계적 유의성이나 건강 상태 판정 기준이 아니다.
- 날짜별 목록에는 기간 안의 저장 행을 표시하고 미응답을 명시한다. 기록이 없으면 0%로 표시하지 않는다.
- 원인 추정·식단 계획 변경·AI 호출·DB 구조나 저장 경로 변경이 없다. 입력 중인 오늘 값은 비교에 반영하지 않는다.

## 기본 검사

로컬 단위/DB 556/556 및 추가 한국 자정 검사 통과, 동기화 27/27, lint·타입 검사 통과. CI 전체 단위/DB는 557/557이다. 새 단위 5개는 기간 양 끝·오늘/미래 제외·윤년·미응답·서로 다른 분모·7일 기준·원본 보존을 검증한다.

브라우저 검사는 합성 계정의 저장 자료로 320px 집계·날짜별 근거·입력과 비교의 분리·새로고침·화면 이동·원본 보존 및 기록 없는 경우를 검사한다. Preview 실제 조회와 최종 CI 결과는 아래에 구분해 기록했다.

## Preview 실제 조회

- 기능 커밋 `cfaf0b22d576aeb11566a37562d11ad8a6b03c8d`, tree `81e87003a73fd7af05f975983f14d843b857aa07`. Preview `dpl_AxumJz4wSxz9Y5QiiWrRSJxZFkKS` READY.
- 허가된 테스트 계정으로 식단 화면의 최근 2026-08-20~09-16, 이전 2026-07-23~08-19 기간과 응답 0일·비율 미기록을 확인했다. 해당 계정에는 두 기간의 응답이 없다.
- 날짜별 근거 펼침, 오늘 소화 상태의 미저장 입력과 과거 비교 분리, 입력 원복 후 새로고침, 홈 이동 후 식단 재진입을 확인했다. 서버에 새 식단을 저장하거나 과거 식단을 만들지 않았다. 값이 있는 비교는 격리 실제 Auth/DB/브라우저 검사로 구분한다.
- 조회 전후 26개 테이블 행 수·전체 내용 해시가 모두 일치했다. 이번 검증에서 호스팅 합성 데이터나 이력을 생성하지 않았다.
- 브라우저 점검 중 홈의 식단 링크가 2곳에 있어 공통 메뉴 안의 링크로 대상을 한정했다. 앱 오류가 아니다.

## 검사 대상 보완

- 첫 CI 실행 중 정적 검토에서 두 기간에 동일한 빈 기록 안내가 존재함을 확인했다. Playwright 검사 대상을 ‘이전 28일’ article 안으로 좁혔다. 기능 코드는 바뀌지 않았다. 시간 제한·재시도·검사 기준은 유지했다.
- 보완 커밋 `4af111319936bdd497ae27cbe756e8ce1ed31060`, tree `68a35e7a7395751f15a1d1eff091a044e3583317`. 첫 실행과 수정본의 결과·정리 상태는 최종 로그로 구분해 기록한다.

- 첫 [CI 35211914511](https://github.com/jace3695/AI-fitness-app/actions/runs/35211914511)는 단위/DB·동기화·lint·타입·독립 빌드를 통과했다. 브라우저 22개 통과·실패 0인 상태에서 수정본으로 교체되어 자동 취소됐다. 전체 통과로 계산하지 않는다. 취소 후 `Remove disposable database and keys`와 합성 증거 업로드 성공, Supabase 컨테이너·볼륨·로컬 키 제거 로그를 확인했다.

## 보완본 첫 전체 결과

- [CI 35212405849, 첫 시도](https://github.com/jace3695/AI-fitness-app/actions/runs/35212405849/attempts/1): 전체 **169/170**, 새 식단 **4/4**(Chromium·작은 WebKit 각각 2개) 통과. 320px 집계·미응답·날짜별 근거·입력 분리·재조회·원본 보존과 빈 기록 안내가 통과했다.
- 기존 `growth owner isolation, expiry and history error recovery are visible without losing records`의 WebKit 사례에서 `page.goto: WebKit encountered an internal error`가 발생했다. `/assistant/quick?command=오늘 타자 연습 완료했어`로 이동하며 `load`를 기다리는 지점이다. 기능 코드와 해당 검사 코드는 이번 단계에서 변경하지 않았다. 원인은 미확정이며 재시도로 숨기지 않고 첫 실패를 기록한다.
- 170개 정리 보고에서 합성 계정 **204개 제거**, 잔여 행 **0**, 외부 origin **0**. 격리 DB·볼륨·키 정리 및 합성 증거 업로드 성공.
- 동일 커밋으로 실패한 워크플로 작업을 재실행했다. 이 저장소는 브라우저 전체가 한 작업이므로 재검증도 전체 범위다. 테스트 기준·자동 재시도 수·시간 제한은 변경하지 않는다.

## 최종 결과

- [CI 35212405849, 재검증](https://github.com/jace3695/AI-fitness-app/actions/runs/35212405849/attempts/2), job `105179331379`: **단위/DB 557/557**, **동기화 27/27**, lint·타입·독립 빌드 통과. 전체 브라우저 **170/170**, 새 식단 **4/4** 통과.
- 기존 자기계발 WebKit 화면 이동 오류는 동일 커밋의 재검증에서 재현되지 않았다. 오류 원인은 미확정이며 최초 실패 기록을 유지한다. 기능 코드나 자동 재시도 수를 변경하지 않았다.
- 170개 정리 보고에서 합성 계정 **205개 제거**, 잔여 행 **0**, 외부 origin **0**을 확인했다. 격리 DB·볼륨·키 제거와 합성 증거 업로드 성공.
- 검사 보완본 Preview `dpl_5L5dDtqJq7fVpqCbQhv3cBDTnda6` READY. 실제 화면의 비교 영역 너비 약 343px(전체 뷰포트 1363px)에서도 가로 넘침이 없었으며, 320px 전체 화면은 양쪽 자동 브라우저에서 검증했다.
- 호스팅 테스트 계정의 **26개 테이블 원본 보존**을 확인했다. 이번 기능은 읽기 전용이므로 호스팅 데이터 생성·저장·되돌리기나 DB 변경이 없다.
- 7단계 개인화는 부분 완료다. 식사 즐겨찾기·운동/식사 시각 연결·다른 장기 추세는 후속이며 실제 iPhone·운영 반영은 마지막 단계로 유지한다.
