# 기록에 따른 전신 서킷 조정

재민님이 매주 운동표를 직접 만들 필요 없이 최근 수행능력, 허리 상태, 피로에 따라 유지·증가·교체·감소를 제안한다. 운동 홈에서 변경 전후를 확인하고 적용 버튼을 눌러야 실제 계획을 바꾼다. 수동 편집은 계속 사용할 수 있다.

## 기본 운동

- 월·수·금: 기존 전신 근력 5종목, 3라운드, 약 25~35분.
- 화·목: 낮은 강도의 전신·코어 회복형 5종목. 1라운드부터 시작하며 안내 범위는 1~2라운드, 약 10~20분이다.
- 주말: 휴식 우선, 토요일은 걷기·가동성 등을 선택할 수 있다.
- 슬라이딩보드는 현재 루틴에서만 제외하며 기존 라이브러리와 기록은 유지한다.

## 제안 기준

아래 횟수·기간은 앱의 보수적인 검토 기준이며 진단이나 개인별 치료 처방이 아니다. 날짜만으로 운동을 바꾸지 않는다.

| 판단 | 근거와 제안 |
| --- | --- |
| 유지 | 자료 부족, 최근 근력 기록 부족, 현재 구성에 적응 중이거나 이번 주 이미 조정한 경우. |
| 증가 | 마지막 적용 이후 같은 근력 루틴·계획량·기록된 저항에서 최근 3회가 여유롭고, 계획 반복수를 완료했으며, 피로가 낮고 허리 불편이 없는 경우. 한 근력 요일의 한 운동만 반복수 1회 증가. |
| 교체 | 약 2주 이상에 걸친 안정적인 근력 수행이 6회 이상 쌓이고 최근 변화 후 수행도 확인된 경우. 한 요일의 한 동작만 보완하고 본운동 5개를 유지. |
| 감소 | 최근 높은 피로, 뻐근함, 미완료·중단 또는 같은 계획·저항 조건에서 수행 저하. 다음 운동 날짜 한 번만 라운드를 줄이고 휴식을 늘림. 이미 1라운드면 휴식 제안. |
| 증상 확인 | 최근 통증·허리 악화·신경 증상 등은 증가와 교체 보류. 증상을 유발하는 운동 중단, 지속·악화 시 의료 평가 안내. 회복형 운동을 자동으로 안전하다고 판단하지 않음. |

운동 비교는 최근 28일, 증가·교체에는 최근 7일 이내의 근력 기록도 필요하다. 통증·신경 증상 검토에는 최근 14일 운동 기록과 최근 7일 컨디션을 사용한다. 미기록·미래·잘못된 날짜와 회복형 운동의 쉬운 수행을 근력 증가 근거로 사용하지 않는다.

## 교체 후보

1. 사이드워크 한 번을 지지형 햄스트링 컬(맨몸, 좌우 8회)로 교체한다.
2. 햄스트링 보완 루틴도 안정적으로 3회 이상 수행했다면 다른 근력일의 버드독을 밴드 팔로프 프레스(약한 밴드, 좌우 6회)로 교체할 수 있다.

새 동작은 전체 2라운드 이하로 도입하며 자세 안내와 지지대·밴드 고정 조건을 확인해야 적용할 수 있다. 데드버그·힙브릿지·슬라이딩보드·중량 힙힌지를 자동으로 재도입하지 않는다.

## 저장과 일관성

- `workoutAdaptiveReview.ts`가 순수 함수로 제안을 계산한다. 생성 자체는 설정을 변경하지 않는다.
- 적용 직전에 저장된 기록·설정·선택 프로그램·현지 날짜를 다시 읽고 미리보기 ID와 비교한다. 오래된 미리보기와 중복 확인을 거부한다.
- 적용한 주에는 추가 증가·교체를 제안하지 않는다. 다음 주에도 마지막 적용 이후 새 근력 기록이 필요하다. 피로와 증상은 이 제한보다 먼저 검사한다.
- 요일별 반복수는 `weeklyExerciseTargets`, 날짜 한 번의 감소는 `dateOverrides`에 저장한다. 전역 목표를 덮어써 회복일에 섞이지 않게 한다.
- 적용 요일의 이전 날짜는 기존 계획을 보존한다. 이미 작성한 기록·날짜별 예외·직접 편집한 일정은 자동 변경 대상에서 제외한다.
- 결정 이력과 계획을 같은 설정 객체에 저장한다. 저장 성공 후 화면 상태를 변경한다. 재접속 시 이력과 적용 내용을 복원한다.
- 직접 편집하는 운동량도 선택한 요일·기간에 적용하며, 좌우 운동 표기를 유지한다.
- 기존 AI 심층 분석은 참고 설명으로 제공한다. 주 5일 서킷의 실제 변경은 운동 홈의 확인 절차로 통일한다.
- 86kg은 사용자 제공 기준값으로 표시한다. 방문 날짜에 새 체중 측정 기록을 만들어 넣지 않는다. 체중 추세는 주 평균과 수행능력·피로를 함께 평가한다.

## 검증 범위

합성 기록으로 네 가지 판단, 증상 보류, 날짜 범위, 준비 조건, 중복·오래된 미리보기 거부, 적용 후 재평가, 날짜·요일별 범위, 이전 날짜 보존, 운동 부위 통계를 검증한다.

2026-09-08 로컬 검증: 전체 자동화 테스트 172개 통과, ESLint 통과, Next.js 프로덕션 빌드 및 타입 검사 통과. 초기 손상된 생성 캐시는 따로 보관하고 재생성했다.

### GitHub 및 검증용 배포

사용자가 GitHub 작업 브랜치 업로드와 미리보기 배포를 명시적으로 승인했다. 연결된 GitHub 앱으로 `feat/five-day-circuit` 브랜치와 draft [PR #188](https://github.com/jace3695/AI-fitness-app/pull/188)을 생성했다. 업로드한 Git 트리와 로컬 검증 트리가 일치함을 확인했다.

- 미리보기: https://ai-fitness-app-git-feat-five-day-circuit-jace3695s-projects.vercel.app/fitness
- 최초 브라우저 검증 코드: `ee1c1426449865738cb5a7655a9912c4b0f1d8d5`, Vercel `dpl_3tbMhevFzKPD727fvo5YzJQ53Qn4`, READY.
- 발견 사항 수정 코드: `213482fd110d1599b7ab5656ae4b87999d129136`, Vercel `dpl_3HwUtiEabm2AmET8cG33Epvc3NLq`, READY.
- 삭제 확인창 개선 코드: `64b16e09ee9147a730de044b99dbb73f8f0c5bf5`, Vercel `dpl_ADSEBzE4ryxTL7b4jDWXfCHivFzx`, READY. GitHub Vercel 상태 성공, 해당 배포의 error/fatal 런타임 로그 없음.
- 운영 `main`은 변경하지 않았고 PR은 병합하지 않았다.

### 로그인 후 실제 브라우저에서 확인한 범위

보안 로그인으로 앱에 진입한 후 기록이 없는 검증용 계정에서 수행했다. 테스트 운동 메모에 `QA-PR188 합성 기록 · 실제 운동 아님`을 명시했다.

| 흐름 | 결과 |
| --- | --- |
| 최초 진입 | 화요일 회복형 서킷, 현재 운동 방향, 기록 부족에 따른 유지 제안을 확인했다. 86kg 기준값과 실제 체중 기록 없음이 구분됐다. |
| 유지 결정 | 유지 확인 → 조정 이력 저장 → 새로고침 → 화면 이동 후 같은 결정 이력 복원을 확인했다. |
| 운동 저장 | 9월 7일 합성 운동을 일부 완료·힘듦·실제 세트/반복 입력으로 저장한 뒤 새로고침 후 달력에서 재조회했다. |
| 감소 미리보기 | 미완료 기록으로 감소 제안을 표시했다. 변경 전에는 기존 화요일 회복 서킷이 유지됐다. |
| 확인 후 적용 | 변경 적용 후 9월 8일만 휴식으로 바뀌었고 새로고침 후 유지됐다. 주간 화면에서 월·수·금 근력과 목요일 회복형 일정은 그대로였다. |
| 중복 감소 방지 | 동일한 미완료 기록으로 다른 날을 연이어 줄이지 않고 확인한 회복 조정을 유지했다. |

실사용 중 회복일 안내에 이전 2라운드 시작 문구가 남아 있고 달력에서는 자세한 허리·신경 증상 입력 및 재조회가 부족한 것을 발견했다. 수정 커밋은 안내를 1라운드 시작/1~2라운드 범위로 맞추고 달력에 허리 상태·신경 증상·불편했던 운동과 세트를 추가했다. 미기록 허리 상태는 그대로 미기록으로 보존한다. 통증 기록에서는 단순 강도 감소 대신 증상 확인 안내를 표시한다. 수정 후에도 자동화 테스트 172개, ESLint, 타입 검사와 프로덕션 빌드는 모두 통과했다.

### 클라우드 브라우저 검증 중단 시점의 범위

이 절은 최초 중단 당시의 이력이다. 현재 완료·미완료 상태는 아래 "데스크톱 후속 검증과 동기화 중복 수정"을 기준으로 한다.

합성 운동 기록을 정리하는 기본 삭제 확인창에서 클라우드 브라우저의 `Input.dispatchMouseEvent`가 시간 초과됐다. 이 확인창은 화면 안에서 삭제 내용과 유지되는 기록을 보여주고 사용자가 확인 또는 취소하는 방식으로 교체했다. 교체 후 자동화 테스트 172개, ESLint, 타입 검사와 프로덕션 빌드를 다시 통과했다.

기존 브라우저 연결을 초기화한 뒤 브라우저 식별자가 새로 발급됐지만, 새 탭 생성과 탭 목록 조회가 계속 `CDP operation refresh tabs timed out`으로 응답하지 않았다. 당시 미리보기는 READY였고 조회한 error/fatal 로그는 없었다. 이 결과는 인증 후 화면·기록 저장의 정상 동작을 증명하지 않는다. 문서화된 브라우저 절차 외의 수단으로 실사용 검증을 대체하지 않았다.

브라우저 제어가 복구되면 다음 항목을 이어서 확인해야 한다.

- 수정한 달력의 허리·신경 증상 저장, 수정, 새로고침 후 재조회와 증상 보류 제안. 화면 안의 삭제 확인·취소도 함께 확인한다.
- 합성 근력 이력을 이용한 증가·교체 제안의 확인 적용, 준비 조건, 이전 날짜 보존. 현재 자동화 테스트만 통과한 상태다.
- 320px/390px 화면에서 실제 카드·확인 버튼·기록 입력 사용성, 콘솔·네트워크 오류. 수정 후 실제 화면 재확인도 남아 있다.
- 다른 기기 또는 독립 세션에서 클라우드 저장·재조회. 같은 브라우저 새로고침을 기기 간 동기화 검증으로 간주하지 않는다.
- 검증용 계정에 남은 9월 7일 합성 기록의 삭제 결과, 9월 8일 테스트 날짜 예외 및 테스트 조정 이력 정리. 실제 사용자 기록은 삭제하지 않는다.

이 항목을 완료하기 전에는 실사용 검증 전체 완료 또는 운영 반영 가능으로 표시하지 않는다. 저장소 AGENTS.md의 실사용 검증 필수 규칙에 따라 PR을 draft로 유지한다.

### 데스크톱 후속 검증과 동기화 중복 수정

2026-09-08 사용자가 데스크톱 검증 결과를 이 대화에 전달했다. 아래 실사용 통과 항목은 전달받은 결과로 기록하며, 클라우드 에이전트가 다시 수행한 결과로 표시하지 않는다. 링크된 `outputs/adaptive-circuit-review-followup.md` 원문은 현재 작업 공간에서 열 수 없었고 공유된 파일 검색에서도 찾지 못했다. 재현용 백업·화면·요청 기록이 있는 원문이 첨부되면 실제 사고 조건과 추가 대조할 수 있다.

| 항목 | 최신 상태 |
| --- | --- |
| 기존 유지·저장·감소 적용과 복원 | 최초 실사용 통과 결과 유지 |
| 허리·신경 증상 저장·수정·재조회·증상 보류 | 사용자 전달 데스크톱 결과: 통과 |
| 증가·교체 적용과 복원, 준비 조건 | 사용자 전달 데스크톱 결과: 통과 |
| 화면 안 삭제 확인·취소 | 사용자 전달 데스크톱 결과: 통과 |
| 320px/390px | 데스크톱에서 확인한 흐름 통과. 모든 화면의 반응형 검증 완료를 뜻하지 않음 |
| 테스트 정리 | 사용자 전달 결과: 합성 기록·테스트 계획·이력 정리, 기존 기록 보존 |
| 백업 복원·동기화 후 운동 상세 중복 | 데스크톱에서 45→90세트 발견. 아래 로컬 재현과 수정 완료, 배포 후 동일 흐름의 실사용 재검증 필요 |
| 독립 세션 동기화, 요청별 네트워크 점검, 과거 날짜 계획 보존 전체 | 미완료 유지 |

#### 재현과 수정

- 기존 `stableState()`는 최상위 일부 객체의 키만 정렬했다. 운동 배열 내부의 운동·세트 객체 필드 순서가 달라지면 동일한 JSON 내용도 변경으로 판정했다.
- JSONB는 객체 키 순서를 보존하지 않는다. 이 차이가 3방향 병합에서 가짜 충돌을 만들고, 기존 배열 합치기가 같은 운동을 두 번 추가할 수 있었다. 참고: https://www.postgresql.org/docs/current/datatype-json.html
- 합성 데이터 3일 × 5종목 × 3세트로 백업 복원과 서버의 객체 키 순서 변경을 재현했다. 수정 전 45세트가 90세트가 되고, 수정 전 운동의 재추가 및 삭제 운동의 복원도 발생했다.
- `cloudSync.ts`의 비교 직렬화에서 모든 깊이의 객체 키를 정렬하도록 수정했다. 원본 저장 데이터는 수정하지 않고 운동·세트 배열의 순서와 반복 개수도 그대로 둔다. 이름이 같다는 이유로 실제 기록을 삭제하거나 합치지 않는다.
- 기존에 이미 저장된 중복을 자동 삭제하는 마이그레이션은 하지 않는다. 현재 테스트 정리는 사용자 전달 결과를 유지하며, 실제 기록을 근거 없이 정리하지 않는다.
- 서로 다른 두 기기에서 같은 운동 내용을 각각 실제로 수정한 충돌 전반을 해결한 변경은 아니다. 이번 수정은 필드 순서 차이로 생기는 잘못된 변경 판단과 그로 인한 중복에 한정한다.

#### 검증 결과와 남은 실사용 절차

`cloudSync.workoutRestore.test.ts`의 신규 5개 검사는 수정 전 모두 실패했고 수정 후 모두 통과했다. 복원 후 45세트 유지, 한쪽 반복수 수정 보존, 5회 연속 병합, 서로 다른 날짜 기록 보존, 배열 순서·실제 반복 보존, 삭제 기록 미복원을 확인했다. 전체 자동화 테스트 177개, ESLint와 Next.js 프로덕션 빌드·타입 검사가 통과했다.

클라우드 브라우저는 재확인 시에도 `CDP operation refresh tabs timed out after 20000ms`로 실패했다. 합성 JSON만 조회하는 읽기 전용 Supabase 질의도 연결 도구의 `Internal error`로 결과를 받지 못했다. 이를 실제 저장·독립 세션·네트워크 검증 통과로 계산하지 않는다. DB 스키마와 실제 사용자 기록은 변경하지 않았다.

수정 미리보기에서 이어서 확인할 범위:

1. 허가된 테스트 계정에서 기존 기록을 백업하고, 재현 사례의 45세트 합성 데이터를 복원한다. 동기화 → 새로고침 → 달력 상세·통계 재조회 후 45세트와 기존 운동 개수가 유지되는지 확인한다. 백업 재복원과 동기화를 반복해도 증가하지 않아야 한다.
2. 반복수 또는 메모를 한 번 수정하고 다시 동기화한다. 수정값을 보존하고 수정 전 운동을 새 항목으로 추가하지 않아야 한다. 기존 날짜의 실제 기록은 보존해야 한다.
3. 다른 브라우저 프로필 등 저장 공간이 독립된 세션에서 같은 테스트 계정으로 로그인해 재조회한다. 양쪽의 서로 다른 날짜 기록 및 삭제·수정 결과도 확인한다. 동일 브라우저 새 탭만으로 독립 세션 통과로 표시하지 않는다.
4. 각 저장·재조회 요청의 성공 여부와 응답을 확인한다. 인증 정보나 실제 개인 기록을 보고서에 노출하지 않는다.
5. 증가·교체 적용 전후의 과거 날짜 계획과 기존 날짜 예외를 대조하고, 합성 기록·테스트 설정만 정리한 뒤 재조회한다.

기존 통과 항목을 유지하고 위 범위가 끝날 때까지 PR #188은 draft로 유지한다.

### 45c7794 데스크톱 내장 브라우저 재검증 (2026-09-08)

이 절이 최신 실검증 상태다. 위의 최초 클라우드 및 데스크톱 통과 이력은 유지한다. 이번에는 실제 내장 브라우저 UI로 복원·수정·적용·삭제했고, 연결된 Supabase의 읽기 전용 조회로 서버 저장값을 대조했다. 브라우저 새로고침과 서버 조회를 독립 세션 검증으로 계산하지 않았다.

- 검증 코드: `45c7794c59b321f7b40c8d1b821e12870a4ab7fc`.
- Vercel 조회에서 `dpl_AnqZMTQZmCejA6FMZPjQznoB3rmV`의 READY 상태, 해당 Git SHA, 브랜치 별칭 연결을 확인했다.
- 실제 조작: 기존 로그인된 브랜치 미리보기. 고정 미리보기는 별도 origin의 독립 저장공간 재조회용으로 열었으나 앱 로그인 단계에 머물렀다.
- 사용자가 현재 계정의 합성 기록 검증을 허가했다. 시작 전 기존 운동 기록과 운동 설정을 로컬 백업하고 서버 JSON 비교 기준을 확보했다. 원본 기록 내용·계정 인증정보는 문서에 싣지 않는다.
- 아래 결과는 이 코드의 실제 실행 결과다. 자동화 177개·린트·빌드·타입 검사 통과는 앞서 기록된 결과를 유지하며 이번 문서 작업에서 재실행하지 않았다.

| 확인 항목 | 결과와 범위 |
| --- | --- |
| 같은 백업 반복 복원 | 9/2·9/4·9/7의 3일 × 5종목 × 3세트 백업을 총 3회 병합 복원했다. 각 회차의 동기화와 새로고침 후 9월 합계가 **45세트·486회·720kg·5종**으로 유지됐다. 첫 두 회차에는 `지금 동기화`도 직접 실행했다. |
| 상세 중복 | 세 번째 복원·동기화·새로고침 후 세 날짜 각각 상세를 펼쳐 **5개 운동 항목, 완료 세트 15개**임을 확인했다. 종목 이름이나 1~3세트 묶음의 중복은 재현되지 않았다. 교체 검증에 추가한 8월 합성 3일은 별도 월이며 위 9월 합계에 포함하지 않았다. |
| 수정값 유지 | 9/7 합성 운동 메모에 `45c7794 수정값 유지 검증`을 추가해 저장했다. 새로고침 후 수정 문구와 45세트·5종목 상세가 유지됐고 서버에서도 수정 메모를 확인했다. 이후 세 번째 동일 백업 복원은 화면 안내대로 해당 날짜 메모를 백업 값으로 되돌리는 별도 동작이다. |
| 증가 후 과거 계획 보존 | 9/9·9/11에 검증용 날짜 예외를 먼저 두어 다음 적용일을 9/14(월)로 만들었다. 스쿼트 8→9회 증가를 확인 적용한 뒤 매주 월요일은 9회, 지난 9/7은 원래 근력 루틴·8회·3라운드·휴식 75초였다. 새로고침 후 날짜를 명시하는 주간 편집 화면과 서버 저장값에서 보존을 확인했다. |
| 교체 후 기존 과거 예외 보존 | 8/24·8/28·8/31 합성 이력을 추가하고 제안 이력을 초기화한 별도 시나리오에서 9/14부터 햄스트링 컬·2라운드 교체를 적용했다. 준비 조건 확인 전 적용 버튼이 비활성인 것도 확인했다. 매주 월요일은 새 루틴·2라운드, 이미 존재하는 9/7 예외는 기존 루틴·스쿼트 8회·3라운드·휴식 75초로 보존됐다. 적용 화면과 서버 JSON으로 대조했다. |
| 다른 날짜 예외 보존 | 증가·교체 두 시나리오 모두 9/9의 2라운드·휴식 90초, 9/11의 2라운드·휴식 105초가 유지됐다. 위 날짜·조건을 확인한 결과이며 모든 과거 날짜/수동 편집 조합 전체를 검증했다는 뜻은 아니다. |
| 합성 기록 삭제와 미복원 | UI에서 합성 표시를 확인한 6일만 삭제했다. 9월 합계가 45→30→15→0으로 감소했고 정리 동기화·새로고침 후에도 0세트였다. 서버에 원래 4개 날짜만 남았으며 삭제한 합성 기록은 재등장하지 않았다. |
| 원본·설정 보존 | 최종 서버의 운동 기록 전체와 운동 설정 전체를 시작 전 JSON 기준과 각각 비교했다. 두 해시가 모두 정확히 일치했다. 날짜 예외·요일 목표·테스트 조정 이력도 원래 상태로 복원했다. |
| 콘솔 | 확인 시점에 내장 브라우저가 제공한 error/warn 로그는 빈 목록이었다. 전체 실행 시간의 모든 오류가 없었다는 포괄적 보증은 아니다. |

#### 요청별 네트워크 증거와 제한

Supabase Logs Explorer에서 `/rest/v1/user_app_state`의 요청 시간·메서드·응답 상태를 조회했다. 최초 복원 시점 주변에서 다음 서버 요청을 직접 확인했다(UTC).

| 시간 | 메서드 | 상태 |
| --- | --- | --- |
| 13:29:16.703 | GET | 200 |
| 13:29:16.770 | OPTIONS | 200 |
| 13:29:16.789 | PATCH | 200 |
| 13:29:16.908 | GET | 200 |

이는 경로와 시간으로 대조한 서버 로그이며 각 요청을 브라우저 요청 ID나 인증 사용자 필터로 일대일 연결한 캡처는 아니다. 응답 본문은 이 로그에 없어, 복원된 운동 배열·수정 메모·증가/교체 설정·최종 정리 결과를 별도의 읽기 전용 서버 조회와 UI로 대조했다.

추가 로그 조회는 `Backend error! Retry your query`로 반복 실패했다. Vercel 로그에서는 인증된 앱 진입 주변의 `POST /api/pin` 및 운동 코치 이력 조회 200과, 고정 미리보기 로그인 대기 시점의 `GET /api/ai/usage` 401을 관찰했다. 이 401은 운동 저장 경로 응답이 아니며 오류 원인을 단정하지 않는다. **수정·삭제·각 반복 동기화의 모든 요청 및 응답을 개별 연결하는 네트워크 검증은 부분 완료로 남긴다.**

#### 독립 세션과 남은 작업

브랜치 주소와 고정 배포 주소는 서로 다른 origin이므로 앱 저장공간을 공유하는 같은 주소의 새 탭과 구별된다. 고정 배포 탭에서 별도로 로그인이 필요한 것을 확인하고 사용자에게 같은 검증 계정의 로그인을 요청했으나, 종료 시점에도 로그인 화면이었다. 로그인 정보를 복사하거나 인증 상태를 우회하지 않았다.

따라서 **독립 세션에서의 최초 다운로드, A/B 양쪽의 서로 다른 날짜 저장, 수정·삭제 전파 및 충돌 보존은 미검증**이다. 별도 기기가 없어도 고정 배포 탭의 정상 로그인이 완료되면 독립 저장공간 간 검증을 이어갈 수 있다. 동일 origin 새 탭·새로고침·관리자 서버 조회로 이 항목을 대체하지 않는다.

남은 절차는 (1) 독립 저장공간 탭의 같은 테스트 계정 로그인 후 양방향 시나리오, (2) 로그 조회 복구 후 각 동작의 요청/응답 일대일 대조다. 재개 시 원본 기준을 다시 확보하고 합성 데이터만 복원한다. 현재 계정은 테스트 전 상태로 정리돼 있다. PR은 draft를 유지하며 실사용 검증 전체 완료나 운영 반영 가능으로 표시하지 않는다.

### 독립 세션 재개 시도 및 네트워크 증거 보완 (2026-09-08)

기존 통과 결과는 유지한다. 사용자가 독립 탭 로그인 완료를 알린 뒤 두 탭을 다시 연결했다. 브랜치 탭은 인증된 운동 화면이었으나, 고정 미리보기 `ai-fitness-6yeiacsha` 탭은 새로고침과 로딩 완료 후에도 이메일·비밀번호 로그인 화면이었다. 정확한 고정 주소에서 같은 계정으로 로그인하도록 요청했다. 따라서 양방향 저장·수정·삭제 검증은 시작하지 않았고 새 합성 데이터나 설정 변경도 하지 않았다.

읽기 전용 서버 대조에서 원본 기록 4일 및 운동 설정의 해시가 이전 정리 완료 기준과 모두 일치했다. 이번 재개 시도에서 정리할 새 테스트 데이터는 없다.

**로그 오류 원인 정정:** 이전 절의 `Backend error! Retry your query`는 당시 관찰한 화면 오류다. 후속 확인에서 로그 편집기의 입력 교체가 기존 SQL 뒤에 새 SQL을 이어 붙이는 것을 발견했다. 전체 선택·삭제 후 단일 SQL을 입력하고 확인하니 조회가 성공했다. 따라서 앞선 실패를 Supabase 서비스 장애로 해석하지 않는다.

Supabase Logs Explorer에서 `/rest/v1/user_app_state` 요청의 접속 주소(Referer), 시각, 요청 식별자, 메서드, 응답 상태를 추가로 확인했다. 아래 요청은 모두 브랜치 미리보기 주소에서 발생한 PATCH이며 상태는 각각 200이었다(UTC). 동작 구분은 이전 검증 시각과 대조한 것으로, 브라우저에서 캡처한 요청 ID와 직접 연결한 자료는 아니다.

| 시각 | 요청 식별자 | 이전 검증 시점 |
| --- | --- | --- |
| 13:51:36.328 | `01a08149-db88-7c8c-99e9-b2bf843f5bd1` | 과거 계획 검증용 날짜 예외 복원 |
| 13:57:29.910 | `01a0814f-40b6-776c-ac70-685087aacb06` | 증가 적용 설정 동기화 |
| 14:15:26.539 | `01a0815f-ae4b-781e-826e-58c216fac33d` | 교체 적용 설정 동기화 |
| 14:26:25.454 | `01a08169-bc2e-73ad-8aaf-aa7089015150` | 세 번째 45세트 백업 복원 |
| 14:28:33.036 | `01a0816b-ae8c-742c-81ec-f1536cb27c14` | 합성 기록 정리 |
| 14:29:01.074 | `01a0816c-1c12-7020-a252-1ea93ce1d2fa` | 원래 설정 복원 |

정리 후 재조회 GET도 14:31:27.730(`01a0816e-58f2-70b9-955a-cb76ebcfebef`), 14:31:27.817(`01a0816e-5949-7848-a73f-3d6fbdb1a9eb`)에 각각 200이었다. 반환 데이터 내용은 앞 절의 UI 재조회 및 별도 읽기 전용 서버 JSON 일치 결과로 확인했다. 로그에는 응답 본문이 없어 개별 요청 본문과 반환 본문 전부를 직접 대조했다고 표시하지 않는다.

독립 탭의 정상 로그인 후 양방향 동기화와 그 시나리오의 요청별 대조는 계속 미완료다. 검증 탭을 보존했고 PR은 draft를 유지한다.

### 독립 저장공간 양방향 동기화 완료 (2026-09-08~09 KST)

이 절이 양방향 동기화 및 요청별 네트워크 대조의 최신 결과다. 위의 로그인 대기 상태를 해소했으며 기존 45세트 반복 복원·과거 계획 보존 등 통과 결과는 유지한다.

- A: 브랜치 미리보기 origin. B: `https://ai-fitness-6yeiacsha-jace3695s-projects.vercel.app` 고정 미리보기 origin.
- B에서 같은 검증 계정의 정상 로그인을 실제 화면으로 확인했다. 서로 다른 origin의 앱 저장공간을 사용하고, 서버 로그의 Referer도 각 주소로 구별된다. 같은 origin 새 탭의 저장공간 공유를 독립 세션으로 계산한 결과가 아니다. 별도 물리 기기/브라우저 프로필 검증은 아니다.
- 앱 동작 기준은 `45c7794` 수정 코드다. 이후 PR 브랜치 변경은 검증 문서 추가 커밋뿐이다.
- 검증 구간: 2026-09-08 14:55:00~15:02:31 UTC (한국 시간 9/8 23:55~9/9 00:02). 원본 기록 전체와 운동 설정 전체의 서버 JSON 비교 기준을 먼저 확인했다. 설정은 이번 검증에서 직접 변경하지 않았다.

| 흐름 | 실제 확인 결과 |
| --- | --- |
| A 생성 → B 재조회 | A에서 9/2에 `QA-PR188 A 동작` 1세트·8회와 합성 메모를 저장했다. B에서 동일한 날짜·운동·횟수·메모를 상세 화면으로 확인했다. |
| B 생성 → A 재조회 | B에서 9/4에 `QA-PR188 B 동작` 1세트·10회와 합성 메모를 저장했다. A에서 재조회했고 9/2 기록도 함께 유지됐다. |
| 양쪽의 서로 다른 날짜 수정 | A가 9/4 메모를, B가 9/2 메모를 수정했다. 두 저장 요청이 모두 성공했고 서버에는 두 수정값이 함께 남았다. A에서 B 수정값, B에서 A 수정값을 확인했으며 새로고침·동기화 후에도 유지됐다. 수정 전 운동을 별도 항목으로 추가하지 않았다. |
| A 삭제 → B 재조회 | A에서 9/2 합성 운동을 삭제했다. B에서 해당 날짜에 기록이 없음을 확인했고, 9/4의 수정된 기록은 유지됐다. |
| B 삭제 → A 재조회 | B에서 남은 9/4 합성 운동을 삭제했다. A에서 동기화 후 삭제 결과를 확인했다. |
| 정리 후 양쪽 새로고침 | A와 B 모두 9월 0세트·0회, 9/2 및 9/4 상세 기록 없음이었다. 삭제 기록은 재등장하지 않았다. |
| 원본 및 설정 보존 | 최종 서버에는 원래 4개 날짜만 남았다. 운동 기록 전체와 운동 설정 전체의 JSON 해시가 모두 검증 시작 전과 정확히 일치했다. 이번에 만든 합성 데이터는 전부 정리됐다. |
| 콘솔 | 최종 확인 시 양쪽에서 제공된 error/warn 로그는 빈 목록이었다. |

동기화 중에는 로컬의 이전 메모가 잠시 표시됐다. 완료 전 스냅샷을 최종 상태로 계산하지 않고 서버 저장 및 재조회가 끝난 뒤 확인했다. 이는 온라인 상태의 서로 다른 날짜 변경과 순차 삭제 검증이며, 같은 날짜를 양쪽에서 동시에 수정하는 충돌이나 오프라인 충돌 전반을 검증했다는 뜻은 아니다.

#### 요청별 네트워크 대조

Supabase Logs Explorer에서 `/rest/v1/user_app_state`를 위의 고정 시간 범위로 조회했다. A는 GET 22/PATCH 3/OPTIONS 3, B는 GET 19/PATCH 4/OPTIONS 5로 **총 56건이 모두 200**이었다. 이 경로·주소·시간 범위에서 4xx/5xx는 없었다. 앱의 다른 모든 API를 검사했다는 뜻은 아니다.

| 동작 | 세션 | UTC 시각 | PATCH 요청 식별자 | 상태 |
| --- | --- | --- | --- | --- |
| 로그인 후 초기 동기화 중 기록된 저장 | B | 14:55:48.621 | `01a08184-a38d-7bf0-86e2-7776bc82f22a` | 200 |
| 9/2 생성 | A | 14:56:18.744 | `01a08185-1938-7e8e-82dc-43dde87a23c6` | 200 |
| 9/4 생성 | B | 14:57:10.743 | `01a08185-e457-756a-81a3-f700f2b6f574` | 200 |
| 9/4 수정 | A | 14:58:28.774 | `01a08187-1526-71a1-bec6-ebae466d41ee` | 200 |
| 9/2 수정 | B | 14:58:29.688 | `01a08187-18b8-7441-a436-347610cf4de2` | 200 |
| 9/2 삭제 | A | 15:00:36.067 | `01a08189-0663-705b-9a99-1cccab0418da` | 200 |
| 9/4 삭제 | B | 15:01:47.784 | `01a0818a-1e88-7b44-9b90-5a5d981d066f` | 200 |

최초 A 생성 이후 B GET `01a08185-1ab9-7649-8423-c2a786513d10`(14:56:19.129), B 생성 이후 A GET `01a08186-0925-72de-9dd7-685f6da7f312`(14:57:20.165), 최종 삭제 이후 A GET `01a0818a-4750-7ddb-b385-cc8e0a1a373f`(15:01:58.224), B 재접속 GET `01a0818a-86bc-7315-be04-284b31230a83`(15:02:14.460)도 각각 200이었다.

요청 식별자·시각·메서드·Referer·상태는 서버 로그 증거이며, 각 동작의 기대 데이터는 상대편 UI와 읽기 전용 서버 조회로 대조했다. HAR나 개별 HTTP 응답 본문 전체를 직접 캡처한 결과는 아니다. 토큰·인증 헤더·원본 개인 기록을 보고서에 싣지 않았다.

요청받은 온라인 양방향 저장·수정·삭제 및 해당 동기화 경로의 요청별 성공/기대 결과 대조는 이 범위에서 통과했다. PR의 기존 통과 이력을 유지하며, PR 상태 변경·병합·운영 배포는 수행하지 않았다.

## P1 점진 조정 확장과 클라우드 재검증 — 2026-09-12 KST

기존 반복수 제안에 중량·밴드·라운드의 보수적 한 단계 진행을 추가했다. 같은 계획과 같은 저항으로 최근 3회가 모두 안전하고 여유 있게 완료된 경우에도 한 번에 한 변수만 바꾼다. 반복수를 12회까지 1회씩 먼저 올리고, 이미 12회인 한 운동은 기록된 덤벨을 0.5kg 또는 밴드를 약→중→강 중 한 단계만 올린다. 새 동작이 있는 2라운드 계획은 같은 조건에서 3라운드까지만 한 번 올린다.

중량·밴드·라운드는 준비 가능한 장비와 현재 통증·저림 없음 확인 전에는 적용할 수 없다. 확인한 중량·밴드 목표는 다음 정상 강도 운동의 세트에만 미리 채우며 회복 모드에는 적용하지 않는다. 통증·악화·저림·근력 저하·피로 신호가 있으면 기존 보류 규칙이 우선한다.

[첫 클라우드 실행](https://github.com/jace3695/AI-fitness-app/actions/runs/34699383725)에서는 화면 적용 뒤 5.5kg 목표가 실제 DB에 15초 안에 도달하지 않아 20개 중 해당 1개가 실패했다. 운동 설정 저장이 공통 동기화 변경 이벤트를 보내지 않아 30초 주기 동기화까지 늦어지는 앱 결함으로 확인했다. 저장 직후 변경 이벤트를 보내도록 수정하고 단위 검사를 추가했다.

[수정 후 클라우드 실행](https://github.com/jace3695/AI-fitness-app/actions/runs/34699891173)은 단위 검사 233개, VM 회귀 25개, lint, TypeScript, 47개 경로 production 빌드와 실제 브라우저 20개가 모두 통과했다. 운동 사례는 합성 최근 3회 기록 → 0.5kg 단일 제안 → 안전 확인 전 적용 차단 → 확인 후 5.5kg 서버 반영을 실제 Auth/PostgREST와 Chromium에서 확인했다. 해당 사례의 요청 5건은 모두 실제 응답이었고, 종료 후 계정과 관련 행은 0건이었다. 실제 iPhone 검증은 전체 개선의 마지막 단계로 유지한다.

## 참고

- ACSM, Resistance Training Guidelines Update 2026: https://acsm.org/resistance-training-guidelines-update-2026/
- NHS, Back pain: https://www.nhs.uk/conditions/back-pain/
- WHO, chronic primary low back pain guideline: https://www.who.int/news/item/07-12-2023-who-releases-guidelines-on-chronic-low-back-pain

근력 운동과 상태에 맞춘 운동의 일반 원칙을 참고했다. 위 문헌이 앱의 3회·6회·14일 같은 개별 판단 수치를 검증했다는 의미는 아니다.
