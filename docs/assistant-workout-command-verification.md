# 운동 완료 명령 — 2026-09-16

6단계의 운동 완료 명령을 확인·이력·되돌리기 흐름으로 연결한다. 원래 6단계 전체는 부분 완료이며 식단과 다른 기록 명령은 후속이다.

## 지원 범위

- 연이 대화 또는 빠른 명령에 `오늘 운동 완료했어` 입력 → 날짜·변경 내용 확인 → 확인하고 저장.
- 한국 시간의 오늘에 사용자가 운동을 완료했다는 사실을 `직접 완료 기록`으로 저장한다. 휴식일에도 실제 수행한 사실을 기록할 수 있다. 계획을 수행했다거나 계획의 모든 동작·횟수·세트를 마쳤다고 추정하지 않는다.
- 운동 종류·세트·횟수·통증·피로·난이도는 자동으로 채우지 않는다. 사용자 지정 계획·날짜별 계획과 유산소·철봉·폼롤러 기록을 유지한다.
- 이미 완료한 날은 추가 저장하지 않는다. 일부 완료·중단·통증·세트 등 일반 운동 상세 기록이 있는 날은 운동 화면에서 확인하도록 안내한다.
- 과거·미래 날짜, 부정·조건·질문·상세 내용이 섞인 명령은 저장하지 않는다. 계획 변경이나 수행 중인 세션의 완료 기능은 이번 명령에 포함하지 않는다.
- 공통 실행 이력의 운동 탭에서 변경 전후·시각을 조회하고 다시 확인하여 되돌린다.

## 저장·복구

- 로그인한 본인의 RLS 권한과 invoker 함수로 기록과 이력을 한 트랜잭션에 저장한다. 인증 키 추가나 유료 호출이 없다.
- 요청 UUID는 같은 내용으로 재시도하면 같은 영수증을 반환한다. 응답 유실·새로고침 후에도 같은 탭에서 대기 요청을 복구한다. 이미 실행한 요청은 만료 후에도 결과를 재조회할 수 있다.
- 확인은 15분간 유효하다. 한국 시간 날짜가 바뀌거나 운동/비서 기록이 초기화되면 미실행 요청을 거절한다.
- 확인한 오늘의 기록만 비교·수정한다. 다른 날짜나 설정의 후속 변경은 보존한다. 같은 날짜의 유산소 등 어떤 기록이든 이후 달라졌으면 되돌리기를 거절한다.
- JSON 문자열 형식의 이전 저장 자료도 지원한다. 문자열의 공백·키 순서는 정규화될 수 있으나 날짜와 값은 보존한다.
- 운동 또는 비서 초기화 시 실행 이력도 지운다. Auth 계정 삭제는 기존 외래키 cascade를 사용하며 Auth 내부 역할에 앱 권한을 추가하지 않는다.

## 검증과 수정 이력

- 로컬 단위·DB **412개**(새 운동 검사 16개), 타입·lint·VM 동기화 **25개** 통과. 원자적 이력 실패 시 rollback, 재시도·중복·만료·날짜 변경, 오늘의 충돌, 다른 날짜 보존, 상세 기록 차단, 초기화·RLS·제한된 Auth 역할의 계정 삭제를 포함한다.
- 최초 코드 `2efc6b813f3729f4ac49c0368d637eba4dae277c`의 [CI 35082324266](https://github.com/jace3695/AI-fitness-app/actions/runs/35082324266)은 단위/DB·코드 검사·독립 빌드 통과, 브라우저 **96/102 통과**였다. 운동의 실제 저장·응답 유실 복구·달력·되돌리기는 양쪽 브라우저에서 통과했다.
- 새 검사 4건은 충돌한 대기 요청을 닫지 않은 채 다음 명령의 확인창 부재를 검사하거나 Next.js 경로 안내까지 포함한 오류 locator를 사용하여 실패했다. 실패한 확인창을 정상 UI로 닫고, 운동 이력 영역의 오류만 검증하도록 수정했다.
- 기존 WebKit 검사 2건은 `/assistant` 탐색의 `load` 이벤트 대기와 고정 메뉴에 가려진 가계부 checkbox 클릭에서 실패했다. 전자는 DOMContentLoaded 뒤 기존 명령 입력창 활성 상태로 준비 완료를 검증하고, 후자는 화면 중앙으로 스크롤한 뒤 실제 pointer check를 유지한다. 강제 클릭·검사 제외·재시도 확대는 하지 않았다.
- 운동 화면이 조회하는 기존 `fitness_ai_review_history`의 실제 스키마를 격리 fixture에 추가해 누락된 읽기 의존성을 해결하고 정리 대상에도 포함했다.
- 첫 실행의 합성 계정 **125개**, 추적 행 잔여 **0**, 외부 origin **0**, 검증 DB·볼륨·키 정리 완료.
- 수정본 `1fa1149f6771083bd0c10ff5c8eb91466d329870`의 [최종 CI 35083616431](https://github.com/jace3695/AI-fitness-app/actions/runs/35083616431)에서 단위/DB **412개**·VM **25개**·lint·타입·독립 빌드·브라우저 **102/102**를 통과했다. 재시도 0회, worker 1개이며 새 운동 흐름 3종을 Chromium과 WebKit에서 각각 검증했다. 최초에 실패했던 기존 학습·가계부 흐름도 통과했다.
- 최종 실행의 cleanup 보고 **102건**, 합성 계정 **125개 삭제**, 추적 행 잔여 **0**, 외부 origin **0**을 확인했다. 검증 DB·볼륨·임시 키 제거와 정제된 증빙 게시도 성공했다.

## 호스팅 Preview 확인

- 기능 코드 Preview `dpl_4615gZMcwpMWBjJZAwzEAzf2jRf6` READY, SHA `2efc6b8…`, Preview 대상 확인.
- 검사 수정본 Preview `dpl_5k4LrdbY52o9j6tdtzb3Pe81ZPfK`도 READY, SHA `1fa1149…`, Preview 대상 확인. 검사 fixture·탐색 절차만 보완했으며 실제 검증한 앱 코드와 DB 함수는 같다.
- 설치 전후 기존 **16개 테이블**의 전체 행 수·해시가 모두 같았다. 새 owner RLS·invoker 구조의 보안 advisory는 없으며 기존 내부 차단 테이블 안내 및 Auth 설정 경고는 그대로다.
- 허가된 테스트 계정에서 `오늘 운동 완료했어` 입력 후 확인 화면을 관찰했다. 저장 버튼 전 앱 상태 해시 `2486b67dacd29efd4d098bf36a85b19b`와 운동 이력 0건이 유지됐다.
- 2026-09-16 **10:02:25 UTC** 확인 저장: 오늘의 `workoutDone`, `workoutStatus`, `workoutRoutineName`, `workoutRecordedAt` 네 필드만 생성됐다. 날짜 외 앱 상태 해시는 원본과 같고, DB 값과 실행 이력이 일치했다.
- 운동 앱의 달력에서 `직접 완료 기록`, `운동 후 허리: 미기록`을 확인했다. 새로고침 뒤에도 같은 상태와 서버 저장 확인을 관찰했다.
- 공통 운동 실행 이력에서 **10:06:38 UTC** 확인 후 되돌렸다. 오늘 기록이 제거되고 전체 앱 상태·기존 운동 저장소 해시가 원본과 일치했다. 달력에서도 다시 `운동 기록 시작하기`가 표시됐다. 동기화용 `updated_at`은 정상적으로 증가시켰으며 과거 값으로 되감지 않았다.
- 테스트 대화 2건은 정확한 ID·계정·내용·생성 시각으로 한정하여 정리했다. 되돌린 테스트 이력 1건도 요청 유효기간이 지난 뒤 원본 상태 해시와 저장·복구 시각을 대조해 정리했다.
- 최종 재조회에서 테스트 대화·운동 이력 모두 0건, 앱 상태·운동 저장소 해시 원본 일치를 확인했다. 공통 실행 이력 화면에서도 빈 운동 이력이 정상 표시됐다.
- 실제 Google 음성·AI 호출 0회, Zephyr 예약 문자수 **38자 유지**. 무료 모드·월 승인·분석 예약은 변경하지 않았다.

운영 병합·실제 iPhone 검증은 마지막 단계다. 이 결과는 운동 계획 변경, 일부 수행의 세부 편집, 식단 명령 또는 6단계 전체 완료를 뜻하지 않는다.
